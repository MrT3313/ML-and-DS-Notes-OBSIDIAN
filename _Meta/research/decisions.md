---
note_kind: meta
title: "Decision log"
date: 2026-09-14
---

# Decision log

One row per decision. Status is relative to the previous Charter (2026-09-13). "Reasoning" means the Charter's own judgment, labelled as such there. Memo references: A = [[track-a-obsidian-practice]], A-tools = [[track-a-tooling-bases]], A-stem = [[track-a-stem-atomicity]], B = [[track-b-domain-hierarchies]], C = [[track-c-adversarial-review]]. Decisions marked **user** were put to the user as a fork and chosen by them on 2026-09-14.

## Previous inventions (brief section 6C)

| # | Item | Decision | Status | Evidence or reasoning | Confidence | What would change it |
|---|---|---|---|---|---|---|
| C1 | Four tests (closed, stable, exhaustive, disjoint) | Dropped. Replaced by a closed folder list plus a placement rule with a designated arbiter. | **discarded** | Unsourced. The previous tree failed its own disjoint test at nearly every boundary on Track B's own cross-cutting table (C attack 1). Cross-cutting is a property of the domain, so a disjointness test can never pass; what a person needs at filing time is a decidable order of precedence, not a partition proof. | high | A folder taxonomy of ML that is actually disjoint |
| C2 | Kind / subject vocabulary; avoid "type" | Kept, and extended: `source_type` renamed `medium` so the word "type" appears nowhere in the schema. | **modified** | Obsidian overloads "type" for property data types (A-tools §1); C attack 2 caught the leftover. | high | Nothing likely |
| C3 | Two-place tie-breaker (mechanism, then more general) | Replaced by a four-step precedence: principle versus family-specific; then PML1's ToC; then PML2, Bishop 2024, Géron; then most general, flagged. | **discarded, replaced** | The mechanism rule contradicted its own PCA example and gave hostile homes for attention, boosting, dropout (C attack 3). A designated arbiter text is decidable in seconds and stable for years; PML1 is the most recent full-coverage family-axis reference (B §1). | medium | A reference text with better coverage of new subfields displacing PML1; or the principle/family test failing on a real case |
| C4 | Seven-note threshold for a new folder | Dropped for top-level areas (closed list, created on first note). Subfolder rule: parent over about 25 notes, subfolder takes at least five, name from an arbiter ToC. | **modified** | Seven was arbitrary and self-violated (C attack 4). The screen-height number is also arbitrary but has a stated reason; the ToC-name requirement is what removes most of the arbitrariness. Reasoning. | medium | Practice showing 25 is too high or too low |
| C5 | Three-level depth cap | Kept in effect (area, subfolder, note) with a rationale: reference texts have part, chapter, section, and a section is note-sized. | **modified** | Reasoning, now stated. | medium | A subfolder that genuinely needs a fourth level |
| C6 | Confidence ladder stub / draft / working / solid | Two values, `draft` and `solid`, plus a `verified` date and a Bases view that flags solid notes edited after verification. | **modified**, **user** | Three of four rungs measured completeness; the one field evidence says completeness ladders go unmaintained (A-tools §4); `solid` decayed silently on revision (C attack 6). The `verified` date turns decay into an automatic list. | medium | A run showing the mtime comparison fires too often to be useful; or evidence that ladders are maintained after all |
| C7 | Title split (noun for objects, claim for results) | Simplified: noun phrases for every Knowledge note, claim in the first body line. | **modified** | Still reasoning; no source addresses explorer sorting (A §2). Dropping the object/result judgment removes a per-note decision (C attack 7). | medium | A source showing claim titles work in an alphabetical file list |
| C8 | Quality bar sections and word counts | Cut to the minimum viable sections; lengths roughly halved; "Sources and where they disagree" no longer blocks completion when only one source exists. | **modified** | C attack 8 estimated 6,000 to 10,000 words per Géron chapter under the old bar. Reasoning, calibrated to one chapter per week. | medium | A logged chapter showing the new bar is still too heavy, or too thin to be useful |
| C9 | Eight kinds closed list | Eight kinds, different eight: `implementation` removed (code is a method section), `reference` added for datasets, libraries, tools, benchmarks. Comparisons and derivations are concepts; a whole book is a source note that chapter notes point up to. | **modified**, **user** | Implementation could never be cited without its method, failing the split test (A §6, C attack 9). Datasets and tools had no home. | high | A kind of artefact that fits none of the eight |
| C10 | "Top level splits by kind" (internal inconsistency) | Justification fixed, not the design: the top level splits by workflow role; subject sits underneath; the hybrid is named. | **modified** | C attack 10. Multi-year folder users converge on exactly this hybrid (A §7). | high | Nothing likely |
| C11 | Method family as the subject axis; task lookup as casualty | Kept, with two additions: `Generalization/` and `Workflow/` as non-family areas, and `Maps/` as the visible home for task and reading-order lookup. Lifecycle, task, and purpose axes were presented and declined. | **modified**, **user** | Every graduate reference text files by family with foundations front-loaded (B §1); teaching order scatters concepts (B §10); Géron's lifecycle chapters have no home in any family tree (B §7); PML2's purpose axis absorbs new subfields but splits families (B §3). Maps in a visible folder are Milo's Atlas pattern (A §3). | high | Year-three evidence that the learner needs task lookup more often than family lookup |
| C12 | Weekly and milestone habits | Cut to three weekly items, ten minutes, one of which is automated by a base. | **modified** | C attack 12; reasoning. | low | A month of logs |
| C13 | No `topics` property | Kept, on new grounds: Bases has `file.backlinks` and `file.hasLink()`, so a self-populating "linked here" view needs no property. Map notes carry it. | **kept**, **user** | The previous justification ("Bases cannot query backlinks") was outdated (A-tools §5). Steph Ango's `categories` property was the alternative; declined because a property drifts and a link does not (A-tools §2). | medium | The `hasLink` view proving too slow or too often stale in a large vault |

## Structural questions left open before (brief section 6D)

| # | Item | Decision | Status | Evidence or reasoning | Confidence | What would change it |
|---|---|---|---|---|---|---|
| D1 | Neural Networks as a sibling versus a parallel subtree | Sibling area, with one level of subfolders allowed inside it. | **kept, clarified** | Deep learning is a chapter, a part, or a book depending on the source (B §6); subfolders let the area grow to Bishop 2024's twenty chapters without moving anything. | medium | The area exceeding what one subfolder level can organize |
| D2 | Generative models | Inside `Neural Networks/` now; first expected subfolder promotion. Not created empty. | **kept, with a trigger** | Fastest-growing division (B §9); Bishop 2024 files them at the end of a deep-learning book; PML2 as a Part. | medium | Their treatment as a probabilistic rather than neural topic in the user's own reading |
| D3 | Graphical models | `Probabilistic Models/`. | **kept, flagged** | Four-way disagreement (B §6). PML2 treats them as a fundamental; PRML as a family. Flagged in the Charter. | low | Nothing decisive exists |
| D4 | Regularization | `Generalization/` for the principle; family-specific techniques (dropout, batchnorm) with their family. | **modified** | The worst cross-cutting offender (B §4); the CS229 notes and ESL both have a dedicated generalization unit. Reasoning for the principle/technique split. | medium | A technique that resists the "exists outside this family?" test |
| D5 | Fairness, interpretability, causality, MLOps in an empty `Practice/` | `Workflow/` replaces `Practice/` and is *not* created empty: Géron chapter 2 fills it in week two. Causality moves to `Probabilistic Models/`. | **modified** | C attack 14; B §7 (fairness lectures exist but never a parent unit; causality sits with graphical models in PML2 4.7). | medium | A textbook giving fairness a home |
| D6 | Foundations as a peer versus nested | Peer area inside `Knowledge/`. | **kept** | Every theory text front-loads it (B §8). | high | Nothing likely |
| D7 | Sources by media type | Kept: `Books/`, `Courses/`, `Papers/`, `Videos/`, `Web/`, with one subfolder per multi-part source (a book or a course). | **kept** | The existing study prompt already uses `Sources/Books/HOML/`. C attack 15 (a course with a textbook splits) is accepted: each is its own source note and they link. Media folders duplicate `medium` in the property, which is the price of clicking. Reasoning. | medium | Fewer than a dozen sources per medium after two years, in which case flatten |
| D8 | Glossary.md | Deleted (already removed in the working tree). Aliases on concept notes plus `Maps/` replace it. | **discarded** | Aliases feed search, autocomplete, and unlinked mentions (A-tools §3). | high | Nothing likely |

## Other decisions

| Item | Decision | Status | Evidence or reasoning | Confidence |
|---|---|---|---|---|
| Dataview | Not installed. | **kept**, new grounds | Bases has backlinks since 1.9.7; Dataview's last release was April 2025 with 637 open issues; Datacore is JavaScript-only and self-described as work in progress (A-tools §5, §7). Inline fields would split the metadata store. | high |
| Inbox | Kept as capture only, emptied weekly. | **kept** | C attack 16 predicts it stays empty or becomes a dump; the weekly rule addresses the second. Cheap to delete later. | low |
| Citing several sources on one claim | Allowed: several source links after a claim where sources agree; divergence in prose. | **modified** | The previous "do not stack citekeys" rule contradicted Sascha Fast's own example (A §1 row 6). | high |
| Split-on-concept-count attribution | Kept as a rule, re-attributed: a forum user's rule for structure notes, not Sascha's rule for content notes. | **modified** | A §1 row 3. | high |
| Matuschak "abandoned evergreen notes" | Not asserted anywhere. | **removed** | Unverifiable; primary text paywalled (A §1 row 7). | high |
| STEM atomicity rules | Seven rules in Charter section 5, labelled reasoning, each tied to an analogue (ProofWiki, PlanetMath, mathlib, Halmos, Lamport). | **new** | No published source exists on personal notes for proofs (A-stem). | low |
| `verified` date | Added to the schema. | **new**, **user** | See C6. | medium |
| `title` property | Only on `meta` notes. | **new** | Filename is the title; the previous Charter and template carried `title` without listing it (C attack 17). | high |
| Template | `Chapter Notes.md` aligned to the schema: `book` replaced by `up`, `Github` replaced by `url`, `medium` and `aliases` added. | **modified** | C attack 17. | high |
| Track C memo | Kept as its own file rather than folded into the summary. | **kept** | It is the audit trail for every "discarded" and "modified" row above; folding it would hide the attacks the design answered. | high |
| PML1 as arbiter | Designated. | **new** | See C3. If PML1 is superseded, the arbiter changes by Charter amendment and nothing moves until then. | medium |
| Study prompt | Section references unchanged (5 and 8 still hold); sign-off now sets `verified` as well as `confidence`; single-note variant unchanged. | **modified** | Brief section 8. | high |
