# Slip box integration prompt

Paste into a fresh conversation with an agent that has file access to the vault and can launch subagents. Fill in the parameter block at the top; nothing below it names a subject, a book, a library, or a folder, so the parameter block is the whole of adapting this prompt to a new source. The prompt has one approval gate, after the plan and before any file is written. To run it end to end without stopping, delete the single line that begins "GATE:".

This is a different role from `Chapter-Study-Prompt.md`. The study prompt's agent is an interrogator and never writes note bodies. This prompt's agent moves notes I already wrote in my own words from the slip box into their permanent place, restating and completing them. Everything it adds beyond what the raw file says is reported back to me at the end so I can read it as a reviewer, not a reader.

One prompt serves every subject. A subject with no domain under `Knowledge/` yet is handled by the FIRST RUN branch in Phase 3c, which is the only place in the prompt that behaves differently for a new subject than for an established one.

---

```
You are the coordinator agent for a knowledge-integration task in an Obsidian vault. The vault root is the current working directory. You direct subagents, you own the plan, and you own the final verification. The subagents do the reading of the repository and the writing of the notes. You personally read only the files named in Phase 2 and Phase 3, plus whatever you need to verify the result.

PARAMETERS.

- Raw notes to integrate: [`_Slip Box (Zettelkasten)/RAW DMLS Ch04 Training Data.md`, the only file in the slip box. 183 lines, the last of which carries no trailing newline, 32 headings running three levels deep (six `##`, twelve `###`, fourteen `####`), two tables, no code block and no mermaid block. The file contains no dash characters of either kind and no "e.g.", both checked, so those two normalizations are discharged before the run starts and finding either in the final vault means a subagent introduced it. Five atom shapes are present by inspection and are named here so they are not missed rather than decided here. `## Types` at line 7 is the empty atom of this run: it holds nothing at all and is followed immediately by `### Nonprobability Sampling`, which makes it pure scaffolding rather than a heading I failed to fill, and its disposition is still a decision taken in the plan. `### Random Sampling` at line 21 is the same shape one degree less empty, carrying nothing of its own before its first subsection, and it is worse than the equivalent atom in the previous run because `Random Sampling` is already a note title in this vault, so an empty heading is also a collision. `#### Using the right evaluation metrics` at line 144 is the bare-cross-reference atom in its purest form, four bullets that name [[Precision]], [[Recall]], [[F1 Score]] and [[ROC Curve]] and say "_already in my notes_" against each, which is an atom whose disposition is a link and possibly a backlink, never a note. The fourth shape is a structural error in the file rather than a gap in it: `#### Algorithm-Level Methods` at line 164 is the category, and its three instances, `#### Cost-Sensitive Learning`, `#### Cost-Balanced Loss` and `#### Focal Loss`, sit beside it at the same heading level instead of beneath it, so the raw file's own hierarchy misrepresents what it says. The plan restores the containment rather than preserving the flat run of four headings. The fifth shape has no counterpart in any previous run and is the most important one here: line 124 reads "Question: isnt this data augmentation?" and is a question addressed to the vault rather than a claim. It is answered in a note and in the final report, never dropped, and the answer is a real distinction rather than a yes or a no, since the perturbation is the same operation in both places and the purpose is not. One further atom carries an error rather than a gap, and it is flagged here because a reader skims past it: lines 81 and 82 are near-duplicate bullets, both saying that an unclicked recommendation is presumed bad, and they attach opposite names to that one fact, `_explicit label_` on the first and `_implicit label_` on the second. One of the two is wrong and the raw file cannot say which, so this is a research item and a correction, not a transcription slip.]
- Chapter note to produce: [`Sources/Books/DMLS/DMLS Ch04 Training Data.md`. The file does not exist at all, so the action is CREATE outright, as in all three previous runs. Unlike the previous run there is no spelling to reconcile: the raw file already says `Ch04` zero padded, which matches every chapter note in the vault and matches [[DMLS]]'s own `## Chapters` list verbatim.]
- Chapter number, for the `chapter:` key: [4]
- Chapter note `url:`: [https://github.com/chiphuyen/dmls-book/blob/main/resources.md#chapter-4-training-data. This follows the scheme the ch01 run settled and all three later runs kept, pointing at `resources.md` with a per-chapter anchor rather than at `summary.md`. Check that the anchor resolves and that the section behind it is chapter 4's; if the file has no such heading, fall back to `summary.md` and then to the repository root, and say in the final report which one was used and why. The container note keeps the repository root either way.]
- Container note: [`Sources/Books/DMLS/DMLS.md`, linked as [[DMLS]]. One edit and one only: line 20, `- DMLS Ch04 Training Data`, is plain text today and becomes a wikilink to the note this run produces. The ch01 run already corrected the numbering and checked all eleven titles against the book's own `ToC.pdf`, so that work is finished and is not reopened; a packet that finds itself editing a second line of that file has drifted and says so instead of editing.]
- Subject domain: [`Knowledge/Machine Learning/`, hub at [[Machine Learning]]. The primary domain has changed back. The previous run was the excursion: a machine learning book whose third chapter was almost entirely another domain's, which produced no new note in `Knowledge/Machine Learning/` at all and amended seven there. This chapter returns the book to its own domain and nearly every atom in it needs a fitted model in the sentence to be stated at all, which is the membership test read the other way round. The domain holds 119 notes across ten areas and is by a wide margin the largest in the vault; `Data/` alone holds 24 across four subfolders, `Challenges/`, `Exploration/`, `Preprocessing/` and `Sampling/`, plus three notes at its top level. So the risk inverts from the previous three runs: the danger is no longer a thin domain being overwhelmed by one chapter, it is a chapter's material being scattered across an area that already has opinions about most of its vocabulary. Almost every atom here lands next to a note that already exists, which is why the collision ledger below is the longest of any run so far and why AMEND, not CREATE, is the default action to assume until an atom proves otherwise. Neighbouring domains receive very little this time and one of them receives nothing: a packet that finds itself writing into `Knowledge/Systems Architecture/` has misread the chapter and says so instead of writing.]
- Domain status: [ESTABLISHED and large, which changes what the status costs rather than what it buys. Phase 3c step 0 does not apply and is skipped in full: no domain folder is created, no hub is created, and no new line is added to `Home.md` under `## Areas`. `Home.md` owes this run nothing structural, since `## Areas` already carries the [[Machine Learning]] line and `## Sources` already carries the [[DMLS]] line, and `README.md`'s layout table describes paths rather than counting notes, so it is expected to need nothing; check both rather than assuming. What owes something is the hub, and the amendment there is harder than in any previous run for a reason worth stating in advance. [[Machine Learning]]'s `## Areas` entries are single paragraphs of continuous prose that name the notes in each folder one by one and argue the relations between them, and the `Data/` paragraph is one such sentence run covering all four subfolders at once, ending on `Preprocessing/` and its three jobs. New notes cannot be appended to it. It is rewritten so that it still reads as one argument, and if this run adds a subfolder the paragraph gains a clause rather than a list. The `Paradigms/` paragraph is the second pressure point and is likely to need touching too, since transfer learning is owed by this chapter and both [[Zero-Shot Learning]] and [[Few-Shot Learning]] are already characterized there against a general case the vault does not own. The hub is the single largest amendment in the run and is a packet of its own that goes last, after the packets whose output it has to describe.]
- Library pin: [scikit-learn 1.6, with NumPy 2.x and pandas 2.x alongside it, and `imbalanced-learn` as the second library this chapter forces into view. This reverses the parameter that held for all three previous runs, and the reversal is evidenced rather than assumed: [[Random Sampling]] pins "NumPy 2.x" and "scikit-learn 1.6" in its `## Implementation`, [[Stratified Sampling]] pins "pandas 2.x" and "scikit-learn 1.6", and [[Data Augmentation]] pins "SciPy 1.14" and "scikit-learn 1.6". That is the machine learning domain's de facto pin and every `method` note this run produces carries a version line in `## Implementation` on the same footing. The rider is the honest negative, and [[Data Augmentation]] already models exactly how to write it, opening its implementation section with "**scikit-learn has no data augmentation API**" and then saying where the nearest thing actually lives. Several of this chapter's methods are in that position: reservoir sampling and importance sampling have no scikit-learn implementation at all, weak supervision belongs to a separate project entirely, active learning has no home in the standard stack, and the resampling family (SMOTE, Tomek links, NearMiss, one-sided selection) lives in `imbalanced-learn`, which is a separate package with its own release train and is not part of scikit-learn however often it is described as though it were. An `## Implementation` section that names the wrong package is worse than one that says the stack does not cover this. Where a claim is about an implementation rather than about a method, the version it is true of is named, which is the rule the previous run reached for pandas column-major and which applies here to `class_weight="balanced"`, to `sample_weight`, and to anything asserted about `imbalanced-learn`'s defaults.]
- Quantitative expectation: [High, the highest of the four runs, and for the first time the risk is not that a rung-two note gets discharged but that a rung-one derivation gets paraphrased. Seven atoms carry genuinely checkable content and none of it is owned anywhere in the vault. Reservoir sampling states its own invariant at line 51, that every incoming $n^{\text{th}}$ element is in the reservoir with probability $k/n$, and the induction behind it is four lines and belongs in the note rather than in the report. Importance sampling is the sharpest: the estimator is unbiased because $\mathbb{E}_Q[f(x)P(x)/Q(x)] = \mathbb{E}_P[f(x)]$, and the support condition the raw file states at line 60 is wrong as written, since $Q(x) \geq 0$ is true of every distribution and says nothing; the condition that actually makes the weight defined is $Q(x) > 0$ wherever $P(x) \neq 0$. That is a correction, not a typing slip, and it is the single most checkable error in the file. Weighted sampling carries an arithmetic claim whose numbers do not close: line 38 gives the observed shares as 15 percent and 75 percent, which do not sum to 100, and then asserts a 3x weight ratio, which is reachable from 25 and 75 and from nothing else, so the note states the reweighting ratio $p/q$ as a formula and the example is repaired to the numbers that produce the stated answer. Simple random sampling's rare-class drawback at line 26 is a probability that can be written down rather than gestured at, the chance that a 1 percent sample of a population containing a 0.01 percent class contains no instance of it. Class-balanced loss at line 170 states the weight as inversely proportional to class count, which is one line of algebra away from the exact form and from what `class_weight="balanced"` already computes inside [[Class Imbalance]]. Focal loss at line 172 describes $(1 - p_t)^{\gamma}$ in words and the formula is one line. And feedback loop length at line 85 is a duration, so it is a quantity with units, and a note that never states what is being measured from and to has discharged a rung it was standing on. Two failures carry over from earlier runs: a discharge written where rung two was available, and padding written above a discharge where a genuinely discharged note should be short. The third failure is the one this run is most exposed to, worse than in the previous run because more of the amendment targets are affected. Seven notes this chapter amends already carry the fixed discharge string: [[Semi-Supervised Learning]], [[Nonrepresentative Training Data]], [[Few-Shot Learning]], [[Unreasonable Effectiveness of Data]], [[Continual Learning]], [[Self-Supervised Learning]] and [[Batch Learning]]. An amendment that adds checkable content to such a note while leaving "**Not further quantitative at this depth.**" standing underneath it produces a note that asserts its own emptiness on top of its own content. [[Semi-Supervised Learning]] is the certain case rather than the risky one, since its discharge says in so many words that "a later chapter would add the objective that ties the two parts together" and this is that chapter. That failure is invisible on a section-present check and is caught only by reading the section, so the verification in Phase 5 reads every amended `## Formal statement` in full.]
- Primary sources, in order of trust: [the original papers behind a claim, reached through the book's own `resources.md` at https://github.com/chiphuyen/dmls-book/blob/main/resources.md and through the references in the chapter itself; then the current official documentation of any named library or tool, read for what it does today; then the book's own text if the agent has it. This chapter is unusually well served by primary literature and almost every named technique has one paper that introduced it, so a research item settled from a blog post has no excuse. Vitter's 1985 "Random Sampling with a Reservoir" in ACM TOMS is where algorithm R and the $k/n$ invariant are stated and proved. For importance sampling the primary statement is any standard Monte Carlo treatment, with Owen's "Monte Carlo theory, methods and examples" as the citable one, and the policy-based reinforcement learning use the raw file names at line 61 is Precup, Sutton and Singh (2000) for off-policy evaluation and Schulman et al. (2017) for the clipped ratio in PPO. Weak supervision is Ratner et al., "Data Programming" (NeurIPS 2016) and "Snorkel: Rapid Training Data Creation with Weak Supervision" (VLDB 2017), which is also where the labeling function abstraction and the denoising step the raw file names at line 107 are defined; the raw file's "Best Tool = Snorkel" at line 99 is a ranking rather than a fact and is a research item before it is a sentence. Transfer learning is Pan and Yang, "A Survey on Transfer Learning" (IEEE TKDE 2010), which is the source that separates the settings the vault's existing zero-shot and few-shot notes already position themselves against. Active learning is Settles, "Active Learning Literature Survey" (University of Wisconsin-Madison TR 1648, 2009), which is where uncertainty sampling and query-by-committee are both defined and where the raw file's "disagreement" at line 133 gets its precise name. For class imbalance: Japkowicz and Stephen (2002) is the study behind the claim at line 141 that sensitivity scales with problem complexity and that linearly separable problems are unaffected, and that claim is stated with its conditions or not at all; SMOTE is Chawla et al. (JAIR 2002); Tomek links are Tomek (1976); NearMiss is Mani and Zhang (2003); one-sided selection is Kubat and Matwin (1997); class-balanced loss is Cui et al. (CVPR 2019); focal loss is Lin et al. (ICCV 2017). For perturbation and the adversarial half of it, Szegedy et al. (2013) and Goodfellow et al. (2014). For label multiplicity, the measures of inter-annotator agreement, Cohen's kappa and Krippendorff's alpha, are where "conflicting labels" becomes a number. The 2022 staleness rider from all three previous runs stands and bites in two specific places rather than generally. The last line of the file, that data synthesis is "very far from this working at scale on many problems", is a claim about the state of a field made in 2022 by a book that went to press before the current generation of generative models, and it is either dated in the note or restated as what is true now; leaving it as an undated present-tense claim is the failure. And the weak supervision tool landscape the chapter describes is four years old, so a claim about which tool is best is checked against what exists now and dated if it cannot be. A vendor blog post is not a primary source for a claim about a competitor, and a library's own documentation is a primary source for its API and not for whether its method works. Wikipedia is not a primary source for anything and may be used only to find one.]
- Exemplar notes, read in Phase 2, one per shape this run will produce: [`Knowledge/Machine Learning/Data/Sampling/Random Sampling.md`, the single most important file to read before anything is planned: it is the `method` exemplar for the whole sampling block, it is the note the simple random sampling atom collides with head on, and its frontmatter carries the structural problem this run has to solve, since its `up` is [[Testing Set]] and its subject is carving a split rather than drawing a sample; `Knowledge/Machine Learning/Data/Sampling/Stratified Sampling.md`, the second `method` exemplar, a SAME THING amendment, and the note that shows what a full hyperparameter table looks like in this domain; `Knowledge/Machine Learning/Data/Challenges/Class Imbalance.md`, the exemplar of a `concept` with a real `## Formal statement` and an optional `## VS`, and simultaneously the largest single amendment target in the run, since it owns the priors, the imbalance ratio and the metric argument and says nothing at all about any of the seven remedies this chapter brings; `Knowledge/Machine Learning/Data/Data Augmentation.md`, the exemplar of a long `method` note and of an `## Implementation` section that says honestly that the standard library does not do this, and the note the entire final section of the raw file collides with; `Knowledge/Machine Learning/Paradigms/Supervision/Semi-Supervised Learning.md`, the exemplar of a short honest note that defines its subject and discharges its formal statement, and the note whose discharge this chapter makes false; `Knowledge/Machine Learning/Paradigms/Supervision/Few-Shot Learning.md` and `Knowledge/Machine Learning/Paradigms/Supervision/Zero-Shot Learning.md`, both read in full and both read for one thing in particular, the paragraph in each that positions it against transfer in general, because the transfer learning note this run owes has to be consistent with two notes that already described the general case carefully without naming it; `Knowledge/Machine Learning/Data/Challenges/Nonrepresentative Training Data.md`, which claims the bare aliases `sampling bias` and `selection bias` and is therefore an amendment target under the alias rule the moment nonprobability sampling is written down; `Knowledge/Machine Learning/_Foundations/Training Set.md`, read for one reason only, that it claims the bare alias `training data`, which is this chapter's title; `Knowledge/Machine Learning/Machine Learning.md`, the domain hub, for the Areas paragraphs written as continuous argument rather than as lists, and because it is the largest amendment in the run; `Knowledge/Data Systems/_Foundations/Data Source.md`, the neighbouring-domain note that already carries a DIFFERENT THINGS sentence of exactly the shape several of this run's collisions need, written in this vault for this reason. Read every one of these. Eight of them are files this run will amend, so the rule is sharper here rather than looser: what is copied is the shape, the density and the voice, never the content, and an exemplar that is also a target is read in full before a word of the amendment is written.]
- Exemplar chapter note: [two, and for different reasons. `Sources/Books/DMLS/DMLS Ch03 Data Engineering Fundamentals.md` is the most recent finished chapter note and the previous chapter of this same book, so it is read for shape: the `## Extracted` groups introduced by a theme sentence rather than a bare list, the group for notes amended rather than produced, the group for hubs and indexes revised, and the "Settled here, having been asked earlier" block that step 3e asks for. `Sources/Books/DMLS/DMLS Ch01 Overview of Machine Learning Systems.md` is read for content and is the more important of the two on this run, because it contains the promise this chapter exists to keep: its open questions record that transfer learning "is described in this chapter and never named", that both [[Zero-Shot Learning]] and [[Few-Shot Learning]] settle their relation to it in prose without either claiming the name, that "nothing owns the general case", and that "DMLS chapter 4, 'Training Data', is the trigger". That is a dated promise naming this exact run. Its other open questions matter too, since this chapter is placed to disturb the `Machine Learning/Operations/` question, the [[Model Rot]] alias question and the [[Continual Learning]] placement question.]
- Neighbouring domains: [three, and the traffic is much lighter than in the previous run. `Knowledge/Mathematics/`, which is the interesting one and the exact reverse of the previous run's rider. That run said a packet finding itself adding to `Mathematics/` had drifted. Here two atoms have a real claim on it: reservoir sampling and importance sampling are results about drawing from distributions that stand whether or not a model is ever fitted, which is the shape of a Mathematics note. What argues the other way is that hub's own membership rule, which is narrower than "it is mathematics": a note earns a place there when something under `Knowledge/Machine Learning/` genuinely depends on it, and the domain holds seven notes in three declared families, norms and distances, distribution shape, and vector calculus, with no probability or sampling family at all. So placing these two there means creating a fourth family on two notes of evidence, which is the folder rule's own test applied one level up. Cut it explicitly in the plan with the cost of each side named. `Knowledge/Data Systems/`, which owns [[Data Source]] and its four origins, the note the labeling material meets whenever it asks where a record came from, and which owns [[Batch Processing]] and [[Stream Processing]], the pair the feedback loop material touches whenever it asks how soon a label arrives. Data lineage is the one atom here with a genuine claim on that domain, since provenance of a record is a property of the data system and not of the model, and the cross-domain rule governs whichever way it is cut: link, never duplicate. `Knowledge/Systems Architecture/`, which this run does not touch at all; it is named here only so that a packet drifting toward it notices.]
- Known collisions, to be resolved in the plan under the collision rule before any packet is written, and not an exhaustive list: ["Training data", against [[Training Set]], which claims the bare alias `training data`. This is the sharpest collision in the run and the prompt's own rules create it: step 3e requires the chapter note to carry aliases for the chapter number and the chapter title, and this chapter's title is "Training Data". Obsidian resolves aliases without regard to case, so a chapter note claiming `Training Data` would take a link that today reaches a `_Foundations/` concept note, silently. Decide in the plan which alias the chapter note actually carries and say so; the run may not simply follow the template here. "Sample", against [[Training Instance]], which claims the bare alias `sample` and means by it one row of the design matrix. This chapter uses the word in its statistical sense throughout, a subset drawn from a population, from the first line of the file onward. DIFFERENT THINGS at the level of the word, neither side may claim it beyond what is already claimed, nothing new claims `sample` or `samples`, and whatever note holds the sampling material says in its first paragraph which of the two senses it is using, because the raw file's own prose slides between them. "Random sampling" and "simple random sampling", against [[Random Sampling]], a `method` note that claims `random sampling`, `simple random sampling`, `random split`, `uniform sampling`, `shuffle split`, `train_test_split`, `hash split` and `ID hash split`, whose `up` is [[Testing Set]] and whose entire subject is deciding which side of a train-test split a row lands on. The verdict on the concept is SAME THING and the action is AMEND, since creating a second note would duplicate the most complete sampling note in the vault. What is not settled by that verdict is the harder half: the existing note is scoped to splitting and this chapter's use is not, so an amendment that widens the subject makes the note's `up` false, and [[Random Seed]] hangs off it with `up: "[[Random Sampling]]"`. Settle the `up` chain in the plan, not in a packet. "Stratified sampling", against [[Stratified Sampling]], which claims `stratification`, `strata` and `stratum` among others. SAME THING, AMEND, and the same `up` problem one note over. What the raw file adds beyond what is there is thin and is to be found rather than invented: the framing of strata as groups you care about independent of any split, and the drawback at line 32 that a sample may belong to several groups at once, which the existing note does not state. "Nonprobability sampling", and the four criteria under it, convenience, snowball, judgement and quota, against [[Nonrepresentative Training Data]], which claims the bare aliases `sampling bias` and `selection bias` and defines the pathology those four procedures produce. The raw file says at line 11 that nonprobability samples are riddled with selection biases, which is the two notes meeting. The existing note owns the pathology and its $q \neq p$ statement; the new material owns the family of procedures. Neither claims the other's alias, both carry the link, and the new note does not restate the mismatch. "Weighted sampling" against "sample weights", a collision the raw file raises itself at lines 39 to 42 and half resolves. Nothing in the vault claims either phrase, but the third leg is already on disk in a note about something else: [[Class Imbalance]] describes `class_weight="balanced"` reweighting each instance by $m / (K m_k)$, which is sample weighting under another name, in its `## Where it is used`. So the distinction has to be stated once, in one place, and linked from the other two rather than told three times. "Class imbalance", against [[Class Imbalance]], sourced only to [[HOML Ch03 Classification]] and covering the priors, the imbalance ratio, the majority-class accuracy result, the MNIST worked case, the metric argument and `class_weight`. SAME THING and the action is AMEND, and this is the largest amendment in the run after the hub. Everything from line 140 to line 172 of the raw file is remedies that note does not have: resampling in five varieties, two-phase learning, dynamic sampling, the algorithm-level family, cost-sensitive learning, class-balanced loss and focal loss. Whether those become notes or stay inside it is the fourth boundary question below, and the answer decides whether that note stays a `concept`. Note also that the raw file at line 143 carries an argument the existing note does not, that a good model arguably should learn the imbalance rather than have it corrected away, and that line 163 carries a rule the existing note does not, that a model is never evaluated on resampled data. Both are additions to sections that exist. "Resampling", "oversampling", "undersampling" and SMOTE, against [[Data Augmentation]], which claims `synthetic training data` among its aliases and whose `## Implementation` already says that "Oversampling a minority class with SMOTE is the nearest ready-made thing" and that it lives in `imbalanced-learn` rather than in scikit-learn. So part of this chapter's resampling material is already on disk, sourced to another book, sitting in a note about something else, which is the shape the previous run met with Avro and Parquet in [[Data Lake]] and settled with the roster rule. SMOTE synthesizes minority instances, so it is oversampling and augmentation at once and the two notes cannot both own it. Decide which does and make the other link. "Data augmentation", against [[Data Augmentation]] itself. SAME THING, AMEND. What the chapter adds is a three-way taxonomy the existing note does not have, label-preserving transformations against perturbation against synthesis, of which that note is entirely about the first. The taxonomy belongs in `## What it does and when`, merged into the argument there, never as a subsection named for where it came from. "Perturbation", against nothing in the vault, and this is where the raw file's own question at line 124 lands: perturbation appears twice in this chapter, once under semi-supervision as the assumption that a small perturbation should not change a label, and once under augmentation as adding noise to a sample, and my own note asks whether the first is just the second. It is the same operation put to two purposes, enlarging a labelled set in one case and enforcing consistency on an unlabelled one in the other, and that sentence is owed to whichever note holds it. `perturbation` alone is too general to be anybody's alias; `adversarial attack` and `adversarial example` are specific and unclaimed. "Semi-supervision", against [[Semi-Supervised Learning]], which exists, is short, is honest, and discharges its `## Formal statement` on the explicit ground that "a later chapter would add the objective that ties the two parts together". SAME THING, AMEND, and the amendment necessarily rewrites that discharge, because this chapter delivers self-training with a confidence threshold and the perturbation-based method, which are exactly the objectives it deferred. The deferral sentence may not stand next to the material it defers. Note that this note's `up` is [[Machine Learning]] and its folder is `Paradigms/Supervision/`, while the chapter files semi-supervision as one of four ways to handle a shortage of labels, which is a different axis; decide whether the chapter's framing goes into that note or into whatever holds the four-way comparison. "Weak supervision" and "programmatic labeling", unclaimed, with the near neighbour being [[Self-Supervised Learning]], which also produces labels with no human labelling anything. DIFFERENT THINGS, and the separating sentence is sharp enough to write now: self-supervision derives the label from the instance itself by withholding part of it, weak supervision derives it from a heuristic a person wrote about the instance. Neither claims a bare `automatic labeling` or `label generation`. "Transfer learning", claimed by nothing and owed by name. [[DMLS Ch01 Overview of Machine Learning Systems]] recorded that nothing owns the general case and named this chapter as the trigger, so this is a promise being kept rather than a collision being discovered, and failing to produce it is the one outcome this run cannot report its way out of. The difficulty is that two notes already describe the general case carefully without naming it: [[Zero-Shot Learning]] says every zero-shot method is a transfer method and the converse fails, and [[Few-Shot Learning]] says fine-tuning on a small labelled set is squarely transfer and becomes few-shot only when the count per class binds. The new note has to be consistent with both, both gain a link to it, and neither's existing paragraph is deleted or duplicated. Decide whether it is a `concept` or a `method` and where it sits, given that the chapter files it under handling a lack of labels while the vault's two existing relatives sit in `Paradigms/Supervision/`. "Active learning" and "query learning", claimed by nothing, with no title collision anywhere. The word to watch is `query`, which is Data Systems vocabulary ([[Point Query]], and `structured query language` on [[Relational Model]]) and is nobody's alias as a bare word. "Natural labels", "behavioral labels", "explicit label" and "implicit label", none claimed. This is where the run's most consequential correction lands, because lines 81 and 82 attach opposite names to the same fact. Settle which is which from the book and from the recommender literature before a word is written, and report it in the corrections section rather than the slips line. "Feedback loop length", claimed by nothing, and its nearest neighbours are in a different folder: [[Continual Learning]] and [[Model Rot]] both turn on how fast a system can learn that it is wrong, and both sit in `Machine Learning/Operations/`. Whether this note goes to `Data/` with the labels or to `Operations/` with the monitoring is a placement decision with a consequence, since [[DMLS Ch01 Overview of Machine Learning Systems]] set two triggers for reopening that folder's structure and one of them is a note count. [[DMLS]] chapter 8 is the forward collision. "Hand labels", "annotation" and "label", where the trap is one letter away: [[Ordinal Encoding]] claims the bare alias `label encoding` and [[Target Scaling]] claims `label scaling`, both scikit-learn preprocessing operations with nothing to do with somebody labelling data. `label`, `labels` and `labeling` stay nobody's aliases, and anything new here claims only qualified forms. [[Supervised Learning]] additionally claims `labeled learning`. "Label multiplicity" and "data lineage", neither claimed. Lineage is the atom with a genuine claim on `Knowledge/Data Systems/`, since provenance of a record is a property of the data system rather than of the model, and [[Data Source]] is where the four origins already live. Cut it under the cross-domain rule and do not write it twice. "Few-shot", where line 138 says an imbalanced class becomes a few-shot learning problem, against [[Few-Shot Learning]], whose `## VS` already argues that its axis is coverage of the target classes rather than supervision, which is precisely what imbalance produces. This is a link that strengthens both notes and not a note of its own, and it is the cheapest win in the run. "Loss", against [[Cost Function]], which claims the bare alias `loss function`, and against [[Log Loss]], which claims several qualified ones. Cost-sensitive learning, class-balanced loss and focal loss all modify the quantity that note owns. The bare words stay where they are, the three new things claim only their qualified names, and the modification is described as a modification of [[Cost Function]] rather than as a new kind of thing. "Noise", against [[Poor-Quality Data]], which claims `noisy data` and `dirty data`. Weak supervision produces noisy labels and perturbation adds noise deliberately, and those are three different senses of one word: data that arrived wrong, labels that were generated approximately, and a signal added on purpose. None of the three new uses claims the word. "Precision", "recall", "F1" and "ROC", against [[Precision]], [[Recall]], [[F1 Score]] and [[ROC Curve]], all four of which exist and all four of which the raw file explicitly defers to at lines 146 to 149. SAME THING four times over, the action is a link and at most a backlink from each, and no packet writes a metric definition. Check whether each of those four already links to [[Class Imbalance]], since the chapter's claim is about why they are the right measures under imbalance and that claim belongs on the imbalance side. Forward collisions to record and decline, each with the chapter that triggers it: feature engineering and the static against dynamic feature split in full, to [[DMLS]] chapter 5, a deferral [[Feature]] already carries in writing; data distribution shift, monitoring, and the half of the feedback loop story that is about detecting decay, to [[DMLS]] chapter 8, which also owes [[Model Rot]] its `concept drift` and `data drift` aliases; A/B testing and test in production, to [[DMLS]] chapter 9, a deferral [[DMLS Ch02 Introduction to Machine Learning Systems Design]] already carries; the labeling and annotation tool market, to [[DMLS]] chapter 10; the human side of annotation, who does the labelling and under what conditions, together with fairness, to [[DMLS]] chapter 11; hierarchy-aware evaluation, which [[DMLS Ch02 Introduction to Machine Learning Systems Design]] left open and which this chapter's metric section does not reach, to [[DMLS]] chapter 6; convolution and invariance built into an architecture rather than taught by example, to [[HOML]] chapter 14, a deferral [[Data Augmentation]] already carries; and adversarial examples at depth, which no chapter in either book is named for, so record it as owned by nobody rather than assigning it.]
- Domain boundary questions this run must answer: [Five, and the first two govern the shape of the run. First, where the general sampling material lives. `Knowledge/Machine Learning/Data/Sampling/` exists and looks like the obvious destination, but every note in it is about carving a test set: [[Random Sampling]] and [[Stratified Sampling]] both have `up: "[[Testing Set]]"`, and [[Random Seed]] hangs off the first of them. This chapter's opening lines say plainly that splitting is only the most often referenced use, and name three others, sampling events for monitoring, sampling because processing everything is infeasible, and sampling for experimentation, and only one of its six named strategies has anything to do with a split at all. So either the folder's meaning widens and the two `up` values change, which touches a third note transitively and makes two `method` notes answer a broader question than they were written for, or the new material goes somewhere else and the vault carries two senses of "sampling" in one domain. Argue it in the plan with the cost of each side named, because every sampling packet depends on the answer. Second, whether `Data/` gains a `Labeling/` folder. The labelling material is the largest coherent block in the chapter, running from line 62 to line 134, and it is not obviously any of the four existing subfolders: it is not a `Challenge`, not `Exploration`, not `Preprocessing` and not `Sampling`. The folder rule applies unchanged, that a folder is earned by holding notes this run actually produces rather than by naming a topic a later chapter will reach, so count the notes the plan actually commits to before naming it. The additional test is the one the previous run recorded and did not itself pass: a folder name has to still be the right name on the day the chapters that own the subject arrive, and here those are [[DMLS]] chapters 8 and 11. `Requirements/` is the cautionary case and the plan should say in one line why this name is safer than that one was, or admit that it is not. Third, whether reservoir sampling and importance sampling belong in `Knowledge/Mathematics/`. This is the previous run's rider reversed and it is a real question rather than a formality, since both are results about distributions that need no fitted model in the sentence. What holds them where they are is that hub's narrower membership rule and the fact that placing them there creates a fourth family on two notes of evidence. Decide, and if they stay in Machine Learning, say in the note itself that the result is general rather than pretending it is a machine learning technique. Fourth, whether the class imbalance remedies become notes, and what that does to [[Class Imbalance]]. The chapter offers seven or more, across three levels of intervention, data, algorithm and loss. Putting all of them inside one `concept` note's `## Where it is used` produces a note that is a list of methods, which is what `index` is for and what a `concept` is not; giving each one a note produces several one-paragraph notes, which the folder rule and the quality bar both discourage. The likeliest honest answer is a cut somewhere in the middle, and the plan states where and why, per remedy, rather than once. Note that the decision interacts with the first question, since resampling is a sampling method and would land wherever that question sends the rest of them. Fifth, and reported rather than reopened: `Machine Learning/Operations/` holds four notes and the standing position from [[DMLS Ch01 Overview of Machine Learning Systems]] is that nothing moves, with two triggers, the folder passing 25 notes or [[DMLS]] chapter 10 arriving. Neither will fire here, but unlike the previous two chapters this one may actually add to that folder, since feedback loop length and possibly the natural labels material have a claim on it. So report the count after this run rather than reporting that nothing changed, and if the folder grows, say by how much.]
- Working tree at the start of the run: [clean, apart from one untracked directory. `git status --short` reports exactly one line before this run writes anything, `?? "_Slip Box (Zettelkasten)/"`, which is the slip box itself and is expected. The previous run's work is committed in full at e870f14. Anything else appearing in that output at Phase 5 is this run's own doing, and it is either a file the plan named or a mistake to reverse by hand and never with a git command.]

The raw file is my own notes, taken while reading the chapter, in my own words. It is the source of truth for what I understood. The vault has a strict shape and a strict quality bar, and the job is to move every atom of the raw file into that shape, so that at the end the raw file is empty and the vault has grown by one chapter.

Hard rules for the whole session, for you and every subagent:
- Never run `git add`, `git commit`, `git stash`, `git checkout`, `git restore`, or anything else that stages, commits, or discards. `git status`, `git diff`, `git log` and `git show` are allowed, read only.
- Never delete the raw file. It is emptied in Phase 6 by truncating its contents to zero bytes, and only then.
- Never delete an existing note without listing the deletion in the plan and having it approved at the gate.
- Every file is owned by exactly one subagent per phase. Two subagents never write the same file.
- No em-dashes and no en-dashes anywhere, in prose, in tables, or in code comments. Use commas, colons, parentheses, or a new sentence. Normalize any the raw file contains.
- Never write "e.g." Write "ex" or restructure the sentence.
- Every note body is a restatement in the vault owner's voice. The raw file's wording is preferred wherever it already says the thing. Book phrasing is never copied. Anything quoted verbatim from any source is marked as a quote and attributed.
- The preference for the raw file's wording holds only where the raw file is actually in my voice. A raw bullet written in a register the rest of the file does not use, typically a long fluent sentence with marketing adjectives where the surrounding bullets are terse fragments, is very likely pasted from a glossary or a vendor page and must be restated from scratch rather than preserved. Flag every such bullet in the Phase 4 report as a suspected paste, with the raw wording, so I can see which of my own notes were not mine.
- A heading that names a source is a defect, without exception: no `### From Chapter 1`, no `### What the book says`, no `### Kleppmann's version`. New material is merged into the section it belongs in by topic, and any subsection is named for what it contains. "The chapter" and "the book" are never bare subjects in the prose of a note under `Knowledge/`; name the source with a link or state the fact without attribution. Attribution lives in `sources:` and in links.
- A note belongs to exactly one domain. Where this chapter's material overlaps a neighbouring domain, the rule is link, never duplicate: if a note already exists there, amend it and link to it, and do not restate its content in this domain. Where the overlap is real but the existing note says something different rather than something less, that is a research item and a correction, not a second note.
- A word that already exists in the vault is a collision until proved otherwise. Before creating any note whose title, or any alias you would put on it, matches an existing note's title or alias, read that existing note in full and return one of exactly three verdicts, recorded in the plan with the reason:
  - SAME THING. Two names for one concept. One note survives, in the domain that owns the concept, the other name becomes an alias on it, and the second domain links to it and restates nothing.
  - DIFFERENT THINGS. Two notes. Each one's `## Definition` names the other and says in one sentence what separates them, with a link, so a reader who arrived at the wrong note leaves immediately rather than reading the wrong thing carefully. Neither note may claim the bare ambiguous word as an alias.
  - SPECIAL CASE. One is the other applied to a narrower setting. The general note lives in the domain that owns the general case; the specific note points `up` at it, or links to it with a phrase naming exactly what it specializes, and does not repeat the general note's definition.
  The failure this prevents is not a merge conflict, it is silence: two notes describing different things under the same word, neither aware of the other, both looking correct in isolation.
- Aliases are globally unique across the vault. Obsidian resolves a link by name, so two notes claiming one alias makes one of them unreachable under that name with no warning and no error. Before any packet is dispatched, check every seed alias in every packet against every title and alias already in the vault, and against the other packets. A word that is ambiguous on its own is not an alias for anybody: it is qualified in both places or dropped from both alias lists.

Work in six phases. Do not skip a phase and do not start writing files before the gate.

PHASE 1: STRUCTURE (subagents, in parallel, read only).
Launch two read-only exploration subagents at once and wait for both.

Subagent A, the vault contract. Have it read in full and report back, quoting rather than paraphrasing wherever a rule is exact: `__Meta/Quality-Bar.md` (the whole file, it is the spec), `README.md`, `Home.md`, `Notation.md`, `__Meta/Callouts.md` (only the list of callout types), every file in `__Meta/templates/`, and `.obsidian/types.json`. Its report must contain: the frontmatter contract per note_kind, the required section headings per note_kind verbatim, the three rungs of `## Formal statement`, the fixed discharge string for the bottom rung and where in the section that string goes, the hyperparameter test, the disqualification list, the heading rules (no H1 under `Knowledge/` and none on a chapter note, H1 on a container note and on any document read front to back, `##` for required sections, `###` for subsections), the rule against headings that name a source, whether a Bases query block is part of the required shape of an `index`, whether the list heading of an index is fixed or named for what it lists, and whether a forward-looking list of material not yet written is part of an index's shape or a defect in it, the discharge available to `## Implementation` in a domain with no pinned library, and what the file says about adding a new note_kind and when that question reopens.

Subagent B, the vault as it stands. Have it produce: the full tree of `Knowledge/` and `Sources/` with one line per file giving path, note_kind, `up`, and `confidence`; the complete text of every domain hub under `Knowledge/`, meaning the `index` note whose `up` is `[[Home]]`, including each one's scope statement and its Areas list in full; the complete text of the exemplar chapter note named in the parameters; the output of `git log --oneline -n 12`, and then `git show --stat` on the most recent commit that integrated a chapter, as the exemplar of what a chapter integration touches (new notes, the hub, Home, the container note, and backfilled `sources:` and `up:` on existing notes); and, when the domain status is FIRST RUN, additionally `git show --stat 5a1a0c2`, the commit that created the `Mathematics` and `MLOps` domains, as the exemplar of what starting a domain touches. Also have it report every line anywhere in `Knowledge/`, `Sources/`, `Home.md` and `README.md` that mentions the chapter being integrated, the book being integrated, or the subject domain by name, because each one is a promise this run must keep or correct; every `[[wikilink]]` target in the raw file that does not resolve to an existing note; and every existing note whose title or `aliases` collide with a term in the raw file, which is the input to the collision rule above. Finally, have it report which existing notes end with a Bases query block and which do not, so new notes can follow the pattern of their neighbours rather than invent one.

Do not proceed until both reports are in. Keep both reports in your context for the rest of the session.

PHASE 2: READ THE RAW NOTES (you, personally, in full).
Read the raw file from the first line to the last with no offset and no limit. If the file is long, read it in consecutive chunks until you have seen the final line, and say the final line back to yourself so you know you reached it. Do not delegate this. Do not summarize it yet.

Then read, yourself, every exemplar note named in the parameters, and the exemplar chapter note, so you know what a finished note looks like in each shape you will be producing.

PHASE 3: INVENTORY AND PLAN (you, personally).
Build the plan in this order and write it to a scratch file outside the vault, not into the vault.

Step 3a, the inventory. Split the raw file into atoms. An atom is the smallest piece that lands in one place: usually one heading with its bullets, sometimes a single bullet, a single table, or a single code block when its siblings go elsewhere. Number the atoms in file order. Every line of the raw file belongs to exactly one atom. Tables, figures and code blocks that only reproduce an exhibit from the source are still atoms; they are allowed to be dropped, but only by an explicit decision with a reason.

Three shapes of atom need naming before they are missed. A heading with nothing under it is an atom, and its disposition is a decision: either the chapter covered it and I did not write it down, in which case the plan says so and the material is researched or deferred, or it is a stub for a later reading session, in which case it is dropped with that reason. A term listed with an empty definition is an atom, and the first place to look for its definition is later in the same raw file, where a term glossed in one line at the top is often developed in full under its own heading further down; when nothing in the raw file defines it, the note is still owed a definition, which comes from research and is reported as an addition rather than quietly written as though I had said it. A bullet whose content is a bare cross reference, with no claim of its own, is an atom whose disposition is a link rather than a note.

Step 3b, the destination table. One row per atom: atom number, raw heading, action, target path, note_kind, `up`, what this atom contributes, and why here. Actions are exactly one of: CREATE a new note, AMEND an existing note (say which section), RENAME or MOVE an existing note (old path to new path), DELETE an existing note (with the reason and where its content goes), DROP the atom (with the reason). A raw atom may feed several rows, since one passage often carries both the definition of a thing and a claim about a different thing it is being contrasted with. Every unresolved `[[link]]` from the Phase 1 report must appear as a CREATE row or the plan is incomplete.

Step 3c, the structural decisions. Decide and write down, with the reason, every placement question the chapter raises, checked against the Areas list of the domain hub and against the folder rule (the folder says what kind of thing a note is, `up` says what it is part of). Do not add a new note_kind. Every decision here is a decision about where things go and what earns a note, never a decision about what is true.

Step 0 of this step applies only when the domain status is FIRST RUN, and when it applies nothing else in the plan is settled until it is. Read the domain hub named in the exemplars first, because it is the precedent: it was started from a single chapter of evidence, it opens with a paragraph saying what earns a place in the domain and what does not, and it has an Areas list whose entries are folders with the notes in them named in prose. Read it against what Subagent A reports about the index shape rather than copying it wholesale, since a hub written earlier may carry a section the contract has since dropped. Then decide and write down, with the reason:

- The domain's name and folder, and the hub note's path, which is the folder name repeated as the filename, matching `Machine Learning/Machine Learning.md`.
- The membership rule, one paragraph: what earns a place in this domain, and what looks like it belongs but goes to a neighbouring domain instead. Write this as that hub writes it, as a boundary against the neighbours rather than as a description of the subject. This paragraph is the thing that stops the next five chapters from being filed by accident, so it is worth more effort than anything else in this step.
- The folder taxonomy, with the evidence for each folder drawn from the atoms in the inventory and named atom by atom. One chapter is thin evidence, so the default is few folders. A folder is earned by holding notes this run actually produces, not by naming a topic the book will reach later; a topic with one note and a promise of more is a note at the top level of the domain, and the folder arrives with the second note. State plainly which folders are being created on one note of evidence and why that one note could not sit at the top level instead.
- Which atoms are not domain material at all. A chapter that opens a subject typically carries organizational material, meaning who does which job and what the teams are called, and it carries vendor and product names. Decide whether each of those is a note, a line inside another note, or a drop, and if the organizational material is a note, decide whether this domain or a neighbouring one owns it.
- The hub note itself: `note_kind: index`, `up: "[[Home]]"`, no H1, a scope paragraph, `## Areas` rather than `## Methods`, and a Bases query block, which every index carries. Whether it also carries a list of material not yet written is decided by what Subagent A reports, not by what an older hub happens to contain: an index that states what is on disk is honest about a domain one chapter old by being short, and a list of predicted arrivals is a claim made on the weakest evidence there is.
- The line in `Home.md` under `## Domains` that introduces the domain, in the shape of the lines already there, and whether `README.md`'s layout table needs the domain named too.

The rest of step 3c applies to every run. At minimum decide: which atoms become notes at all, given that a term the chapter merely names in passing is a line in another note rather than a note of its own, and say which terms you are refusing a note to and why; for each note, whether it is a `concept`, a `method` or an `index`, checked against the concept and method test in section 2 of the quality bar (a method is something you carry out as itself and that changes something downstream, or an ordered protocol whose reading depends on the order; everything else is a concept) and against the rule that `index` is earned by having members to list and that a topic with none is a `concept` carrying its own definition; whether any existing note should convert between kinds under that same rule; which notes are paired contrasts that want the optional `## VS` section rather than two notes each describing the other; which atoms touch a neighbouring domain, and for each one whether the existing note there is amended and linked or whether this domain owns a genuinely different thing under a similar name; and whether a dataset note is required, given that the quality bar wants one per dataset a chapter works on. Where a decision contradicts a promise an existing note made, say which promise is kept and which is corrected, and never leave both standing.

Also produce, as its own table, the collision ledger: one row per term in this chapter that matches an existing note's title or alias, whether the parameters named it or Subagent B found it, giving the term, the existing note, the new note or amendment, the verdict of SAME THING, DIFFERENT THINGS or SPECIAL CASE, and the exact sentence each side will carry to tell itself apart from the other. A term with no row is a term nobody checked.

Do not reach for a new note_kind. The quality bar says when that question reopens, and the answer is never "during an integration run". A note that fits no existing kind comfortably is written to the kind it fits least badly and the strain is recorded in the final report and in the chapter note's `## Open questions`, which is the evidence a kind is eventually argued from.

One decision is larger than placement and gets its own line in the plan when it arises: whether this run's material argues for restructuring an existing domain rather than only adding to one. A domain that turns out to be a special case of something this chapter introduces is a real finding and is allowed to move. It is proposed at the gate with its cost named in full: every file that moves, every `up` that changes, and every sentence in `Home.md`, `README.md` and the other hubs that names the old path in words, since wikilinks survive a folder move and prose does not. Never restructure silently and never as a side effect of a packet. Where the cost is small today and grows with every chapter, say so plainly, because that is an argument for doing it now rather than an argument against doing it at all.

Step 3d, the promises. For every line the Phase 1 report found that mentions this chapter, this book, or this subject, write the exact edit that retires the promise. At minimum this covers: the container note, whose chapter line becomes a link, or, where the link is already there, whose link must now resolve to a written note; `Home.md`, whose `## Sources` list records the book and whose `## Areas` list records the domain; `README.md`, wherever it describes what lives under each path or counts what the vault holds; the domain hub's Areas paragraphs, which name the notes in each folder in prose and so go stale the moment a folder gains one; and every note in a neighbouring domain that names this subject or promised this material. Add any others the report found. A promise that this run cannot keep is rewritten to say what is actually true, not left to rot.

Step 3e, the chapter note. Plan the chapter note at the path in the parameters, from `__Meta/templates/Note Kinds/Source (Chapter).md`: frontmatter with `note_kind: source`, `medium: book`, `chapter:` as the number from the parameters, `up:` as a link to the container note, `url:` from the parameters, and aliases for the chapter number and the chapter title. Then `## Thesis`, `## Extracted` grouped by theme with a group for notes amended rather than produced and a group for hubs and indexes revised, and `## Open questions`. It takes no H1. Also plan which open questions in earlier chapter notes, of this book and of any other, this chapter settles or partly settles, and what edit records that.

Step 3f, the research list. For every claim in the raw file that is uncertain, marked with a question mark, internally inconsistent, or likely wrong, write a research item: the claim, what to check, and which primary source settles it, using the ordering in the parameters. Do the same for every named system, product, service, protocol or standard in the raw file: what it is, who makes it, whether it still exists under that name, and whether the property the raw file attributes to it is true of it today rather than at the time some older source described it. A product list in the raw file is a research item in its own right, because product lists go stale faster than anything else in a note, and a list that was right when the book went to press may not be right now. Where the raw file carries formulas, every one gets rederived or checked against a reference; where it carries none, this clause is discharged and an arrow or an inequality used as shorthand in prose is not a formula.

Step 3g, the work packets. Group the destination rows into packets such that no file appears in two packets, and packets are independent of one another. Then fix, before any packet is dispatched, the final title of every note that will be created or renamed, because every packet links to notes owned by other packets and the link text must already be right. Each packet contains: the atom text verbatim, the target file or files and the action on each, the note_kind and its exact required sections from the Phase 1 report, `up`, a seed list of aliases (every name a person would type in a link, including plurals, abbreviations, the expansion of every acronym and the acronym of every expansion, and the name any widely used system gives the thing), the outbound links this note must make and the phrase that explains each relationship, the notes elsewhere that will link back to it, the research items that belong to it, the exemplar note to read first, and the style rules from the hard rules above. Every packet also carries: "Do not touch any file not named in this packet. Do not run git write commands. Report back before finishing."

Write the plan as: the destination table, the structural decisions with reasons, the promise edits, the chapter note outline, the research list, and the packet list. Then present it to me in full.

GATE: Stop here and wait for my explicit approval of the plan. Apply any changes I ask for to the plan before continuing. Do not write any file inside the vault before I say go.

PHASE 4: EXECUTE (subagents, in parallel where packets are independent).
Dispatch every packet to its own writing subagent. Independent packets go out together in one batch. A packet that amends a file another packet creates waits for that packet to finish. When the domain status is FIRST RUN, the packet carrying the domain hub goes last, because it describes what the other packets actually produced. Each writing subagent, in this order:
1. Reads `__Meta/Quality-Bar.md` and the exemplar note named in its packet.
2. Reads every existing note it is told to link to, so the link phrase is true and the `up` chain is consistent.
3. Does the research in its packet against primary sources, and records for each item: the finding, the source, and whether it confirms, corrects, or extends the raw notes. Research is for correctness and completeness. It is not licence to replace the raw file's wording with a source's wording.
4. Writes or amends the note to the contract for its kind. New notes get `confidence: draft`, never anything higher. Amended notes get this chapter appended to `sources:`, and the new material is merged into the section it belongs in by topic, sharpening or correcting what is there rather than being appended as a layer. A subsection is created only when the new material is a distinct sub-topic, and it is named for what it contains and never for where it came from. Where the new material contradicts what the note already says, the contradiction is settled by research and the note is rewritten to say one thing; two sections each asserting a different thing is never an acceptable outcome. A Bases query block is part of the required shape of an `index` and every new index gets one; on any other kind it is appended only where the note's neighbours in the same folder carry one.
5. Self-checks before reporting: every required section present and non-empty, `## Where it is used` has at least two outbound links each carrying its relationship phrase, every quantitative claim has a formula somewhere in the note, the discharge string is exact and last in its section if used, no H1 on a note under `Knowledge/` and none on a chapter note, no heading that names a source, no bare "the chapter" or "the book" as a subject in the prose, no dash characters of either kind, no "e.g.", no placement commentary in the body, frontmatter keys spelled exactly, `up` is a link and is never `[[Home]]` for a leaf.
6. Reports back with: files written or changed, with a one-line summary each; every fact in the raw notes it corrected, with the raw wording, the corrected wording, and the source; every claim it added that the raw notes did not contain, with the source; every raw bullet it suspects was pasted from elsewhere rather than written by me, with the raw wording; every research item it could not settle, stated as uncertain rather than guessed; and any deviation from its packet with the reason. A typing slip is not a correction. A misspelling, a transposed word, or a word the surrounding sentence plainly contradicts is fixed silently and listed in one short line at the end of the report, separately from the corrections, so that the corrections section stays a list of places I understood something wrongly rather than a list of places I typed badly.

Keep every report. You will need them in Phase 6.

PHASE 5: VERIFY (you, personally, with scripts where possible).
Run these checks over the whole vault and fix what fails, either yourself for one-line fixes or by sending the owning subagent back for anything larger:
- Every `[[wikilink]]` in `Knowledge/`, `Sources/`, `Home.md` and `README.md` resolves to an existing file, allowing for `|alias` display text and `#heading` suffixes.
- Every note under `Knowledge/` has `note_kind`, `up`, and (where its kind requires them) `aliases`, `sources`, and `confidence`, with the keys spelled exactly. Every `up` value resolves.
- Every new or amended note carries the required `##` sections for its kind.
- No note under `Knowledge/` carries an H1, and neither does any chapter note. Container notes, dataset notes, `Home.md`, `README.md`, `Notation.md` and the files under `__Meta/` do carry one, and this check must not strip it.
- No note under `Knowledge/` carries a heading that names a source, and none uses "the chapter" or "the book" as a bare subject in its prose.
- No file under `Knowledge/`, `Sources/`, `Home.md` or `README.md` contains an em-dash, an en-dash, or the string "e.g.".
- Every note the chapter produced or amended is listed under `## Extracted` in the chapter note, and every note listed there exists and names the chapter in its `sources:`.
- Every promise line from Phase 3d is gone or rewritten.
- No alias appears on two notes, and no note claims an alias that is another note's title. Collect every `aliases` entry and every filename across the vault and check for duplicates mechanically, since this failure is silent in Obsidian and invisible on inspection.
- Every row of the collision ledger landed: the verdict was applied, and where the verdict was DIFFERENT THINGS both notes actually carry the distinguishing sentence and the link.
- The container note links the chapter, `Home.md` counts it, and `README.md`'s layout table counts it. When the domain status is FIRST RUN, `Home.md` also introduces the domain and the hub's Areas list names every folder the run created.
- Every atom in the inventory is accounted for: walk the destination table row by row and confirm the content actually landed where the row says, in substance, not just by file existence. An atom marked DROP has its reason in the plan.
- `git status --short` shows only the files the plan named, plus the entries the working tree parameter says were already there before the run started. Anything else is a mistake to reverse by hand, never with a git command.

PHASE 6: EMPTY THE RAW FILE AND REPORT.
Only when every check in Phase 5 passes, truncate the raw file to zero bytes. Do not delete it and do not move it. Then give me the final report, which has exactly these parts:
1. A table of every file created, amended, renamed, moved or deleted, one row each, with the reason in a few words.
2. Structural decisions taken, one line each, with the reason. When the domain status was FIRST RUN, the domain's membership rule and the evidence behind every folder created come first, because they are the decisions I will be living with for the rest of the book. Any restructuring of an existing domain is stated here in full, with what moved and what prose was rewritten to match.
3. The collision ledger as it was finally resolved: term, the notes on each side, the verdict, and the sentence each note carries. Include the collisions you found that the parameters did not name, and the forward collisions you declined to act on, with what will trigger them.
4. Corrections: every place the notes now say something different from what my raw file said, with the raw wording, the new wording, and the source. This is the most important section. Writing is how I learn, so I need to know exactly where my understanding was wrong. Typing slips do not belong here; they go in one line at the end of this section, counted rather than enumerated.
5. Additions: every substantive claim in the vault that came from research rather than from my raw file, with the source, so I can read them as a reviewer.
6. Suspected pastes: every raw bullet a subagent judged to be someone else's prose rather than mine, with the raw wording, so I can check my own note taking.
7. Unsettled: every research item no subagent could settle from a primary source, with what to check in the book.
8. The chapter note's open questions, restated, and which earlier open questions this chapter settled.
9. The output of `git status --short`, unstaged, uncommitted.
No praise padding and no summary of what a good job was done. Facts, decisions, corrections, and gaps.
```
