# Slip box integration prompt

Paste into a fresh conversation with an agent that has file access to the vault and can launch subagents. Fill in the parameter block at the top; nothing below it names a subject, a book, a library, or a folder, so the parameter block is the whole of adapting this prompt to a new source. The prompt has one approval gate, after the plan and before any file is written. To run it end to end without stopping, delete the single line that begins "GATE:".

This is a different role from `Chapter-Study-Prompt.md`. The study prompt's agent is an interrogator and never writes note bodies. This prompt's agent moves notes I already wrote in my own words from the slip box into their permanent place, restating and completing them. Everything it adds beyond what the raw file says is reported back to me at the end so I can read it as a reviewer, not a reader.

One prompt serves every subject. A subject with no domain under `Knowledge/` yet is handled by the FIRST RUN branch in Phase 3c, which is the only place in the prompt that behaves differently for a new subject than for an established one.

---

```
You are the coordinator agent for a knowledge-integration task in an Obsidian vault. The vault root is the current working directory. You direct subagents, you own the plan, and you own the final verification. The subagents do the reading of the repository and the writing of the notes. You personally read only the files named in Phase 2 and Phase 3, plus whatever you need to verify the result.

PARAMETERS.

- Raw notes to integrate: [`_Slip Box (Zettelkasten)/RAW DMLS Ch06 Model Development and Offline Evaluation.md`, the only file in the slip box. 169 lines, the last of which ends in a trailing space and no newline, so unlike the previous run's raw file this one does not end in a trailing newline. 25 headings running three levels deep (three `##`, seven `###`, fifteen `####`), no table, no code block and no mermaid block. The file contains no dash characters of either kind and no "e.g.", both checked, so those two normalizations are discharged before the run starts and finding either in the final vault means a subagent introduced it. It also contains no `[[wikilink]]` at all, the second consecutive raw file with none, so Phase 1's unresolved-link report will come back empty and every link in the finished notes is one this run decided on rather than one I asked for. Seven atom shapes are present by inspection and are named here so they are not missed rather than decided here. First and largest, `## 4 Phases of ML Development` at line 133 carries four bullets, lines 134 to 137, which name four phases and say nothing whatever about any of them: this is the empty-heading shape applied to an entire section rather than to one heading, it is the biggest empty atom any run has met, and its disposition is a decision taken in the plan, with the content coming from research or the atom being dropped with its reason. Second, line 143, `- Simple Heuristic: `, is the term-with-an-empty-definition shape, conspicuous because all four of its siblings in the same list carry a gloss; nothing later in the file defines it, so the definition comes from research and is reported as an addition rather than quietly written as though I had said it. Third, line 26 is a verbatim quotation, "All models are wrong, but some are useful" attributed to George Box, which under the hard rules below is marked as a quote and attributed with a date rather than absorbed into the prose, and the wording and the year are themselves a research item, since Box published a weaker form in 1976 and the familiar sentence in 1979. Fourth, one atom is a question addressed to the vault rather than a claim: line 153 objects that an invariance test cannot be right if it works by removing features, since the feature removed might be an important one. It is answered in a note and in the final report, never dropped, and the answer is a real distinction rather than a yes or a no. Fifth, line 71 carries a structural defect rather than a gap, `$\rightarrow` opened and never closed, the only unbalanced math delimiter in the file, so that bullet renders as literal TeX from there to the end of the line. Sixth, line 120 reads `- Tay with Rune`, a garble of Ray with Tune, which is a typing slip sitting on top of a factual claim, so it is repaired and the tool behind it is then checked like every other product in the file. Seventh, the heading at line 114, `#### Soft AutoML: Hyperparameter Tuning `, carries a trailing space, which is a slip and not a gap. The file is also the most typo-dense of the six: `orrigional`, `entier`, `vaalutsion`, `processo flogging`, `ad hock`, `shoudl`, `differet`, `powerfull`, `usefull`, `usefullness`, `cratch`, `representatino`, `hypermarameters`, `acumulate`, `shufflinf`, `abount`, `QUESTOIN`, `trens`, `afect` and a `per later` that means per layer. Every one of those is a typing slip, fixed silently and counted rather than enumerated in the final report, and none of them belongs in the corrections section.]
- Chapter note to produce: [`Sources/Books/DMLS/DMLS Ch06 Model Development and Offline Evaluation.md`. The file does not exist at all, so the action is CREATE outright, as in all five previous runs. There is no spelling to reconcile: the raw file already says `Ch06` zero padded, which matches every chapter note in the vault and matches line 22 of [[DMLS]]'s own `## Chapters` list verbatim. Unlike the previous two runs the title collides with nothing, since no note under `Knowledge/` is called "Model Development and Offline Evaluation" and none claims it as an alias, so the yield rule that [[Quality Bar]] now carries in section 2, written down by the ch05 run after `DMLS Ch04 Training Data` and `DMLS Ch05 Feature Engineering` both had to give their titles up, does not bite here and this note takes both aliases the contract asks for, `DMLS Chapter 6` and the chapter's own title. Check that rather than assuming it, because the failure the rule prevents is silent.]
- Chapter number, for the `chapter:` key: [6]
- Chapter note `url:`: [https://github.com/chiphuyen/dmls-book/blob/main/resources.md#chapter-6-model-development-and-offline-evaluation. This follows the scheme the ch01 run settled and all five later runs kept, pointing at `resources.md` with a per-chapter anchor rather than at `summary.md`. Check that the anchor resolves and that the section behind it is chapter 6's; if the file has no such heading, fall back to `summary.md` and then to the repository root, and say in the final report which one was used and why. The container note keeps the repository root either way. This chapter's entry in that file matters more than the previous five did, because the chapter names more papers and more tools than any DMLS chapter so far and the book's own resource list is the fastest route to which paper it means.]
- Container note: [`Sources/Books/DMLS/DMLS.md`, linked as [[DMLS]]. One edit and one only: line 22, `- DMLS Ch06 Model Development and Offline Evaluation`, is plain text today and becomes a wikilink to the note this run produces. The ch01 run already corrected the numbering and checked all eleven titles against the book's own `ToC.pdf`, so that work is finished and is not reopened; a packet that finds itself editing a second line of that file has drifted and says so instead of editing.]
- Subject domain: [`Knowledge/Machine Learning/`, hub at [[Machine Learning]]. The domain holds 140 notes across ten areas, and this chapter lands almost nowhere near the previous one. `Data/` holds 43 notes, seven at its top level and thirty-six across five subfolders, and receives at most a clause. What receives the chapter is `Evaluation/`, 12 notes at its top level with a two-note `Hyperparameter Search/` under it, `Models/Ensembles/`, which holds exactly one note, `Optimization/` with 11, `Operations/` with 4, and `_Foundations/` with 9. The membership test is again not in doubt anywhere, since every atom needs a fitted model in the sentence before it can be stated at all, but placement is harder than last run rather than easier, for two reasons: the material is spread across five folders instead of concentrated in one, and two of its blocks, experiment tracking and distributed training, land on folder boundaries that are already written down in prose and that exclude them as written. The other inversion from the previous run is the default action. There, AMEND was the default and a packet reaching for CREATE owed a reason. Here the vault already owns baselines, model selection, hyperparameter search, learning curves, the ensemble idea and error analysis, and it owns nothing whatever on calibration, slice-based evaluation, behavioural testing, experiment tracking, distributed training or AutoML, so CREATE is the default on those six and a packet that reaches for AMEND on one of them owes a reason in writing. Neighbouring domains are not excluded this time, which is the parameter below rather than a silent permission.]
- Domain status: [ESTABLISHED and large. Phase 3c step 0 does not apply and is skipped in full: no domain folder is created, no hub is created, and no new line is added to `Home.md` under `## Areas`. Whether `Home.md` owes anything else is a decision this run takes rather than skips, and the case is weaker than last run's: its machine learning gloss reads "how a model is specified, fitted and evaluated, and how the data it is fitted on is obtained, labelled, repaired and built into the columns the model actually reads", and this chapter is squarely inside "specified, fitted and evaluated", so the likely answer is that it needs nothing. Check it rather than assuming, and check `README.md` as well, whose layout table describes paths rather than counting notes and is expected to need nothing. The hub is again the single largest amendment in the run and is a packet of its own that goes last, after the packets whose output it has to describe, and it carries four pressure points rather than two. Its `Models/` bullet ends on the sentence "`Ensembles/` holds [[Ensemble Learning]], the idea rather than any method built on it", which is falsified the moment this run files a method in that folder. Its `Evaluation/` bullet is one continuous sentence run that names every note in the folder one by one and ends on `Hyperparameter Search/`, so anything new there is a rewrite of that run and not an append. Its `Optimization/` bullet describes the folder as the mechanics of fitting, meaning the search through parameter space, which is a description a training run spread over many machines either fits or breaks. And its `Operations/` bullet restates [[MLOps]]'s boundary, so if the experiment-tracking decision moves that boundary, two files say the old thing and both are edited in the same sitting.]
- Library pin: [scikit-learn 1.6, with NumPy 2.x and pandas 2.x alongside it, which is the machine learning domain's established pin and is evidenced on disk: [[Missing Value Imputation]], [[Feature Distribution Transformation]] and [[Data Augmentation]] all open their `## Implementation` with "scikit-learn 1.6". Every `method` note this run produces carries a version line on the same footing. This is the chapter where the standard stack covers the least ground of any so far, so naming the right honest negative is most of the work, and [[Data Augmentation]] already models how to write one, opening with "**scikit-learn has no data augmentation API**" and then saying where the nearest thing actually lives. Where the stack does cover the material, name the API exactly: `sklearn.ensemble.BaggingClassifier` and `BaggingRegressor` with `oob_score`, `RandomForestClassifier`, `AdaBoostClassifier`, `GradientBoostingClassifier` and `HistGradientBoostingClassifier`, `StackingClassifier` and `StackingRegressor` with their `cv` argument, `VotingClassifier`, `sklearn.calibration.calibration_curve`, `CalibratedClassifierCV` with `method="sigmoid"` for Platt scaling and `method="isotonic"`, `sklearn.metrics.brier_score_loss`, `sklearn.dummy.DummyClassifier`, and `sklearn.model_selection.HalvingGridSearchCV` and `HalvingRandomSearchCV`. Say which version a claim is true of whenever the claim is about an implementation rather than about a method, which is the rule earlier runs reached for pandas copy-on-write and for `class_weight="balanced"`, and which bites hard in exactly one place here: calibrating an already-fitted estimator changed shape in 1.6, where `cv="prefit"` is deprecated in favour of wrapping the estimator in `sklearn.frozen.FrozenEstimator`, so an implementation written from an older recipe is wrong on the pinned version. Check that against the 1.6 release notes rather than trusting this sentence. The honest negatives are four and they carry most of this chapter. First, scikit-learn has no experiment tracking and no versioning of any kind: MLflow, Weights and Biases and DVC are third-party, and the nearest thing in the stack is `joblib` persistence beside a `cv_results_` table held in memory, which is not a tracked experiment and must not be presented as one. Second, scikit-learn has no distributed training. `n_jobs` is process-level parallelism on one machine through joblib, and an implementation section that presents `n_jobs=-1` as data parallelism is not merely thin, it is wrong: the gradient-accumulating data parallelism this chapter describes lives in deep learning frameworks, in `torch.distributed` and `DistributedDataParallel`, in DeepSpeed and in Megatron-LM, and multi-machine scikit-learn work goes through a joblib backend provided by Dask or Ray and parallelizes candidates rather than gradients. Third, scikit-learn has no AutoML: auto-sklearn and TPOT are third-party, and the successive halving in `HalvingGridSearchCV` is a search budget policy rather than automated model design. Fourth, there is no slice-finding API; the honest statement is a `groupby` over held-out predictions with `sklearn.metrics` computed per group, plus the named research tools. Two of the chapter's named tools are misattributed in the raw file and both are corrections rather than slips: line 118 files auto-sklearn under scikit-learn, and it is an independent project from Hutter's group built on top of scikit-learn rather than a scikit-learn utility, and line 119 files Keras Tuner under TensorFlow, which is a claim to check against what the Keras project says today rather than against what was true when the book went to press.]
- Quantitative expectation: [High, and shaped differently from the previous run. There the risk was an existing discharge left standing under new content; here the risk is the opposite and more ordinary, a survey chapter producing notes that name a method without stating it, because the raw file names fifteen techniques and writes down the mathematics of none of them. Nine atoms carry checkable content and almost all of it is unowned. Bagging: the bootstrap is sampling with replacement, so the expected fraction of distinct rows in one resample is $1 - (1 - 1/n)^n \to 1 - e^{-1} \approx 0.632$, which is both why bagging works and where the out-of-bag estimate comes from, and neither appears in the vault. Boosting: AdaBoost has an exact weight update, $\alpha_t = \tfrac{1}{2}\ln\frac{1 - \epsilon_t}{\epsilon_t}$ with the reweighting that follows from it, and a training-error bound of $\prod_t 2\sqrt{\epsilon_t(1 - \epsilon_t)}$, which is what makes "convert weak learners to strong ones" at line 49 a theorem rather than a slogan. Stacking: the content is a condition rather than a formula, that the metalearner is fitted on out-of-fold predictions, since fitting it on in-fold predictions hands it a column that already knows the answer, which is [[Data Leakage]] arriving inside an ensemble. Calibration is where the highest rung is available and where it is most likely to be skipped, because the raw file describes the counting procedure at line 157 without ever writing the thing being counted: perfect calibration is $P(Y = 1 \mid \hat{p} = p) = p$ for all $p$, the reliability diagram is that conditional estimated by binning, the expected calibration error is $\sum_b \frac{n_b}{n}\lvert \text{acc}_b - \text{conf}_b \rvert$, the Brier score is a mean squared error on probabilities with a decomposition into reliability, resolution and uncertainty, and Platt scaling is a one-dimensional logistic fit $\sigma(as + b)$ estimated on held-out scores. Simpson's paradox at line 165 is an arithmetic fact about weighted averages and is written as the inequality it is, two component ratios each favouring one side while the pooled ratio favours the other, or it is not written at all; naming the paradox without the inequality discharges a rung that is sitting there. Data parallelism's straggler claim at line 105 is a maximum over workers, so the expected iteration time is $\mathbb{E}[\max_i T_i]$, which grows in the worker count even when the $T_i$ are identically distributed, and that is why the claim is derivable rather than merely asserted. Pipeline parallelism at line 111 has the quantity that makes line 110's warning checkable, GPipe's bubble fraction $(K-1)/(M + K - 1)$ for $K$ stages and $M$ microbatches, which goes to zero only as the microbatch count grows. Confidence measurement at lines 160 to 162 is a threshold on a score and the quantity it trades is coverage against selective risk, which is the accuracy-coverage curve. And the search strategy bullet at line 127 meets an argument already derived in the vault. Three atoms are the opposite failure and are equally real in a run this heavy on new notes: learning curves at line 21 are already derived in [[Learning Curve]], which states in so many words that the picture answers "would more data help"; the random-against-grid argument behind line 122 is already derived in [[Randomized Search]], with Bergstra and Bengio cited and the $(1-p)^n$ miss probability worked through, and successive halving is already in [[Grid Search]]; and the zero rule baseline at line 144 is already $\max_k m_k/m$ in [[Baseline Model]]. Those are linked, never rederived. Two failures carry over from earlier runs: a discharge written where rung two was available, and padding written above a discharge where a genuinely discharged note should be short.]
- Primary sources, in order of trust: [the original papers behind a claim, reached through the book's own `resources.md` at https://github.com/chiphuyen/dmls-book/blob/main/resources.md and through the references in the chapter itself; then the current official documentation of any named library or tool, read for what it does today; then the book's own text if the agent has it. Nearly every technique in this chapter has one paper that introduced it, so a research item settled from a blog post has no excuse, and this chapter names more tools than any DMLS chapter so far, which makes the documentation tier busier than usual rather than optional. Ensembles: bagging is Breiman, "Bagging Predictors" (*Machine Learning* 24, 1996), which is also where the list of unstable methods at line 45 comes from and is therefore the source to check that line against; out-of-bag estimation is Breiman's companion technical report of the same period; random forests are Breiman (*Machine Learning* 45, 2001); boosting is Schapire, "The Strength of Weak Learnability" (1990) for the result that a weak learner implies a strong one, and Freund and Schapire (*JCSS* 1997) for AdaBoost itself, with Friedman (*Annals of Statistics* 2001) for gradient boosting and Chen and Guestrin (KDD 2016) for XGBoost; stacking is Wolpert, "Stacked Generalization" (*Neural Networks* 1992), with Breiman's "Stacked Regressions" (1996) as the source of the out-of-fold requirement. Experiment tracking and versioning have no single canonical paper, which is itself worth recording: the primary sources are the tools' own documentation read for what they do today, plus Sculley and colleagues, "Hidden Technical Debt in Machine Learning Systems" (NeurIPS 2015) for why the surround costs more than the model, and, for the vocabulary of reproducibility, Gundersen and Kjensmo (AAAI 2018) and Pineau and colleagues' account of the NeurIPS reproducibility program (*JMLR* 2021). Debugging is the weakest-sourced section in the chapter and the parameter says so in advance: "overfit a single batch" is a practitioner recipe whose best citable statement is the debugging-strategies chapter of Goodfellow, Bengio and Courville's *Deep Learning* (2016), and attributing it to a blog post is not acceptable; Breck and colleagues, "The ML Test Score" (IEEE Big Data 2017) is the citable source for testable practice. Distributed training: Dean and colleagues, "Large Scale Distributed Deep Networks" (NeurIPS 2012) for DistBelief and for both parallelisms in one paper; Li and colleagues, "Scaling Distributed Machine Learning with the Parameter Server" (OSDI 2014); Goyal and colleagues (2017) for the linear scaling rule and warmup; McCandlish, Kaplan and Amodei (2018) for the gradient noise scale and the critical batch size, and Shallue and colleagues (*JMLR* 2019) for the measured diminishing returns, which is the paper that actually settles line 106 rather than restating it; Huang and colleagues, "GPipe" (NeurIPS 2019) and Narayanan and colleagues, "PipeDream" (SOSP 2019) for pipeline parallelism; Chen and colleagues, "Training Deep Nets with Sublinear Memory Cost" (2016) for gradient checkpointing, which line 100 names and never explains; Rajbhandari and colleagues, "ZeRO" (SC 2020) for the memory arithmetic; and Dean and Barroso, "The Tail at Scale" (*CACM* 2013) for the straggler claim, with Chen and colleagues, "Revisiting Distributed Synchronous SGD" (2016) for backup workers as the standard answer to it. AutoML: Hutter, Kotthoff and Vanschoren's *Automated Machine Learning* (Springer 2019, open access) is the standing reference; Bergstra and Bengio (*JMLR* 2012) for random against grid, which the vault already carries; Snoek, Larochelle and Adams (NeurIPS 2012) for Bayesian optimization; Jamieson and Talwalkar (AISTATS 2016) and Li and colleagues (*JMLR* 2018) for successive halving and Hyperband; Feurer and colleagues (NeurIPS 2015) for auto-sklearn, which settles the line 118 attribution directly; Zoph and Le (ICLR 2017) for neural architecture search by reinforcement learning and Real and colleagues (AAAI 2019) for the evolutionary route, which are the two strategies line 129 names; Elsken, Metzen and Hutter (*JMLR* 2019) for the survey whose three components, search space, performance estimation strategy and search strategy, are exactly the decomposition at lines 125 to 127 and are therefore the likeliest source of that wording; Andrychowicz and colleagues (NeurIPS 2016) for learned optimizers; and Kingma and Ba (ICLR 2015) for Adam. Offline evaluation: Ribeiro, Wu, Guestrin and Singh, "Beyond Accuracy: Behavioral Testing of NLP Models with CheckList" (ACL 2020) is the primary source for three of this chapter's four test types and is the single most load-bearing citation in the second half; calibration is Platt (1999) for the sigmoid fit, Zadrozny and Elkan (ICML 2001, KDD 2002) for isotonic regression, Niculescu-Mizil and Caruana (ICML 2005) for the comparison scikit-learn's own user guide is built on, Guo, Pleiss, Sun and Weinberger (ICML 2017) for modern networks being miscalibrated and for temperature scaling, Brier (1950) for the score and Murphy (1973) for its decomposition; confidence and thresholds are Chow (1970) for the reject option, Geifman and El-Yaniv (NeurIPS 2017) for selective prediction, and Hendrycks and Gimpel (ICLR 2017) for the maximum-softmax-probability baseline; slice-based evaluation is Chung, Kraska, Polyzotis, Tae and Whang, "Slice Finder: Automated Data Slicing for Model Validation" (ICDE 2019), which is the tool line 169 names, with Sagawa and colleagues (ICLR 2020) on worst-group optimization and Goel and colleagues, "Robustness Gym" (NAACL 2021) alongside it; and Simpson's paradox is Simpson (*JRSS B* 1951), Blyth (*JASA* 1972) who named it, and Bickel, Hammel and O'Connell (*Science* 1975) for the Berkeley admissions case that is the standing worked instance. Model assumptions: Mitchell, "The Need for Biases in Learning Generalizations" (1980) for inductive bias, which is the note [[DMLS Ch02 Introduction to Machine Learning Systems Design]] predicted this chapter would trigger, and Wolpert (1996) with Wolpert and Macready (1997) for the no-free-lunch results the whole section is downstream of. The state-of-the-art trap at line 8 deserves a measured result rather than four opinions: Musgrave, Belongie and Lim, "A Metric Learning Reality Check" (ECCV 2020) and Dacrema, Cremonesi and Jannach (RecSys 2019) both measured the thing the line asserts, and Sculley, Snoek, Wiltschko and Rahimi, "Winner's Curse? On Pace, Progress, and Empirical Rigor" (ICLR 2018 workshop) with Lipton and Steinhardt (2018) are the arguments behind it. BERT at line 15 is Devlin, Chang, Lee and Toutanova (NAACL 2019). The 2022 staleness rider from all five previous runs stands and bites in three specific places rather than generally: line 15 offers pretrained BERT as the example of a model with a rich ecosystem that is simple to start with, which is a claim about practice made before the current generation of models and is dated or restated as what is true now; line 132's optimizer list and line 122's "popular methods" are both claims about what practitioners reach for in 2022 and are attributed and dated; and line 3's claim that non-neural algorithms are generally more explainable than neural networks is a claim about a whole field, arguable rather than false, so it is stated with its reason or attributed rather than asserted flat. A vendor blog post is not a primary source for a claim about a competitor, a library's own documentation is a primary source for its API and not for whether its method works, and Wikipedia is not a primary source for anything and may be used only to find one, which matters here because line 165's gloss of Simpson's paradox and line 45's list of unstable methods are the two likeliest paste sites in the file and each is checked against the source it was probably taken from.]
- Exemplar notes, read in Phase 2, one per shape this run will produce: [`Knowledge/Machine Learning/Models/Ensembles/Ensemble Learning.md`, the single most important file to read before anything is planned. It is the note this chapter's ensembles section collides with head on, it is the only file in its folder, it already carries the variance decomposition with the correlation floor, the four families in one line each and the random forest evidence on California housing, and it carries the promise this chapter either keeps or reassigns: "The mechanisms, and the reasons to prefer one over another, are deliberately absent from this note and arrive with a later source." It also claims `base learner` and `weak learner`, which are terms the raw file uses in two different sections. `Knowledge/Machine Learning/Evaluation/Baseline Model.md`, the `method` exemplar with a full hyperparameter table and a five-row strategy table, and the note this chapter's entire Baselines section is addressed to: it already derives two of the five baselines the chapter lists. `Knowledge/Machine Learning/Evaluation/Error Analysis.md`, the second `method` exemplar and the more important one for shape, because it is a protocol rather than an estimator, its `## Hyperparameters` is discharged with "None." rather than a table, and that is the shape most of this chapter's new methods will need; it is also the note that already does by hand what line 168 calls the error-analysis route to finding slices. `Knowledge/Machine Learning/Evaluation/Model Selection.md`, read in full and read early, because it claims the bare aliases `hyperparameter tuning` and `hyperparameter search`, which is exactly what line 114 calls soft AutoML, and because the six steps at lines 7 to 34 are six judgements addressed to that one note. `Knowledge/Machine Learning/Data/Perturbation.md`, read in full as the note the perturbation-test atom has to separate itself from: there a perturbation is manufactured to be trained on with a budget chosen so the label survives, here a perturbed test split is scored so the fall in the score is the reading, and the operation is identical while the purpose is opposite. `Knowledge/Machine Learning/Operations/MLOps.md`, read for its scope paragraph alone, which is the obstacle the experiment-tracking decision runs into: it says in writing that a note earns a place there when it is about a model already in production, and that the pre-deployment half already has owners. `Knowledge/Machine Learning/_Foundations/Machine Learning Project Lifecycle.md`, read for its six phases and its back edges, because `## 4 Phases of ML Development` is a subdivision of that note's third phase and must not become a second lifecycle. `Knowledge/Machine Learning/Data/Labeling/Active Learning.md`, read as the exemplar of an `## Implementation` that leans on `CalibratedClassifierCV` and as the note whose `## Failure modes` already states that an uncalibrated model makes every uncertainty measure meaningless, so whatever note claims calibration has to be consistent with it and linked from it. `Knowledge/Machine Learning/Evaluation/Hyperparameter Search/Randomized Search.md`, read for one reason, that Bergstra and Bengio's argument and the $(1-p)^n$ miss probability are already derived there, so the AutoML packet knows what it must not rewrite. `Knowledge/Machine Learning/Machine Learning.md`, the domain hub, for the Areas paragraphs written as continuous argument rather than as lists, and because it is the largest amendment in the run. Read every one of these. Several are files this run will amend, so the rule is sharper here rather than looser: what is copied is the shape, the density and the voice, never the content, and an exemplar that is also a target is read in full before a word of the amendment is written.]
- Exemplar chapter note: [two, and for different reasons. `Sources/Books/DMLS/DMLS Ch05 Feature Engineering.md` is the previous chapter of this same book and the most recent finished chapter note, so it is read for shape: the `## Extracted` groups introduced by a theme sentence rather than a bare list, the group for notes amended rather than produced, the group for notes re-pointed by a verdict rather than deepened, the paragraph for notes checked and deliberately left unchanged, the group for hubs and indexes revised, and the "Settled here, having been asked earlier" block that step 3e asks for. It is read for content as well, because its `## Open questions` name this run twice by number, owing this chapter the time-based split terms and, for the third time in three chapters, hierarchy-aware evaluation, and because it reports latency and throughput unsettled for the fifth consecutive chapter with an exact record this run has to continue rather than restate. `Sources/Books/DMLS/DMLS Ch02 Introduction to Machine Learning Systems Design.md` is read for three lines rather than for shape, and it is the more important of the two for planning: it names this chapter by its title as the likeliest trigger for inductive bias, it records the hierarchy-aware evaluation debt in full with the reason no metric note can express it, and it says in writing that "DMLS chapter 6 owes the experiment half and chapter 10 owes the registry half" of versioning and reproducibility. That is the most specific promise any earlier chapter has made about this one.]
- Neighbouring domains: [three, and unlike the previous run the expected traffic is not nil. `Knowledge/Systems Architecture/` has a real claim this time, through the distributed training section, and the cross-domain rule governs it: link, never duplicate. [[Distributed System]] already carries the system model every protocol is proved against, [[Node]] the unit such a system is counted in, [[High-Performance Computing]] the tradition of distributing one enormous calculation rather than one service, which is what a training run is and which that note already names in its own definition, and [[Scalability]] the memory arithmetic $P \cdot b \cdot k$ for one copy of a model, which is exactly the quantity data parallelism replicates once per worker. So the boundary is drawable and the plan draws it: that domain owns the machines, the interconnect, the failure model and what is required of any system so arranged, and this domain owns what takes a fitted model to state, the gradient, the parameter copy, the batch and the update. A note that restates the system model has misread the rule, and a note that describes a worker without mentioning a gradient is in the wrong domain. `Knowledge/Data Systems/` owns [[Batch Processing]] and [[Stream Processing]] and is very likely to receive nothing; it is named so that a packet drifting toward it notices. `Knowledge/Mathematics/` has a colourable claim through Simpson's paradox, which is a fact about weighted averages and not about models, and the presumption is against it exactly as it was in the previous two runs: that hub admits a note when something under `Knowledge/Machine Learning/` genuinely depends on it, it holds seven notes in three declared families, and two consecutive runs have declined to open a fourth family on thin evidence. If the decision goes the other way here it is argued in the plan with the cost named, not taken inside a packet.]
- Known collisions, to be resolved in the plan under the collision rule before any packet is written, and not an exhaustive list: ["Model selection" and "hyperparameter tuning", against [[Model Selection]], a `concept` at `Knowledge/Machine Learning/Evaluation/Model Selection.md` with `up: "[[Generalization]]"`, claiming `hyperparameter tuning`, `hyperparameter search` and `model comparison`. SAME THING and AMEND, and two separate sections of the chapter are addressed to it, `#### 6 Steps for model selection` at line 6 and `#### Soft AutoML: Hyperparameter Tuning` at line 114. What the chapter adds is not a procedure, since the argmin over candidates is already there, but six judgements about how the set of candidates gets built and compared: the state-of-the-art trap, start simple, the human bias by which an engineer who likes an architecture spends longer on it and therefore gets more out of it, the demand that architectures be compared under comparable setups, good performance now against later, and the trade-offs. Decide whether those are subsections of that note, a note of their own, or split, and note that two of the six are already owned elsewhere, the now-against-later question by [[Learning Curve]] and the false positive against false negative trade-off by [[Precision-Recall Tradeoff]]. `AutoML` as a word is claimed by nobody. "Ensembles", "bagging", "boosting", "stacking", "base learner" and "weak learner", against [[Ensemble Learning]], a `concept` in `Models/Ensembles/` with `up: "[[Machine Learning]]"`, claiming `ensemble`, `ensembles`, `ensemble methods`, `base learner` and `weak learner`. SAME THING on the umbrella and AMEND, and this is the largest single decision in the run because the real question is what gets created underneath it. That note already carries the variance formula with its correlation floor, the four families in a line each, and the measured random forest result, and it carries an explicit promise that the mechanisms "are deliberately absent from this note and arrive with a later source". [[HOML]] chapter 7 is titled "Ensemble Learning and Random Forests" and has not been read, so this chapter is a survey arriving before the treatment. Argue both sides in the plan with the cost named: three method notes written from a survey will be rewritten when the chapter that actually teaches them arrives, and a fourth deferral leaves a one-note folder whose one note says its members are missing. Note also that `random forest` is named in eleven notes and owned by none, that voting is a fourth family the vault names and the chapter does not, and that `base learner` and `weak learner` are already spoken for, so nothing new may claim them. "Baselines", "random baseline", "zero rule baseline", "human baseline", against [[Baseline Model]], a `method` with `up: "[[Model Selection]]"` claiming twelve aliases including `baseline`, `baselines`, `dummy classifier` and the four `Dummy` spellings. SAME THING and AMEND, and it is the cleanest amendment in the run. Two of the chapter's five baselines are already derived there with formulas, the random baseline as $\sum_k \pi_k^{2}$ and the zero rule as $\max_k m_k/m$, so `zero rule` is a name for something the note already owns and becomes an alias rather than content. Three are genuinely new and not one of them is a `DummyClassifier` strategy: the simple heuristic, which is the atom with the empty definition; the human baseline, which is a measurement of people rather than a fitted estimator; and the existing solution, which is whatever system the model is meant to replace. Decide whether a baseline that is not an estimator strains that note's `method` shape, since `## Hyperparameters` and `## Implementation` have nothing to say about asking a person, and record the strain in the report and in the chapter note rather than reaching for a new kind. "Perturbation tests", against [[Perturbation]], a `concept` with `up: "[[Data Augmentation]]"` claiming `perturbation`, `perturbations`, `perturbation-based method` and four adversarial spellings. Not SAME THING and not obviously DIFFERENT THINGS, because the operation is identical and the purpose is opposite: there the perturbed instance is manufactured to be trained on, here the perturbed test split is scored and the fall in the score is the reading. SPECIAL CASE is arguable in both directions, so the verdict is argued rather than assumed, and whichever way it goes the note that owns the word carries a sentence pointing at the measurement use. Note that `perturbation-based method` is close enough to `perturbation test` that the alias rule has to be checked rather than eyeballed. "Invariance tests" and "directional expectation tests", claimed by nobody, and the bare word `invariance` is unavailable because it already appears in four notes in four senses, in [[Data Augmentation]], [[Resampling]], [[Feature Scaling]] and [[Moment]]. These two and the perturbation test are three of the four test types of a single paper, so treating them as three unrelated bullets is the failure to avoid: decide whether the family is one note or three, and whether the fourth type the chapter never names belongs in it. My QUESTION at line 153 is answered here rather than dropped, and the answer is a distinction rather than a yes or a no, since an invariance test does not remove a feature, it changes one while holding the others fixed and requires the prediction not to move, which is a statement about what the model should be insensitive to and not about what information it has. "Model calibration", "calibration curve" and "Platt scaling", claimed by nobody, and this is the term the vault leans on hardest without owning, which makes it the strongest CREATE case in the run. Four notes already use the word and each would gain a link rather than a restatement: [[Softmax Regression]]'s failure modes say nothing enforces that a predicted probability matches the empirical frequency, [[Scikit-Learn Estimator API]] says `predict_proba` is calibrated only as well as the model happens to be, [[Multiclass Classification]] says softmax supplies calibrated-looking probabilities, and [[Active Learning]] actually calls `CalibratedClassifierCV` in its implementation while its failure modes say an uncalibrated model makes every uncertainty measure meaningless. Whatever the new note claims has to be consistent with all four. "Confidence measurement" and "certainty threshold", against [[Association Rule Learning]], whose `## Formal statement` defines $\text{confidence}(A \Rightarrow B) = P(B \mid A)$, a completely different quantity, and against the `confidence:` frontmatter key every note in the vault carries. The bare word is unavailable twice over and nothing may claim it. The material itself is a threshold on a per-instance score, which is [[Precision-Recall Tradeoff]]'s subject from another side, since that note already claims `decision threshold`, `classification threshold` and `score threshold`. Decide what is left once that note and the calibration note have taken what is theirs, and note that the one thing neither owns is abstention, declining to answer at all rather than answering differently. "Slice-based evaluation" and "Simpson's paradox", claimed by nobody. `slice` appears in seventeen notes, almost always in the gradient-descent sense of a slice of a batch, which collides in search rather than in meaning. The near owners are [[Error Analysis]], which is this material done by hand on a [[Confusion Matrix]] and which line 168 names as one of the three ways slices are found, [[Class Imbalance]], whose whole subject is one particular slice being small, and [[Data Mismatch]], which claims `distribution shift`. Decide which note owns the claim that an aggregate score hides a subgroup score and make sure only one does. Simpson's paradox is a separate decision, since it is a fact about weighted averages rather than about models and `Knowledge/Mathematics/` has a claim on it, which the neighbouring-domains parameter answers with a presumption rather than a rule. "Experiment tracking", "versioning" and "artifact", claimed by nobody, and the hardest placement decision in the run. `artifact` is already in use across a dozen notes in this domain, almost always meaning the fitted model object, which is not the chapter's sense of a file generated during an experiment, so the bare word is unavailable and any note here says which sense it means in its first sentence. The folder whose name fits best is the folder whose own boundary rule excludes it: [[MLOps]]'s scope paragraph says a note earns a place there when it is about a model already in production and that the pre-deployment half already has owners in `Data/`, `Evaluation/` and `Composition/`. [[Machine Learning System]] holds the nearest claim on disk, that deployed behaviour is a joint function of code, data and the fitted artifact so each is pinned separately, and [[Maintainability]] one domain over says what is versioned is not only the source. [[DMLS Ch02 Introduction to Machine Learning Systems Design]] left this open and named this chapter as the owner of the experiment half, which is the promise below. "Debugging", claimed by nobody, and `debug` appears only in three notes in other domains. Two of the chapter's three techniques are already spoken for: "start simple and gradually add components" is the same advice the model selection section gives and must not be written twice, and "set a random seed" is [[Random Seed]], a `concept` in `Data/Sampling/` with `up: "[[Random Sampling]]"` whose `## Where it is used` is entirely about splits, so it gains the reproduce-a-training-run use rather than a second note being written. "Overfit a single batch" is the one genuinely new technique and it is a procedure with a pass condition, which is the shape of a `method`. "Distributed training", "data parallelism", "model parallelism", "pipeline parallelism" and "gradient checkpointing", claimed by nobody anywhere in the vault, which is the largest unowned block in the chapter. The near neighbours inside this domain are [[Out-of-Core Learning]], whose definition is line 99's problem exactly and whose formal statement is the chunk partition, [[Mini-Batch Gradient Descent]], which owns the batch size the whole section turns on, and [[Batch Gradient Descent]]; the near neighbours outside it are in the parameter above. Decide whether this is one note or a family, and decide the domain question before the folder question. "AutoML", "neural architecture search" and "learned optimizer", claimed by nobody. The soft half is the first collision in this list and is already owned twice over, by [[Model Selection]]'s aliases and by the `Hyperparameter Search/` folder, so what is genuinely new there is Bayesian optimization, named once at line 122 and owned by nobody. The hard half has no owner and no near neighbour, and it is the part of the chapter that most clearly names a subject rather than defining one, three components in three bullets one of which is itself nearly empty. Decide what that earns, remembering that a term a chapter merely names in passing is a line in another note rather than a note of its own. The three optimizers at line 132 are one owned and two unowned, [[Stochastic Gradient Descent]] against Adam and momentum, and momentum is not an optimizer in the sense the other two are, which is a correction rather than a slip. "Model assumptions", "IID", "smoothness" and "inductive bias", claimed by nobody, and this is the collision an earlier chapter predicted in writing: [[DMLS Ch02 Introduction to Machine Learning Systems Design]] recorded that any future note on inductive bias must be qualified in its first sentence against [[Bias-Variance Tradeoff]]'s `bias`, a precise statistical quantity already load-bearing in several notes, and named this chapter by title as the likeliest trigger. The seven assumptions at lines 28 to 34 are that note's subject arriving. Decide whether they are one note, a section of [[Model]], or a line each in the notes that already make each assumption, and note that one of the seven is stated wrongly in the raw file: line 29 makes IID a neural network assumption, and it is an assumption of nearly all supervised learning, including every linear model in `Models/Linear/`, which is a correction and not a typing slip. Line 31's tractability claim is a second research item on the same block, since a large family of generative models exists precisely because the posterior over the latent is not tractable and is approximated instead. "4 Phases of ML Development", against [[Machine Learning Project Lifecycle]], a `concept` claiming five aliases whose third phase is machine learning model development. DIFFERENT THINGS at the level of the word and the same subject at the level of the tree, since the chapter's four phases are a subdivision of that note's third phase rather than a rival cycle. Whatever is decided, two notes must not each define a phase sequence. This atom is also the run's largest empty heading, so the content comes from research or the atom is dropped with its reason. "Trade-offs", against [[Precision-Recall Tradeoff]] and [[Bias-Variance Tradeoff]]. The false positive against false negative trade-off at line 23 is already derived in the first and is linked rather than restated. The compute against accuracy trade-off at line 24 is owned by nobody in this domain and [[Production Machine Learning]] is where a model's cost to serve already lives; it is also one of only two places `latency` appears in this chapter's raw file. Named products and tools, of which only one appears anywhere in the vault today: MLflow, Weights and Biases, auto-sklearn, Keras Tuner, Ray Tune, BERT, Adam, and XGBoost, which is named once inside [[Feature Importance]]. Products do not earn notes under the rules already in force; they are named inside the note describing what they implement and dated wherever a claim about them is a claim about a market. The garbled name at line 120 is repaired, not preserved, and the attributions at lines 118 and 119 are checked rather than copied. Forward collisions to record and decline, each with the chapter that triggers it: A/B testing and test in production, to [[DMLS]] chapter 9, a deferral three chapter notes already carry; model registries and the experiment-tracking tool market, to [[DMLS]] chapter 10, which [[DMLS Ch02 Introduction to Machine Learning Systems Design]] named explicitly as the registry half; data distribution shift, to [[DMLS]] chapter 8, which also still owes [[Model Rot]] its `concept drift` and `data drift` aliases; interpretability at depth, of which SHAP is an instrument, to [[DMLS]] chapter 11, where [[Feature Importance]] already sent it; the ensemble methods as a book that teaches them teaches them, to [[HOML]] chapter 7, which is the second collision in this list and is decided rather than declined; the architectures a neural architecture search searches over, to [[HOML]] part II; and decision trees, to [[HOML]] chapter 6, which is also the chapter [[Node]] has been waiting on for three runs. Hierarchy-aware evaluation is not declined here, because three chapter notes now owe it to this chapter by name; it is treated below under promises.]
- Domain boundary questions this run must answer: [Six, and the first two are the ones the plan leans on. First, where experiment tracking and versioning live, given that the folder whose name fits excludes them in writing and that [[DMLS Ch02 Introduction to Machine Learning Systems Design]] named this chapter as their owner. The candidates and their costs: `Operations/`, which means rewriting [[MLOps]]'s scope paragraph and the hub sentence that restates it, so a boundary changes rather than a folder growing; `Evaluation/`, which is about estimating generalization honestly and has nothing to do with recording a run; `_Foundations/`, beside [[Machine Learning System]], whose code-data-artifact claim is the nearest thing on disk; or a new folder for the practice of developing a model, which the folder rule permits only if this run actually produces the notes to fill it and which would be the first new folder in this domain since `Labeling/`. Answer it with the count in hand. Second, whether the three ensemble methods are written now or deferred to [[HOML]] chapter 7. This is the run's largest decision and it is taken in the plan with both costs named, never inside a packet, and whichever way it goes [[Ensemble Learning]]'s standing promise is rewritten to say what is actually true afterwards. Third, where distributed training goes. The cross-domain half is settled by the link-never-duplicate rule and by the boundary in the neighbouring-domains parameter; what is not settled is whether the ML side is one note or a family, and which folder holds it, since `Optimization/` is described in the hub as the mechanics of fitting, meaning the search through [[Parameter Space]], and a training run spread over many machines is about where that search happens rather than about how it steps. Fourth, whether the behavioural test family is one note or three, and where it sits relative to [[Error Analysis]], which is the vault's existing protocol for reading a model's mistakes, and [[Perturbation]], which owns the operation. Fifth, whether the assumptions block becomes a note at all, and if it does, how its first sentence is qualified against [[Bias-Variance Tradeoff]], which is a requirement an earlier chapter wrote down rather than a preference. Sixth, and reported rather than reopened: `Machine Learning/Operations/` holds four notes and the standing position from [[DMLS Ch01 Overview of Machine Learning Systems]] is that nothing moves, with two triggers, the folder passing 25 notes or [[DMLS]] chapter 10 arriving; report the count after this run, and note that the first boundary question above may move that folder's boundary for the first time even though neither trigger has fired. Report also the two sizes the previous run left open and this chapter does not touch, `Data/Preprocessing/` at ten notes and the `Data/` top level at seven. And report, for the sixth consecutive chapter, what this chapter's raw file contains on latency and throughput, because the answer has changed again and the record is easy to get wrong: `latency` appears twice, at lines 5 and 24, both times as `inference latency` and both times as a property a model is selected on rather than as a quantity being defined, and `throughput` does not appear at all. Two qualitative mentions is stronger than chapter 5's single one and weaker than a definition, so the debt is still not discharged and [[DDIA]] chapter 2 is still where both are expected.]
- Promises this run must keep or correct, before anything else is decided: [five, and three of them name this chapter by number in writing. First, [[DMLS Ch02 Introduction to Machine Learning Systems Design]]'s open question on versioning and reproducibility says "DMLS chapter 6 owes the experiment half and chapter 10 owes the registry half", and records that [[Maintainability]] and [[Scalability]] both ask for something neither owns. This run keeps the experiment half or rewrites the line to name what actually owns it. Second, the same note's inductive bias question names this chapter by its title as the likeliest trigger and states in advance the qualification any such note must carry. Third, hierarchy-aware evaluation is owed here by three chapter notes, [[DMLS Ch02 Introduction to Machine Learning Systems Design]], [[DMLS Ch04 Training Data]] and [[DMLS Ch05 Feature Engineering]], and this chapter's raw file contains nothing about label hierarchies at all: its offline evaluation half is baselines, four test types, calibration, confidence and slices. So the promise is either kept by research, if the book's chapter treats it and I did not write it down, which is the empty-heading disposition applied to a whole subject, or it is rewritten to name the chapter that actually owns it, settled against the book's own table of contents rather than guessed. This is exactly the shape of the previous run's static-and-dynamic-feature promise, and three notes carry it. Fourth, [[DMLS Ch05 Feature Engineering]] left `time-based split`, `temporal split` and `split by time` unclaimed and owed them here, and the raw file does not contain them either, so the same disposition applies and the report says which of the two happened and on what evidence. Fifth, [[Ensemble Learning]] promises in its own prose that the mechanisms arrive with a later source, which is a promise this run either keeps or rewrites to name [[HOML]] chapter 7. A promise this run cannot keep is rewritten, never left to rot, and every note carrying it is edited in the same sitting so that all of them say the same true thing afterwards.]
- Working tree at the start of the run: [clean, apart from one untracked directory. `git status --short` reports exactly one line before this run writes anything, `?? "_Slip Box (Zettelkasten)/"`, which is the slip box itself and is expected. The previous run's work is committed in full at c8fd055. Anything else appearing in that output at Phase 5 is this run's own doing, and it is either a file the plan named or a mistake to reverse by hand and never with a git command.]

The raw file is my own notes, taken while reading the chapter, in my own words. It is the source of truth for what I understood. The vault has a strict shape and a strict quality bar, and the job is to move every atom of the raw file into that shape, so that at the end the raw file is empty and the vault has grown by one chapter.

Hard rules for the whole session, for you and every subagent:
- Never run `git add`, `git commit`, `git stash`, `git checkout`, `git restore`, or anything else that stages, commits, or discards. `git status`, `git diff`, `git log` and `git show` are allowed, read only.
- Never delete the raw file. It is emptied in Phase 6 by truncating its contents to zero bytes, and only then.
- Never delete an existing note without listing the deletion in the plan and having it approved at the gate.
- Every file is owned by exactly one subagent per phase. Two subagents never write the same file.
- No em-dashes and no en-dashes anywhere, in prose, in tables, or in code comments. Use commas, colons, parentheses, or a new sentence. Normalize any the raw file contains.
- Never write "e.g." Write "ex" or restructure the sentence.
- Every note body is a restatement in the vault owner's voice. The raw file's wording is preferred wherever it already says the thing. Book phrasing is never copied. Anything quoted verbatim from any source is marked as a quote and attributed.
- The preference for the raw file's wording holds only where the raw file is actually in my voice. A raw bullet written in a register the rest of the file does not use, typically a long fluent sentence with marketing adjectives where the surrounding bullets are terse fragments, is very likely pasted from a glossary or a vendor page and must be restated from scratch rather than preserved. Flag every such bullet in the Phase 4 report as a suspected paste, with the raw wording, so I can see which of my own notes were not mine.
- A heading that names a source is a defect, without exception: no `### From Chapter 1`, no `### What the book says`, no `### Kleppmann's version`. New material is merged into the section it belongs in by topic, and any subsection is named for what it contains. "The chapter" and "the book" are never bare subjects in the prose of a note under `Knowledge/`; name the source with a link or state the fact without attribution. Attribution lives in `sources:` and in links.
- A note belongs to exactly one domain. Where this chapter's material overlaps a neighbouring domain, the rule is link, never duplicate: if a note already exists there, amend it and link to it, and do not restate its content in this domain. Where the overlap is real but the existing note says something different rather than something less, that is a research item and a correction, not a second note.
- A word that already exists in the vault is a collision until proved otherwise. Before creating any note whose title, or any alias you would put on it, matches an existing note's title or alias, read that existing note in full and return one of exactly three verdicts, recorded in the plan with the reason:
  - SAME THING. Two names for one concept. One note survives, in the domain that owns the concept, the other name becomes an alias on it, and the second domain links to it and restates nothing.
  - DIFFERENT THINGS. Two notes. Each one's `## Definition` names the other and says in one sentence what separates them, with a link, so a reader who arrived at the wrong note leaves immediately rather than reading the wrong thing carefully. Neither note may claim the bare ambiguous word as an alias.
  - SPECIAL CASE. One is the other applied to a narrower setting. The general note lives in the domain that owns the general case; the specific note points `up` at it, or links to it with a phrase naming exactly what it specializes, and does not repeat the general note's definition.
  The failure this prevents is not a merge conflict, it is silence: two notes describing different things under the same word, neither aware of the other, both looking correct in isolation.
- Aliases are globally unique across the vault. Obsidian resolves a link by name, so two notes claiming one alias makes one of them unreachable under that name with no warning and no error. Before any packet is dispatched, check every seed alias in every packet against every title and alias already in the vault, and against the other packets. A word that is ambiguous on its own is not an alias for anybody: it is qualified in both places or dropped from both alias lists.

Work in six phases. Do not skip a phase and do not start writing files before the gate.

PHASE 1: STRUCTURE (subagents, in parallel, read only).
Launch two read-only exploration subagents at once and wait for both.

Subagent A, the vault contract. Have it read in full and report back, quoting rather than paraphrasing wherever a rule is exact: `__Meta/Quality-Bar.md` (the whole file, it is the spec), `README.md`, `Home.md`, `Notation.md`, `__Meta/Callouts.md` (only the list of callout types), every file in `__Meta/templates/`, and `.obsidian/types.json`. Its report must contain: the frontmatter contract per note_kind, the required section headings per note_kind verbatim, the three rungs of `## Formal statement`, the fixed discharge string for the bottom rung and where in the section that string goes, the hyperparameter test, the disqualification list, the heading rules (no H1 under `Knowledge/` and none on a chapter note, H1 on a container note and on any document read front to back, `##` for required sections, `###` for subsections), the rule against headings that name a source, whether a Bases query block is part of the required shape of an `index`, whether the list heading of an index is fixed or named for what it lists, and whether a forward-looking list of material not yet written is part of an index's shape or a defect in it, the discharge available to `## Implementation` in a domain with no pinned library, and what the file says about adding a new note_kind and when that question reopens.

Subagent B, the vault as it stands. Have it produce: the full tree of `Knowledge/` and `Sources/` with one line per file giving path, note_kind, `up`, and `confidence`; the complete text of every domain hub under `Knowledge/`, meaning the `index` note whose `up` is `[[Home]]`, including each one's scope statement and its Areas list in full; the complete text of the exemplar chapter note named in the parameters; the output of `git log --oneline -n 12`, and then `git show --stat` on the most recent commit that integrated a chapter, as the exemplar of what a chapter integration touches (new notes, the hub, Home, the container note, and backfilled `sources:` and `up:` on existing notes); and, when the domain status is FIRST RUN, additionally `git show --stat 5a1a0c2`, the commit that created the `Mathematics` and `MLOps` domains, as the exemplar of what starting a domain touches. Also have it report every line anywhere in `Knowledge/`, `Sources/`, `Home.md` and `README.md` that mentions the chapter being integrated, the book being integrated, or the subject domain by name, because each one is a promise this run must keep or correct; every `[[wikilink]]` target in the raw file that does not resolve to an existing note; and every existing note whose title or `aliases` collide with a term in the raw file, which is the input to the collision rule above. Finally, have it report which existing notes end with a Bases query block and which do not, so new notes can follow the pattern of their neighbours rather than invent one.

Do not proceed until both reports are in. Keep both reports in your context for the rest of the session.

PHASE 2: READ THE RAW NOTES (you, personally, in full).
Read the raw file from the first line to the last with no offset and no limit. If the file is long, read it in consecutive chunks until you have seen the final line, and say the final line back to yourself so you know you reached it. Do not delegate this. Do not summarize it yet.

Then read, yourself, every exemplar note named in the parameters, and the exemplar chapter note, so you know what a finished note looks like in each shape you will be producing.

PHASE 3: INVENTORY AND PLAN (you, personally).
Build the plan in this order and write it to a scratch file outside the vault, not into the vault.

Step 3a, the inventory. Split the raw file into atoms. An atom is the smallest piece that lands in one place: usually one heading with its bullets, sometimes a single bullet, a single table, or a single code block when its siblings go elsewhere. Number the atoms in file order. Every line of the raw file belongs to exactly one atom. Tables, figures and code blocks that only reproduce an exhibit from the source are still atoms; they are allowed to be dropped, but only by an explicit decision with a reason.

Three shapes of atom need naming before they are missed. A heading with nothing under it is an atom, and its disposition is a decision: either the chapter covered it and I did not write it down, in which case the plan says so and the material is researched or deferred, or it is a stub for a later reading session, in which case it is dropped with that reason. A term listed with an empty definition is an atom, and the first place to look for its definition is later in the same raw file, where a term glossed in one line at the top is often developed in full under its own heading further down; when nothing in the raw file defines it, the note is still owed a definition, which comes from research and is reported as an addition rather than quietly written as though I had said it. A bullet whose content is a bare cross reference, with no claim of its own, is an atom whose disposition is a link rather than a note.

Step 3b, the destination table. One row per atom: atom number, raw heading, action, target path, note_kind, `up`, what this atom contributes, and why here. Actions are exactly one of: CREATE a new note, AMEND an existing note (say which section), RENAME or MOVE an existing note (old path to new path), DELETE an existing note (with the reason and where its content goes), DROP the atom (with the reason). A raw atom may feed several rows, since one passage often carries both the definition of a thing and a claim about a different thing it is being contrasted with. Every unresolved `[[link]]` from the Phase 1 report must appear as a CREATE row or the plan is incomplete.

Step 3c, the structural decisions. Decide and write down, with the reason, every placement question the chapter raises, checked against the Areas list of the domain hub and against the folder rule (the folder says what kind of thing a note is, `up` says what it is part of). Do not add a new note_kind. Every decision here is a decision about where things go and what earns a note, never a decision about what is true.

Step 0 of this step applies only when the domain status is FIRST RUN, and when it applies nothing else in the plan is settled until it is. Read the domain hub named in the exemplars first, because it is the precedent: it was started from a single chapter of evidence, it opens with a paragraph saying what earns a place in the domain and what does not, and it has an Areas list whose entries are folders with the notes in them named in prose. Read it against what Subagent A reports about the index shape rather than copying it wholesale, since a hub written earlier may carry a section the contract has since dropped. Then decide and write down, with the reason:

- The domain's name and folder, and the hub note's path, which is the folder name repeated as the filename, matching `Machine Learning/Machine Learning.md`.
- The membership rule, one paragraph: what earns a place in this domain, and what looks like it belongs but goes to a neighbouring domain instead. Write this as that hub writes it, as a boundary against the neighbours rather than as a description of the subject. This paragraph is the thing that stops the next five chapters from being filed by accident, so it is worth more effort than anything else in this step.
- The folder taxonomy, with the evidence for each folder drawn from the atoms in the inventory and named atom by atom. One chapter is thin evidence, so the default is few folders. A folder is earned by holding notes this run actually produces, not by naming a topic the book will reach later; a topic with one note and a promise of more is a note at the top level of the domain, and the folder arrives with the second note. State plainly which folders are being created on one note of evidence and why that one note could not sit at the top level instead.
- Which atoms are not domain material at all. A chapter that opens a subject typically carries organizational material, meaning who does which job and what the teams are called, and it carries vendor and product names. Decide whether each of those is a note, a line inside another note, or a drop, and if the organizational material is a note, decide whether this domain or a neighbouring one owns it.
- The hub note itself: `note_kind: index`, `up: "[[Home]]"`, no H1, a scope paragraph, `## Areas` rather than `## Methods`, and a Bases query block, which every index carries. Whether it also carries a list of material not yet written is decided by what Subagent A reports, not by what an older hub happens to contain: an index that states what is on disk is honest about a domain one chapter old by being short, and a list of predicted arrivals is a claim made on the weakest evidence there is.
- The line in `Home.md` under `## Domains` that introduces the domain, in the shape of the lines already there, and whether `README.md`'s layout table needs the domain named too.

The rest of step 3c applies to every run. At minimum decide: which atoms become notes at all, given that a term the chapter merely names in passing is a line in another note rather than a note of its own, and say which terms you are refusing a note to and why; for each note, whether it is a `concept`, a `method` or an `index`, checked against the concept and method test in section 2 of the quality bar (a method is something you carry out as itself and that changes something downstream, or an ordered protocol whose reading depends on the order; everything else is a concept) and against the rule that `index` is earned by having members to list and that a topic with none is a `concept` carrying its own definition; whether any existing note should convert between kinds under that same rule; which notes are paired contrasts that want the optional `## VS` section rather than two notes each describing the other; which atoms touch a neighbouring domain, and for each one whether the existing note there is amended and linked or whether this domain owns a genuinely different thing under a similar name; and whether a dataset note is required, given that the quality bar wants one per dataset a chapter works on. Where a decision contradicts a promise an existing note made, say which promise is kept and which is corrected, and never leave both standing.

Also produce, as its own table, the collision ledger: one row per term in this chapter that matches an existing note's title or alias, whether the parameters named it or Subagent B found it, giving the term, the existing note, the new note or amendment, the verdict of SAME THING, DIFFERENT THINGS or SPECIAL CASE, and the exact sentence each side will carry to tell itself apart from the other. A term with no row is a term nobody checked.

Do not reach for a new note_kind. The quality bar says when that question reopens, and the answer is never "during an integration run". A note that fits no existing kind comfortably is written to the kind it fits least badly and the strain is recorded in the final report and in the chapter note's `## Open questions`, which is the evidence a kind is eventually argued from.

One decision is larger than placement and gets its own line in the plan when it arises: whether this run's material argues for restructuring an existing domain rather than only adding to one. A domain that turns out to be a special case of something this chapter introduces is a real finding and is allowed to move. It is proposed at the gate with its cost named in full: every file that moves, every `up` that changes, and every sentence in `Home.md`, `README.md` and the other hubs that names the old path in words, since wikilinks survive a folder move and prose does not. Never restructure silently and never as a side effect of a packet. Where the cost is small today and grows with every chapter, say so plainly, because that is an argument for doing it now rather than an argument against doing it at all.

Step 3d, the promises. For every line the Phase 1 report found that mentions this chapter, this book, or this subject, write the exact edit that retires the promise. At minimum this covers: the container note, whose chapter line becomes a link, or, where the link is already there, whose link must now resolve to a written note; `Home.md`, whose `## Sources` list records the book and whose `## Areas` list records the domain; `README.md`, wherever it describes what lives under each path or counts what the vault holds; the domain hub's Areas paragraphs, which name the notes in each folder in prose and so go stale the moment a folder gains one; and every note in a neighbouring domain that names this subject or promised this material. Add any others the report found. A promise that this run cannot keep is rewritten to say what is actually true, not left to rot.

Step 3e, the chapter note. Plan the chapter note at the path in the parameters, from `__Meta/templates/Note Kinds/Source (Chapter).md`: frontmatter with `note_kind: source`, `medium: book`, `chapter:` as the number from the parameters, `up:` as a link to the container note, `url:` from the parameters, and aliases for the chapter number and the chapter title. Then `## Thesis`, `## Extracted` grouped by theme with a group for notes amended rather than produced and a group for hubs and indexes revised, and `## Open questions`. It takes no H1. Also plan which open questions in earlier chapter notes, of this book and of any other, this chapter settles or partly settles, and what edit records that.

Step 3f, the research list. For every claim in the raw file that is uncertain, marked with a question mark, internally inconsistent, or likely wrong, write a research item: the claim, what to check, and which primary source settles it, using the ordering in the parameters. Do the same for every named system, product, service, protocol or standard in the raw file: what it is, who makes it, whether it still exists under that name, and whether the property the raw file attributes to it is true of it today rather than at the time some older source described it. A product list in the raw file is a research item in its own right, because product lists go stale faster than anything else in a note, and a list that was right when the book went to press may not be right now. Where the raw file carries formulas, every one gets rederived or checked against a reference; where it carries none, this clause is discharged and an arrow or an inequality used as shorthand in prose is not a formula.

Step 3g, the work packets. Group the destination rows into packets such that no file appears in two packets, and packets are independent of one another. Then fix, before any packet is dispatched, the final title of every note that will be created or renamed, because every packet links to notes owned by other packets and the link text must already be right. Each packet contains: the atom text verbatim, the target file or files and the action on each, the note_kind and its exact required sections from the Phase 1 report, `up`, a seed list of aliases (every name a person would type in a link, including plurals, abbreviations, the expansion of every acronym and the acronym of every expansion, and the name any widely used system gives the thing), the outbound links this note must make and the phrase that explains each relationship, the notes elsewhere that will link back to it, the research items that belong to it, the exemplar note to read first, and the style rules from the hard rules above. Every packet also carries: "Do not touch any file not named in this packet. Do not run git write commands. Report back before finishing."

Write the plan as: the destination table, the structural decisions with reasons, the promise edits, the chapter note outline, the research list, and the packet list. Then present it to me in full.

GATE: Stop here and wait for my explicit approval of the plan. Apply any changes I ask for to the plan before continuing. Do not write any file inside the vault before I say go.

PHASE 4: EXECUTE (subagents, in parallel where packets are independent).
Dispatch every packet to its own writing subagent. Independent packets go out together in one batch. A packet that amends a file another packet creates waits for that packet to finish. When the domain status is FIRST RUN, the packet carrying the domain hub goes last, because it describes what the other packets actually produced. Each writing subagent, in this order:
1. Reads `__Meta/Quality-Bar.md` and the exemplar note named in its packet.
2. Reads every existing note it is told to link to, so the link phrase is true and the `up` chain is consistent.
3. Does the research in its packet against primary sources, and records for each item: the finding, the source, and whether it confirms, corrects, or extends the raw notes. Research is for correctness and completeness. It is not licence to replace the raw file's wording with a source's wording.
4. Writes or amends the note to the contract for its kind. New notes get `confidence: draft`, never anything higher. Amended notes get this chapter appended to `sources:`, and the new material is merged into the section it belongs in by topic, sharpening or correcting what is there rather than being appended as a layer. A subsection is created only when the new material is a distinct sub-topic, and it is named for what it contains and never for where it came from. Where the new material contradicts what the note already says, the contradiction is settled by research and the note is rewritten to say one thing; two sections each asserting a different thing is never an acceptable outcome. A Bases query block is part of the required shape of an `index` and every new index gets one; on any other kind it is appended only where the note's neighbours in the same folder carry one.
5. Self-checks before reporting: every required section present and non-empty, `## Where it is used` has at least two outbound links each carrying its relationship phrase, every quantitative claim has a formula somewhere in the note, the discharge string is exact and last in its section if used, no H1 on a note under `Knowledge/` and none on a chapter note, no heading that names a source, no bare "the chapter" or "the book" as a subject in the prose, no dash characters of either kind, no "e.g.", no placement commentary in the body, frontmatter keys spelled exactly, `up` is a link and is never `[[Home]]` for a leaf.
6. Reports back with: files written or changed, with a one-line summary each; every fact in the raw notes it corrected, with the raw wording, the corrected wording, and the source; every claim it added that the raw notes did not contain, with the source; every raw bullet it suspects was pasted from elsewhere rather than written by me, with the raw wording; every research item it could not settle, stated as uncertain rather than guessed; and any deviation from its packet with the reason. A typing slip is not a correction. A misspelling, a transposed word, or a word the surrounding sentence plainly contradicts is fixed silently and listed in one short line at the end of the report, separately from the corrections, so that the corrections section stays a list of places I understood something wrongly rather than a list of places I typed badly.

Keep every report. You will need them in Phase 6.

PHASE 5: VERIFY (you, personally, with scripts where possible).
Run these checks over the whole vault and fix what fails, either yourself for one-line fixes or by sending the owning subagent back for anything larger:
- Every `[[wikilink]]` in `Knowledge/`, `Sources/`, `Home.md` and `README.md` resolves to an existing file, allowing for `|alias` display text and `#heading` suffixes.
- Every note under `Knowledge/` has `note_kind`, `up`, and (where its kind requires them) `aliases`, `sources`, and `confidence`, with the keys spelled exactly. Every `up` value resolves.
- Every new or amended note carries the required `##` sections for its kind.
- No note under `Knowledge/` carries an H1, and neither does any chapter note. Container notes, dataset notes, `Home.md`, `README.md`, `Notation.md` and the files under `__Meta/` do carry one, and this check must not strip it.
- No note under `Knowledge/` carries a heading that names a source, and none uses "the chapter" or "the book" as a bare subject in its prose.
- No file under `Knowledge/`, `Sources/`, `Home.md` or `README.md` contains an em-dash, an en-dash, or the string "e.g.".
- Every note the chapter produced or amended is listed under `## Extracted` in the chapter note, and every note listed there exists and names the chapter in its `sources:`.
- Every promise line from Phase 3d is gone or rewritten.
- No alias appears on two notes, and no note claims an alias that is another note's title. Collect every `aliases` entry and every filename across the vault and check for duplicates mechanically, since this failure is silent in Obsidian and invisible on inspection.
- Every row of the collision ledger landed: the verdict was applied, and where the verdict was DIFFERENT THINGS both notes actually carry the distinguishing sentence and the link.
- The container note links the chapter, `Home.md` counts it, and `README.md`'s layout table counts it. When the domain status is FIRST RUN, `Home.md` also introduces the domain and the hub's Areas list names every folder the run created.
- Every atom in the inventory is accounted for: walk the destination table row by row and confirm the content actually landed where the row says, in substance, not just by file existence. An atom marked DROP has its reason in the plan.
- `git status --short` shows only the files the plan named, plus the entries the working tree parameter says were already there before the run started. Anything else is a mistake to reverse by hand, never with a git command.

PHASE 6: EMPTY THE RAW FILE AND REPORT.
Only when every check in Phase 5 passes, truncate the raw file to zero bytes. Do not delete it and do not move it. Then give me the final report, which has exactly these parts:
1. A table of every file created, amended, renamed, moved or deleted, one row each, with the reason in a few words.
2. Structural decisions taken, one line each, with the reason. When the domain status was FIRST RUN, the domain's membership rule and the evidence behind every folder created come first, because they are the decisions I will be living with for the rest of the book. Any restructuring of an existing domain is stated here in full, with what moved and what prose was rewritten to match.
3. The collision ledger as it was finally resolved: term, the notes on each side, the verdict, and the sentence each note carries. Include the collisions you found that the parameters did not name, and the forward collisions you declined to act on, with what will trigger them.
4. Corrections: every place the notes now say something different from what my raw file said, with the raw wording, the new wording, and the source. This is the most important section. Writing is how I learn, so I need to know exactly where my understanding was wrong. Typing slips do not belong here; they go in one line at the end of this section, counted rather than enumerated.
5. Additions: every substantive claim in the vault that came from research rather than from my raw file, with the source, so I can read them as a reviewer.
6. Suspected pastes: every raw bullet a subagent judged to be someone else's prose rather than mine, with the raw wording, so I can check my own note taking.
7. Unsettled: every research item no subagent could settle from a primary source, with what to check in the book.
8. The chapter note's open questions, restated, and which earlier open questions this chapter settled.
9. The output of `git status --short`, unstaged, uncommitted.
No praise padding and no summary of what a good job was done. Facts, decisions, corrections, and gaps.
```
