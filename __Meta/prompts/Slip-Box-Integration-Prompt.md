# Slip box integration prompt

Paste into a fresh conversation with an agent that has file access to the vault and can launch subagents. Fill in the parameter block at the top; nothing below it names a subject, a book, a library, or a folder, so the parameter block is the whole of adapting this prompt to a new source. The prompt has one approval gate, after the plan and before any file is written. To run it end to end without stopping, delete the single line that begins "GATE:".

This is a different role from `Chapter-Study-Prompt.md`. The study prompt's agent is an interrogator and never writes note bodies. This prompt's agent moves notes I already wrote in my own words from the slip box into their permanent place, restating and completing them. Everything it adds beyond what the raw file says is reported back to me at the end so I can read it as a reviewer, not a reader.

One prompt serves every subject. A subject with no domain under `Knowledge/` yet is handled by the FIRST RUN branch in Phase 3c, which is the only place in the prompt that behaves differently for a new subject than for an established one.

---

```
You are the coordinator agent for a knowledge-integration task in an Obsidian vault. The vault root is the current working directory. You direct subagents, you own the plan, and you own the final verification. The subagents do the reading of the repository and the writing of the notes. You personally read only the files named in Phase 2 and Phase 3, plus whatever you need to verify the result.

PARAMETERS.

- Raw notes to integrate: [`_Slip Box (Zettelkasten)/RAW DMLS Ch07 Model Deployment and Prediction Service.md`, the only file in the slip box. 94 newline-terminated lines plus a 95th that ends in a trailing space and no newline, the same ending as the previous run's raw file. 16 headings running two levels deep (four `##`, twelve `###`), no table, no code block, no mermaid block, and the shortest raw file of the seven so far, which is not the same as the thinnest: it is a survey of deployment choices in which almost every bullet is a claim about practice rather than a definition. The file contains no em-dash or en-dash and no "e.g.", both checked, so those two normalizations are discharged before the run starts and finding either in the final vault means a subagent introduced it. One ASCII hyphen does a dash's job, in the heading at line 2, `### Myth 1 - YOu only deploy...`, where its three siblings use a colon, so it is normalized to a colon with the heading's capitalization slip. It contains no `[[wikilink]]` at all, the third consecutive raw file with none, so Phase 1's unresolved-link report will come back empty and every link in the finished notes is one this run decided on. Every `$` delimiter is balanced, thirteen pairs of them, twelve `$\rightarrow$` and one `$\geq 100$`, so unlike the previous run there is no unclosed math. Seven atom shapes are present by inspection and are named here so they are not missed rather than decided here. First and largest, lines 40 to 47 split "the model takes too long to generate predictions" into three routes, make inference faster (optimization), make the model smaller (compression), and make the hardware faster, and only the second is developed anywhere in the file: the first and third are named and never returned to. This is the empty-heading shape applied to two thirds of a list, and it matches a whole section of the book the file skips, the one on compiling and optimizing models for edge devices (intermediate representations, operator fusion, and using machine learning to search for faster schedules), so its disposition is a decision taken in the plan, researched or dropped with the reason. Second, line 50's `types` list under low-rank factorization has exactly one member, compact convolutional filters, and names neither the architecture it came from nor the arithmetic that makes it a factorization, which is the term-with-a-thin-definition shape. Third, line 43 names the first compression technique `Low Rank Optimization` and line 48 heads its own section `Low-Rank Factorization`: the heading is right and the list entry is a slip, since the surrounding sentence plainly contradicts it. Fourth, line 18 opens a parenthesis at "(like an SQL tables or in memory database" and never closes it, the only unbalanced delimiter in the file, which is a structural slip rather than a gap. Fifth, line 17 carries a bare cross reference inside a claim: "(Data Passing Through Services)" is a phrase I copied from chapter 3's vocabulary, where it is one of the three modes of data flow, so its disposition is a link to [[Request-Driven Communication]] and [[Modes of Data Flow]], not a note. Sixth, line 6 contains two different claims joined by an arrow, one about software rot, which is a property of any program, and one about data distribution shift, which is the model-specific thing, and the arrow reads as though the second were an instance of the first; they are split in the plan and the second is chapter 8's. Seventh, line 81's list of edge devices mixes consumer devices with FPGAs and ASICs, which are hardware kinds rather than devices a consumer owns, a correction to check against the book rather than a typing slip. The file is typo-dense in its usual way: `YOu`, `overtime` meaning overcome, `research research`, `decision treed`, `then` for than, `impoves`, `with further reduces` for which further reduces, `ad thus`, `increasing popular`, `care` for cars, `its` for it's throughout, `reduct`, `replace` for replaced, and `to long`. Every one of those is a typing slip, fixed silently and counted rather than enumerated in the final report, and none of them belongs in the corrections section.]
- Chapter note to produce: [`Sources/Books/DMLS/DMLS Ch07 Model Deployment and Prediction Service.md`. The file does not exist, so the action is CREATE outright, as in all six previous runs. There is no spelling to reconcile: the raw file already says `Ch07` zero padded, which matches every chapter note in the vault and matches line 23 of [[DMLS]]'s own `## Chapters` list verbatim. The title collides with nothing: no note under `Knowledge/` is called "Model Deployment and Prediction Service", no alias anywhere contains `deploy`, and `prediction service` appears once in the whole vault, in the prose of [[Feature]], as a phrase rather than a claim. So the yield rule in section 2 of [[Quality Bar]] does not bite and this note takes both aliases the contract asks for, `DMLS Chapter 7` and the chapter's own title. Check that rather than assuming it, because this run is likely to create a note whose natural alias list includes `model deployment` or `deployment`, and the failure the rule prevents is silent.]
- Chapter number, for the `chapter:` key: [7]
- Chapter note `url:`: [https://github.com/chiphuyen/dmls-book/blob/main/resources.md#chapter-7-model-deployment-and-prediction-service. This follows the scheme the ch01 run settled and all six later runs kept. The anchor resolves, checked on 2026-09-24, but the section behind it is empty: `## Chapter 7. Model Deployment and Prediction Service` is immediately followed by `## Chapter 8`, with not one resource listed, the first chapter of seven for which that is true. The fallback rule was written for a missing heading and the heading is present, so decide in the plan whether an anchor that lands on nothing still satisfies it or whether `summary.md#chapter-7-model-deployment-and-prediction-service` is the more honest target, since that section does exist and has a paragraph of the author's own; say in the final report which was used and why. The container note keeps the repository root either way. The empty resource list has a second consequence that matters more than the URL: the book's own list is not a route to the papers this time, so the research tier below is found from the chapter's own references and from the primary literature directly.]
- Container note: [`Sources/Books/DMLS/DMLS.md`, linked as [[DMLS]]. One edit and one only: line 23, `- DMLS Ch07 Model Deployment and Prediction Service`, is plain text today and becomes a wikilink to the note this run produces. The ch01 run already corrected the numbering and checked all eleven titles against the book's own `ToC.pdf`, so that work is finished and is not reopened; a packet that finds itself editing a second line of that file has drifted and says so instead of editing.]
- Subject domain: [`Knowledge/Machine Learning/`, hub at [[Machine Learning]]. The domain holds 154 notes, fourteen more than before the previous run, across ten areas, and this chapter lands in the one area every previous run has reported and none has grown: `Operations/`, four notes, [[MLOps]] as its index plus [[Production Machine Learning]], [[Model Rot]] and [[Continual Learning]]. This is the first chapter whose centre of gravity is squarely inside [[MLOps]]'s written boundary, "a model already in production rather than a model being built", so for the first time the membership test for that folder passes on most of the chapter rather than failing on it. Two blocks are nevertheless hard to place. Model compression acts on a fitted model and is motivated entirely by serving, but two of its four techniques are training procedures in their own right, knowledge distillation trains a second model and quantization-aware training changes the fit, and pruning is named in the raw file as originating in decision trees, where it is a regularizer applied during or after growth; so compression fits `Operations/` by motive, `Optimization/` by mechanism for half its members, and neither cleanly. Cloud against edge is partly generic hosting vocabulary that `Knowledge/Systems Architecture/Hosting/` already owns in [[Cloud Computing]], and partly the model-specific question of where inference runs, which is this domain's. `Optimization/` with 14 notes, `_Foundations/` with 12, `Evaluation/` with 17 at its top level and `Data/` with 43 are expected to receive a clause each at most. The default action inverts again from the previous run. There, CREATE was the default on six unowned subjects; here the vault owns more of this chapter than its silence suggests, because [[Production Machine Learning]] already carries a full paragraph on batch serving as a first-class mode, with SageMaker, Vertex AI, Azure, the half-price batch APIs and MLPerf's Offline and Server scenarios, and already defines latency and inference in passing. So batch against online prediction is AMEND-or-CREATE argued in the plan with that paragraph in hand, never a CREATE that restates it, while model compression and its four techniques are unowned and CREATE is the default there, with a packet reaching for AMEND on one of them owing a reason in writing.]
- Domain status: [ESTABLISHED and large. Phase 3c step 0 does not apply and is skipped in full: no domain folder is created, no hub is created, and no new line is added to `Home.md` under `## Areas`. Whether `Home.md` owes anything else is a decision this run takes rather than skips, and unlike the previous two runs the case is real: its machine learning gloss at line 13 reads "how a model is specified, fitted and evaluated, and how the data it is fitted on is obtained, labelled, repaired and built into the columns the model actually reads", which says nothing at all about a model once it ships, while `Operations/` has existed since the ch01 run and this chapter is its first substantial growth. Decide whether the gloss gains a clause on deploying and serving, and if it does, keep it a clause. `README.md` names no machine learning path at all (checked) and is expected to need nothing; confirm it. The hub is again the single largest amendment in the run and is a packet of its own that goes last. Its pressure points: the `Operations/` bullet at line 38 names every note in the folder and restates [[MLOps]]'s boundary, so any note filed there is a rewrite of that bullet and any note filed elsewhere that the reader would look for there is a sentence in it; the `Optimization/` bullet at line 34 describes the folder as the mechanics of fitting, which a compression technique that retrains either fits or breaks; and the `Paradigms/` bullet at line 30 names [[Batch Learning]] versus [[Online Learning]] as the training regime axis, which is exactly the pair of words this chapter reuses on a different axis, so if batch and online prediction are written, the hub says in one sentence that the serving axis and the training axis are independent, the way it already says supervision and coverage are independent for [[Few-Shot Learning]].]
- Library pin: [scikit-learn 1.6, with NumPy 2.x and pandas 2.x alongside it, which is the machine learning domain's established pin and is evidenced on disk in every `## Implementation` the last four runs wrote. This chapter is where the pinned stack covers the least ground of any run so far, less than the previous one, so the honest negatives are most of the implementation work and [[Data Augmentation]]'s opening, "**scikit-learn has no data augmentation API**", is the model for each. Where the stack does touch the material, name the API exactly. Batch against online prediction: scikit-learn's user guide carries a "Computational Performance" page that separates prediction latency from prediction throughput and distinguishes bulk from atomic prediction, which is this chapter's distinction measured on one machine, and `sklearn.set_config(assume_finite=True)` is the documented way to cut per-call validation overhead in the atomic case; read that page for what it says in 1.6, because it may be the first source in seven runs to define latency and throughput as quantities, which bears on the debt below. Persistence for serving is `joblib.dump` and `joblib.load`, with `skops.io` as the format scikit-learn's own model persistence page recommends where loading an untrusted file is a risk, and `skl2onnx` converts a fitted estimator to ONNX for runtimes outside Python; check what the 1.6 persistence page recommends today rather than trusting this sentence. Pruning: the one place the stack has the thing itself is minimal cost-complexity pruning on trees, `DecisionTreeClassifier(ccp_alpha=...)` with `cost_complexity_pruning_path`, which is the decision-tree origin line 59 names, and nothing in scikit-learn prunes a neural network. Low-rank factorization: `sklearn.decomposition.TruncatedSVD` and `randomized_svd` exist and factorize a data matrix, not a model's weight matrix, which is the honest negative with a trap in it, since an implementation that presents `TruncatedSVD` as model compression is wrong rather than thin. The honest negatives are four. First, scikit-learn has no model quantization, no reduced-precision inference and no mixed-precision training; the real APIs are `torch.ao.quantization` and `torch.export`-based flows in PyTorch, post-training quantization in LiteRT (the name TensorFlow Lite has carried since Google renamed it in 2024, to be checked), and TensorRT on NVIDIA hardware. Second, scikit-learn has no knowledge distillation API; the nearest honest statement is that fitting a small estimator on a large one's `predict_proba` output is distillation's procedure with nothing to name it. Third, scikit-learn has no serving layer: no endpoint, no batching, no model server, and the tools that do this for any framework are third-party. Fourth, it has nothing for the edge or the browser. Say which version a claim is true of whenever the claim is about an implementation, and in this chapter that rule bites on the framework names in line 77 more than on scikit-learn itself.]
- Quantitative expectation: [High, and the failure mode is the survey one again but worse, because this raw file contains no formula at all and no arrow that stands for one, so every rung of `## Formal statement` above the discharge has to be found rather than transcribed, and a note that describes compression in prose is the easiest wrong outcome in the run. Eight atoms carry checkable content and almost none of it is owned. Low-rank factorization: a weight matrix $W \in \mathbb{R}^{m \times n}$ replaced by $UV$ with $U \in \mathbb{R}^{m \times r}$ and $V \in \mathbb{R}^{r \times n}$ holds $r(m+n)$ parameters instead of $mn$, which saves only when $r < mn/(m+n)$, and the best rank-$r$ approximation in Frobenius norm is the truncated SVD by Eckart and Young (1936); the compact filter line has its own ratio, MobileNets' depthwise separable convolution costing $\tfrac{1}{N} + \tfrac{1}{D_K^2}$ of a standard convolution for $N$ output channels and a $D_K \times D_K$ kernel, which is what "reduce the number of parameters and increase speed" at line 51 is a slogan for. Knowledge distillation: the softened softmax $p_i = \exp(z_i/T) / \sum_j \exp(z_j/T)$ and a loss mixing the hard-label cross-entropy with $T^2$ times the divergence between teacher and student at temperature $T$, where the $T^2$ keeps the soft term's gradients on the same scale as $T$ changes, which is the whole method and is absent from the file. Pruning: magnitude pruning to a sparsity $s$, and the storage claim at line 60 is conditional rather than true, since a sparse format stores an index beside every surviving value, so compressed sparse row storage beats dense only below a density that depends on the value and index widths, roughly one half when both are 32 bits; the claim is written as that inequality or it is qualified in words, never asserted flat. Quantization: the affine map $q = \operatorname{clamp}(\operatorname{round}(x/s) + z,\ 0,\ 2^b - 1)$ with scale $s = (\beta - \alpha)/(2^b - 1)$ over a clipping range $[\alpha, \beta]$, a rounding error of at most $s/2$ inside the range and an unbounded one outside it, which is line 72's downside stated exactly, and a size reduction of $32/b$ against single precision. That size reduction meets an argument already derived in the vault, [[Scalability]]'s $P \cdot b \cdot k$ memory floor, restated in [[Distributed Training]] with $\beta$ for bytes; quantization is a change to $b$ and is linked there, never rederived. Batch against online prediction: a batch job run every $\Delta$ hours serves a prediction at most $\Delta$ old and $\Delta/2$ old on average under uniform arrivals, and it spends compute on every precomputed row whether or not anybody requests it, so the waste is one minus the fraction consumed; both belong wherever the choice is written. Network against inference latency at line 89 is a sum, end-to-end latency being network time plus queueing plus inference, and the network term has a physical floor, a round trip over fibre costing on the order of a millisecond per hundred kilometres, which is what makes the line a comparison rather than an assertion. The browser line at 95 is a measured ratio and has one: Jangda and colleagues measured WebAssembly running slower than native code by a mean of roughly 45 percent in Firefox and 55 percent in Chrome on SPEC CPU (USENIX ATC 2019), to be checked. And line 10's scale myth rests on a survey statistic that is a number, not a feeling. Two atoms are the opposite failure: latency and inference are already defined in passing in [[Production Machine Learning]], the latency of a single prediction as the interval between a request arriving and a result going back, and batch serving is already documented there at length, so those are linked and sharpened, never rederived; and the retraining-cadence claim behind line 8 is [[Continual Learning]]'s subject and chapter 9's, linked rather than restated. Two failures carry over from earlier runs: a discharge written where rung two was available, and padding written above a discharge where a genuinely discharged note should be short.]
- Primary sources, in order of trust: [the original papers behind a claim, reached through the chapter's own references since the book's resource list for this chapter is empty; then the current official documentation of any named library, framework or platform, read for what it does today; then the book's own text if the agent has it, including `summary.md`'s chapter 7 section, which is the author's own words and is the only first-party text on the web for this chapter. Compression survey: Cheng, Wang, Zhou and Zhang, "A Survey of Model Compression and Acceleration for Deep Neural Networks" (2017), which is the likeliest source of the four-way taxonomy at lines 43 to 46 and is checked against it; Gholami and colleagues, "A Survey of Quantization Methods for Efficient Neural Network Inference" (2021) for quantization. Low-rank factorization: Eckart and Young (*Psychometrika* 1936) for the approximation theorem; Denton and colleagues (NeurIPS 2014) and Jaderberg, Vedaldi and Zisserman (BMVC 2014) for low-rank weight approximation in convolutional networks; Iandola and colleagues, "SqueezeNet" (2016) and Howard and colleagues, "MobileNets" (2017) for compact filters, which settle what line 51 is describing. Knowledge distillation: Hinton, Vinyals and Dean, "Distilling the Knowledge in a Neural Network" (2015) for the method and the temperature; Buciluă, Caruana and Niculescu-Mizil, "Model Compression" (KDD 2006) as the earlier form, which is the source to check before the note says who introduced the idea; Sanh and colleagues, "DistilBERT" (2019) for the standard worked instance and its measured size, speed and retained-quality figures. Pruning: Breiman, Friedman, Olshen and Stone, *Classification and Regression Trees* (1984) for cost-complexity pruning, which settles the decision-tree origin at line 59, and Quinlan's C4.5 (1993) beside it; LeCun, Denker and Solla, "Optimal Brain Damage" (NeurIPS 1989, published 1990) for pruning in networks; Han, Pool, Tran and Dally (NeurIPS 2015) and Han, Mao and Dally, "Deep Compression" (ICLR 2016) for magnitude pruning at scale; Frankle and Carbin, "The Lottery Ticket Hypothesis" (ICLR 2019) and Liu and colleagues, "Rethinking the Value of Network Pruning" (ICLR 2019), which disagree about what pruning finds and are both cited if either is; Blalock, Gonzalez Ortiz, Frankle and Guttag, "What Is the State of Neural Network Pruning?" (MLSys 2020) for how weakly the field's results compare; and Hooker and colleagues, "What Do Compressed Deep Neural Networks Forget?" (2019), which is the primary source for line 61's claim that pruning can introduce bias and is the single most load-bearing citation on that block. Quantization: Jacob and colleagues (CVPR 2018) for integer-arithmetic-only inference, which is the fixed-point claim at line 76; Micikevicius and colleagues, "Mixed Precision Training" (ICLR 2018) and Courbariaux, Bengio and David, "BinaryConnect" (NeurIPS 2015) for training in low precision, which line 73 names; Kalamkar and colleagues (2019) for bfloat16 and Micikevicius and colleagues, "FP8 Formats for Deep Learning" (2022) for what "support from modern training hardware" at line 75 means now rather than when the book went to press. Batch and streaming: Dean and Ghemawat, "MapReduce" (OSDI 2004) and Zaharia and colleagues (NSDI 2012) for Spark's resilient distributed datasets, both read for whether line 37's "batch systems like MapReduce and Spark" is still fair to Spark, which has had a streaming engine since 2013 and Structured Streaming since 2016; Marz for the Lambda architecture and Kreps, "Questioning the Lambda Architecture" (O'Reilly, 2014) for Kappa, which is the canonical statement of line 38's two-pipeline problem and its proposed unification; Akidau and colleagues, "The Dataflow Model" (VLDB 2015) as the research answer to it. Latency: Dean and Barroso, "The Tail at Scale" (*CACM* 2013), already cited in the vault, for why a tail percentile rather than a mean is the number a caller feels. Cloud and edge: Shi, Cao, Zhang, Li and Xu, "Edge Computing: Vision and Challenges" (*IEEE Internet of Things Journal* 2016) for the term; the GDPR's own text (Regulation (EU) 2016/679) and not a vendor page for line 91's compliance claim, which is a legal claim to state carefully or attribute; and McMahan and colleagues (AISTATS 2017) for federated learning only if a packet reaches for it, since the raw file does not. Browser: Haas and colleagues, "Bringing the Web up to Speed with WebAssembly" (PLDI 2017) for what WebAssembly is, Jangda, Powers, Berger and Guha, "Not So Fast" (USENIX ATC 2019) for line 95's speed claim, and the W3C and browser vendors' own pages for WebGPU and WebNN, which postdate the book and change what "slow in browsers" means. Myths: the book cites figures for deployment frequency and model counts at named companies, and the scale myth's statistic is, by its likely source, the 2019 Stack Overflow Developer Survey, which surveyed developers in general and not machine learning engineers, so line 10's "most ML related jobs" is a research item and very likely a correction. Frameworks at line 77: LiteRT's and TensorFlow Lite's own pages for the rename, PyTorch's own pages for whether PyTorch Mobile is still supported or has been superseded by ExecuTorch, and NVIDIA's TensorRT documentation for what hardware it targets, since TensorRT is a GPU inference SDK and calling it on-device is true only of NVIDIA's embedded boards. The 2022 staleness rider from all six previous runs stands and bites harder here than in any chapter so far, in four places: the framework list at line 77, the hardware support claim at line 75, the batch-only characterization of Spark at line 37, and the author's own prediction in `summary.md` that systems will move to online prediction on-device as hardware improves, which is a forecast and is dated as one if it is used. A vendor blog post is not a primary source for a claim about a competitor, a library's own documentation is a primary source for its API and not for whether its method works, and Wikipedia is not a primary source for anything and may be used only to find one, which matters here because line 6's gloss of software rot, "the phenomenon in which software programs degrade over time even if nothing seems to change", reads like a pasted definition and is checked as a suspected paste.]
- Exemplar notes, read in Phase 2, one per shape this run will produce: [`Knowledge/Machine Learning/Operations/MLOps.md`, the single most important file to read before anything is planned, read in full and read first. It is the index for the folder this chapter finally grows, its scope paragraph is the membership test every placement decision below is checked against, and it already says in writing where the hosting vocabulary lives and that what belongs in `Operations/` is "the model-specific half, what serving a fitted model asks that serving an ordinary service does not", which is the sentence the cloud and edge block is placed by. `Knowledge/Machine Learning/Operations/Production Machine Learning.md`, read in full, because it is the note this chapter's batch and online block collides with head on: it already defines inference and single-prediction latency, and it already documents batch serving across three cloud platforms, three model providers and MLPerf, so the packet that writes batch against online prediction knows exactly what it must not rewrite. `Knowledge/Machine Learning/Paradigms/Training Regime/Batch Learning.md` and `Online Learning.md`, read for their definitions, because they own the words `batch` and `online` on the training axis and this chapter uses both words on the serving axis. `Knowledge/Machine Learning/_Foundations/Feature.md`, read for its static and dynamic feature paragraph, which is the vocabulary line 20's batch and streaming features restate and which carries this chapter's promise in its own prose. `Knowledge/Machine Learning/Data/Preprocessing/Feature Distribution Transformation.md`, read for its aliases and its bucketizing section, because it claims `quantization` and `quantize` in the binning sense, which is the sharpest alias collision in the run. `Knowledge/Machine Learning/Optimization/Distributed Training.md`, the `concept` exemplar from the previous run and the most recent note to carry a memory formula it inherited from another domain rather than rederiving, which is exactly the move quantization has to make against [[Scalability]]. `Knowledge/Machine Learning/Evaluation/Error Analysis.md`, the `method` exemplar whose `## Hyperparameters` is discharged with "None." rather than a table, since distillation and pruning have real hyperparameters and low-rank factorization and quantization have some, and the packet decides per technique rather than by default. `Knowledge/Machine Learning/Models/Ensembles/Bagging.md`, the most recent `method` exemplar with a full hyperparameter table and an `## Implementation` that names exact 1.6 APIs, for shape and density. `Knowledge/Systems Architecture/Hosting/Cloud Computing.md` and `Knowledge/Systems Architecture/Requirements/Scalability.md`, read as the two neighbouring-domain notes this chapter must link rather than duplicate. `Knowledge/Machine Learning/Machine Learning.md`, the domain hub, for the Areas paragraphs written as continuous argument rather than as lists, and because it is the largest amendment in the run. Read every one of these. Several are files this run will amend, so the rule is sharper here rather than looser: what is copied is the shape, the density and the voice, never the content, and an exemplar that is also a target is read in full before a word of the amendment is written.]
- Exemplar chapter note: [two, and for different reasons. `Sources/Books/DMLS/DMLS Ch06 Model Development and Offline Evaluation.md` is the previous chapter of this same book and the most recent finished chapter note, so it is read for shape: the `## Extracted` groups introduced by a theme sentence rather than a bare list, the group for notes amended rather than produced, the group for hubs and indexes revised, the "Notes refused with their reasons" entry, and the "Settled here, having been asked earlier" block that step 3e asks for. It is read for content as well, because its `## Open questions` record the latency and throughput debt for the sixth time with an exact line count this run continues rather than restates, report `Operations/` at four notes and "not reopened, for the sixth time", and decline data distribution shift forward to chapter 8, which this raw file's line 6 touches. `Sources/Books/DMLS/DMLS Ch01 Overview of Machine Learning Systems.md` is read for three entries rather than for shape, and it is the more important of the two for planning: line 53 says "inference" is owned by nothing, is "owed to DMLS chapter 7, 'Model Deployment and Prediction Service'", and states in advance that whatever note arrives "has to separate the statistics homonym in the same breath"; line 58 says deployment targets, monitoring and alerting, and rollback are absent "and DMLS chapters 7 and 8 are what owe them"; and line 59 says "the deployment mechanics are still missing, and DMLS chapter 7 now owes them alongside [[HOML]] chapter 19". Those are the most specific promises any earlier chapter has made about this one.]
- Neighbouring domains: [three, and the expected traffic is the heaviest of any run. `Knowledge/Systems Architecture/` has three real claims. `Hosting/` owns [[Cloud Computing]], which claims `cloud`, `cloud computing`, `public cloud` and `managed service`, so line 80's "cloud" is a link and not a definition, and edge computing, which no note owns and no alias names, is generic hosting vocabulary, a place computation runs, before it is anything about models; decide whether it is a new note in `Hosting/`, beside [[Cloud Computing]] and [[Self-Hosting]], with the model-specific half, on-device inference, kept in this domain, or whether the one chapter of evidence earns only a section, and argue it in the plan with the cost named, since a note filed in another domain from an ML chapter is the cross-domain rule being used rather than broken, but it has to be argued. `Distribution/` owns [[Modes of Data Flow]] and [[Request-Driven Communication]], which is where line 17's "Data Passing Through Services" and its synchronous HTTP request already live, and [[Event-Driven Communication]], which claims `real-time transport`, the exact phrase line 34 uses. `Requirements/` owns [[Scalability]], whose $P \cdot b \cdot k$ floor is the quantity quantization shrinks, and [[Maintainability]], which is the nearest owner of software rot, a property of any program, if line 6's first half becomes anything more than a clause. So the boundary is drawable and the plan draws it: that domain owns where computation runs, how processes talk, and what any system must provide, and this domain owns what takes a fitted model to state, the weights, their precision, the forward pass and the prediction. `Knowledge/Data Systems/` has a real claim for the first time since chapter 3: [[Batch Processing]] and [[Stream Processing]] own the two processing modes that batch prediction and streaming features run on, and line 37's MapReduce and Spark are that domain's vocabulary; MapReduce is named nowhere in the vault today and Spark in eight notes, so the packet reads both notes before writing a sentence about either and links rather than restates. `Knowledge/Mathematics/` has a colourable claim through low-rank approximation and the SVD, which is a fact about matrices and not about models; the SVD is already used in six machine learning notes, [[Linear Regression]], [[Ridge Regression]], [[Normal Equation]], [[Batch Gradient Descent]], [[Embedding]] and [[Feature Scaling]], and owned by none, so the case is stronger than the previous run's Simpson's paradox case was. The presumption is still against opening a fourth family in that hub on one run's evidence, but if the decision goes the other way it is argued in the plan with the six existing users named as the evidence, not taken inside a packet.]
- Known collisions, to be resolved in the plan under the collision rule before any packet is written, and not an exhaustive list: ["Quantization", against [[Feature Distribution Transformation]], a note in `Data/Preprocessing/` claiming `quantization` and `quantize` as aliases in the sense of bucketizing a continuous feature, plus the same verb used for turning a regression target into classes in [[Regression]], [[Classification]] and the hub's `Tasks/` bullet. DIFFERENT THINGS without any doubt, since one maps a feature's values to bucket indices before fitting and the other maps a model's weights and activations to fewer bits after or during fitting, and the shared mathematics, rounding to a grid, is exactly what makes the collision dangerous. Under the alias rule the bare word is ambiguous and is therefore not an alias for anybody: it is qualified in both places or dropped from both, which means this run edits an existing note's alias list, and whatever qualified form each side takes, `model quantization` and `feature quantization` or better, is checked against the rest of the vault first. Both `## Definition` sections carry the distinguishing sentence and the link. "Batch prediction" and "online prediction", claimed by nobody, against [[Batch Learning]], which claims `offline learning` and `batch training`, and [[Online Learning]], which claims `incremental learning`, `streaming learning` and `mini-batch learning`. DIFFERENT THINGS on two independent axes, one about when the model is fitted and one about when it is asked, and a system can be any of the four combinations, which is the sentence each side carries; neither side may claim bare `batch`, `online` or `offline`, and `streaming prediction` must be checked against `streaming learning` rather than eyeballed. The same pair also collides with [[Production Machine Learning]], which already documents batch serving, so decide whether batch against online prediction is a new note that the setting note points at, or a section of that note, with the answer depending on whether the new material is a distinct sub-topic or the same one said again. "Asynchronous prediction" and "synchronous prediction", claimed by nobody, but [[Data Parallelism]] claims `synchronous SGD` and `asynchronous SGD`, and [[Production Machine Learning]] names SageMaker's "asynchronous inference", which is a queued online endpoint and not batch prediction at all, so the book's use of `asynchronous prediction` as a synonym for batch prediction is checked rather than copied, and if it is kept as an alias the note says which sense it means. "Batch features" and "streaming features", against [[Feature]], whose prose defines a static feature computed by a scheduled job with [[Batch Processing]] and a streaming feature "also called a dynamic feature" with [[Stream Processing]], carrying no alias for any of the four. SAME THING, two names each, so the question is not the verdict but whether the pair now earns a note of its own, which the promise below turns on, and if it does not, the terms become aliases on whichever note keeps them. "Unifying batch and streaming pipelines" and the bug it prevents, against [[Data Mismatch]], which claims `train-serving skew`. The two-pipeline bug at line 38 is the same feature computed two different ways, which is one cause of train-serving skew rather than a distribution difference, so decide whether [[Data Mismatch]]'s claim on that alias is right, or whether skew and mismatch are DIFFERENT THINGS that the vault has been filing under one note since the HOML run; [[Feature Generalization]] also uses the phrase. "Model compression", "low-rank factorization", "knowledge distillation", "pruning" and "quantization", the largest unowned block in the chapter and claimed by nobody apart from the quantization collision above. Decide whether this is one note or a family of five, and where the family lives, which is a boundary question below. `distillation` appears once in the vault, in [[Data Augmentation]]'s paragraph on generated text, where it names this chapter's technique in passing, so that note gains a link. `pruning` appears in three notes in three senses, candidate pruning in [[Association Rule Learning]]'s Apriori, pruned decision trees in [[Baseline Model]]'s account of Holte's comparison with C4, and once in [[Triangle Inequality]], so the bare word is not claimable and is qualified; decision tree pruning is also [[HOML]] chapter 6's material, which is the forward collision below. `low-rank` appears nowhere, and `factorization` twice in unrelated senses. `sparse` appears in seventeen notes, most of them about sparse matrices in scikit-learn or sparse solutions under L1, and [[Lasso Regression]]'s sparsity is weights driven to zero by a penalty, which is the same end state as magnitude pruning reached by a different road, worth one linking sentence and not a merge. "Teacher" and "student" are unclaimed and stay so, as bare words. "Inference", against nobody by alias, and it is the promise below: [[Production Machine Learning]] defines it in passing as the forward pass turning one input into one prediction, ten notes use the word, and the statistics homonym, estimating parameters from data, is live in the vault's own vocabulary of fitting. So bare `inference` is ambiguous and is not an alias for anybody; decide who owns the serving sense under a qualified name, and whether that owner is [[Production Machine Learning]] or a new note. "Latency", against [[Production Machine Learning]], which defines single-prediction latency, and against the latency vocabulary [[DDIA]] chapter 2 is expected to bring into `Knowledge/Systems Architecture/`; `inference latency` and `network latency` are both used at line 89 and claimed by nobody. "Cloud", "edge", "edge device" and "on-device", against [[Cloud Computing]] for the first; `edge` appears in 47 notes, almost all as a graph edge, a decision-boundary edge or an edge case, so it collides in search and not in meaning, and the bare word is never an alias. "Software rot" and "bit rot", claimed by nobody, against [[Model Rot]], which claims `model drift`, `concept drift`, `data drift`, `model decay` and `performance decay`. DIFFERENT THINGS: software rot is a program degrading while its code is unchanged because its environment moved, and model rot is a model degrading because the data moved, which is the distinction line 6's arrow blurs, and [[Model Rot]] gains the sentence rather than the alias. "Data distribution shift", against [[Data Mismatch]], which claims `distribution shift` and `dataset shift`, and [[Model Rot]]; this chapter names it in one clause and chapter 8 is titled for it, so it is linked and declined. Named products, frameworks and platforms, of which only Spark appears in the vault today: MapReduce, Spark, TensorFlow Lite, PyTorch Mobile, TensorRT, WebAssembly and JavaScript, plus the book's worked examples, Netflix's four-hourly recommendations at line 19 and the delivery-time features at lines 24 and 25. Products do not earn notes under the rules already in force; they are named inside the note describing what they implement and dated wherever a claim about them is a claim about a market, and line 77's three names are each checked for whether they still exist under that name. Forward collisions to record and decline, each with the chapter that triggers it: data distribution shift and monitoring, to [[DMLS]] chapter 8, which also still owes [[Model Rot]] its drift vocabulary as a treatment rather than as aliases; update frequency, the how-often-can-you-update question at line 8, and test in production, to [[DMLS]] chapter 9; feature stores, model registries and the cloud-cost repatriation argument, to [[DMLS]] chapter 10, which the book's own summary for that chapter confirms; decision tree pruning as a book that teaches trees teaches it, to [[HOML]] chapter 6, which is also the chapter [[Node]] has been waiting on for four runs; and deployment mechanics in a specific framework, TensorFlow Serving, LiteRT and TensorFlow.js, to [[HOML]] chapter 19, which [[DMLS Ch01 Overview of Machine Learning Systems]] already names alongside this chapter.]
- Domain boundary questions this run must answer: [Six, and the first two are the ones the plan leans on. First, where model compression lives. The candidates and their costs: `Operations/`, which fits the motive, since compression exists only because a model has to be served somewhere cheaper or faster, and fits [[MLOps]]'s written boundary without an edit; `Optimization/`, which fits the mechanism for distillation and quantization-aware training but whose hub bullet describes the folder as the search through [[Parameter Space]] for the point minimizing a cost, and post-training quantization searches nothing; `Models/`, which holds algorithm families and compression is not one; or a new folder, which the folder rule permits only if this run actually produces the notes to fill it. An umbrella plus four techniques is five notes, which is closer to `Labeling/`'s six than `Development/`'s declined two, so answer it with the count in hand, say whether the umbrella is a `concept` or an `index` under the rule that an index is earned by members to list, and remember that a folder created here would be the first new folder since `Labeling/` and the first ever under `Operations/`. Second, whether batch against online prediction is one note, two notes, or an amendment to [[Production Machine Learning]], and whether streaming prediction is a third member or a special case of online prediction, which is what the raw file's own three-mode list at lines 12 to 15 implies and what line 14's "online prediction that uses only batch features" makes awkward to name. Third, where edge computing lives, with the cross-domain argument in the neighbouring-domains parameter taken to a decision and the model-specific half kept here. Fourth, whether the inference promise is kept by a note of its own or by sharpening [[Production Machine Learning]], and in either case which qualified name carries the serving sense. Fifth, whether [[Feature]]'s static and dynamic paragraph is enough for this chapter's batch and streaming features, or whether the pair and the pipeline agreement now earn a note, remembering that the book's own treatment of the store that serves both is chapter 10's. Sixth, and reported rather than reopened: `Machine Learning/Operations/` holds four notes and the standing position from [[DMLS Ch01 Overview of Machine Learning Systems]] is that nothing moves, with two triggers, the folder passing 25 notes or [[DMLS]] chapter 10 arriving; neither will fire this run, but this is the first run that will grow the folder at all, so report the count before and after, and if the folder doubles, say so plainly rather than as a triumph. Report also the sizes the previous run left open, `Evaluation/` at seventeen notes at its top level and `Optimization/` at fourteen, and whether this run touched either. And report, for the seventh consecutive chapter, what this chapter's raw file contains on latency and throughput, because the answer has changed again and the record is easy to get wrong: `latency` appears four times on three lines, at line 32 as the "latency challenges" of online prediction, at line 70 as `inference latency`, and twice at line 89, as `network latency` against `inference latency` with the claim that the first is often the larger bottleneck, which is the first time any raw file has compared two latencies rather than naming one; `throughput` does not appear at all. That is stronger than chapter 6's two qualitative mentions and still not a definition, so the debt is not discharged by the raw file. It may be discharged by research this time, since scikit-learn's own computational performance page defines both, and [[Production Machine Learning]] already defines single-prediction latency; decide in the plan whether that counts, and if it does, the record in the chapter note says the debt was paid by documentation rather than by [[DDIA]] chapter 2, which is where every previous record expected it.]
- Promises this run must keep or correct, before anything else is decided: [five, and every one of them names this chapter by number in writing. First, [[DMLS Ch01 Overview of Machine Learning Systems]] at line 53 says "inference" is owned by nothing, is "owed to DMLS chapter 7", and that the note which arrives "has to separate the statistics homonym in the same breath". The raw file uses the word at lines 41, 70, 73, 76, 77 and 89 without defining it once, so this promise is kept by a decision, not by transcription. Second, the static and dynamic feature promise, carried in four places: [[Feature]] says "[[DMLS]] chapter 7, on deployment and the prediction service, is where this belongs at length, both how a running system computes each kind and why the two pipelines are made to agree on the value they produce"; [[DMLS Ch03 Data Engineering Fundamentals]] at line 54, [[DMLS Ch04 Training Data]] at line 79 and [[DMLS Ch05 Feature Engineering]] at line 84 all say the same thing. The raw file half keeps it: lines 20 to 25 define batch and streaming features with an example, and lines 36 to 38 say the two pipelines are a common source of bugs and that unifying them is an open problem, but say nothing about how a running system makes them agree. So decide whether the half kept is enough to call the promise kept, researched, or rewritten to name chapter 10's feature store for the other half, and edit all four notes in the same sitting so they say the same true thing afterwards; this is the same shape as the static-and-dynamic promise the ch05 run had to rewrite, and that run's wording is the precedent. Third, [[DMLS Ch01 Overview of Machine Learning Systems]] at line 58 says deployment targets, monitoring and alerting, and rollback are absent "and DMLS chapters 7 and 8 are what owe them". This chapter delivers deployment targets, batch against online and cloud against edge, and nothing on monitoring, alerting or rollback, so the line is rewritten to say that one of three arrived and to name chapter 8 for monitoring and alerting and the chapter that actually treats rollback, settled against the book's table of contents rather than guessed. Fourth, the same note at line 59 says "the deployment mechanics are still missing, and DMLS chapter 7 now owes them alongside [[HOML]] chapter 19"; this run keeps the DMLS half and leaves the HOML half standing, and the line says which. Fifth, [[DMLS Ch06 Model Development and Offline Evaluation]]'s open questions report `Operations/` as "not reopened, for the sixth time" and record the latency and throughput debt with an exact count; neither is a promise to this chapter by name, but both are records this run continues in its own chapter note rather than edits in that one, so the ch06 note is not touched unless something it says has become false. A promise this run cannot keep is rewritten, never left to rot, and every note carrying it is edited in the same sitting so that all of them say the same true thing afterwards.]
- Working tree at the start of the run: [clean, apart from one untracked directory. `git status --short` reports exactly one line before this run writes anything, `?? "_Slip Box (Zettelkasten)/"`, which is the slip box itself and is expected. The previous run's work is committed in full at f1ea3ef. Anything else appearing in that output at Phase 5 is this run's own doing, and it is either a file the plan named or a mistake to reverse by hand and never with a git command.]

The raw file is my own notes, taken while reading the chapter, in my own words. It is the source of truth for what I understood. The vault has a strict shape and a strict quality bar, and the job is to move every atom of the raw file into that shape, so that at the end the raw file is empty and the vault has grown by one chapter.

Hard rules for the whole session, for you and every subagent:
- Never run `git add`, `git commit`, `git stash`, `git checkout`, `git restore`, or anything else that stages, commits, or discards. `git status`, `git diff`, `git log` and `git show` are allowed, read only.
- Never delete the raw file. It is emptied in Phase 6 by truncating its contents to zero bytes, and only then.
- Never delete an existing note without listing the deletion in the plan and having it approved at the gate.
- Every file is owned by exactly one subagent per phase. Two subagents never write the same file.
- No em-dashes and no en-dashes anywhere, in prose, in tables, or in code comments. Use commas, colons, parentheses, or a new sentence. Normalize any the raw file contains.
- Never write "e.g." Write "ex" or restructure the sentence.
- Every note body is a restatement in the vault owner's voice. The raw file's wording is preferred wherever it already says the thing. Book phrasing is never copied. Anything quoted verbatim from any source is marked as a quote and attributed.
- The preference for the raw file's wording holds only where the raw file is actually in my voice. A raw bullet written in a register the rest of the file does not use, typically a long fluent sentence with marketing adjectives where the surrounding bullets are terse fragments, is very likely pasted from a glossary or a vendor page and must be restated from scratch rather than preserved. Flag every such bullet in the Phase 4 report as a suspected paste, with the raw wording, so I can see which of my own notes were not mine.
- A heading that names a source is a defect, without exception: no `### From Chapter 1`, no `### What the book says`, no `### Kleppmann's version`. New material is merged into the section it belongs in by topic, and any subsection is named for what it contains. "The chapter" and "the book" are never bare subjects in the prose of a note under `Knowledge/`; name the source with a link or state the fact without attribution. Attribution lives in `sources:` and in links.
- A note belongs to exactly one domain. Where this chapter's material overlaps a neighbouring domain, the rule is link, never duplicate: if a note already exists there, amend it and link to it, and do not restate its content in this domain. Where the overlap is real but the existing note says something different rather than something less, that is a research item and a correction, not a second note.
- A word that already exists in the vault is a collision until proved otherwise. Before creating any note whose title, or any alias you would put on it, matches an existing note's title or alias, read that existing note in full and return one of exactly three verdicts, recorded in the plan with the reason:
  - SAME THING. Two names for one concept. One note survives, in the domain that owns the concept, the other name becomes an alias on it, and the second domain links to it and restates nothing.
  - DIFFERENT THINGS. Two notes. Each one's `## Definition` names the other and says in one sentence what separates them, with a link, so a reader who arrived at the wrong note leaves immediately rather than reading the wrong thing carefully. Neither note may claim the bare ambiguous word as an alias.
  - SPECIAL CASE. One is the other applied to a narrower setting. The general note lives in the domain that owns the general case; the specific note points `up` at it, or links to it with a phrase naming exactly what it specializes, and does not repeat the general note's definition.
  The failure this prevents is not a merge conflict, it is silence: two notes describing different things under the same word, neither aware of the other, both looking correct in isolation.
- Aliases are globally unique across the vault. Obsidian resolves a link by name, so two notes claiming one alias makes one of them unreachable under that name with no warning and no error. Before any packet is dispatched, check every seed alias in every packet against every title and alias already in the vault, and against the other packets. A word that is ambiguous on its own is not an alias for anybody: it is qualified in both places or dropped from both alias lists.

Work in six phases. Do not skip a phase and do not start writing files before the gate.

PHASE 1: STRUCTURE (subagents, in parallel, read only).
Launch two read-only exploration subagents at once and wait for both.

Subagent A, the vault contract. Have it read in full and report back, quoting rather than paraphrasing wherever a rule is exact: `__Meta/Quality-Bar.md` (the whole file, it is the spec), `README.md`, `Home.md`, `Notation.md`, `__Meta/Callouts.md` (only the list of callout types), every file in `__Meta/templates/`, and `.obsidian/types.json`. Its report must contain: the frontmatter contract per note_kind, the required section headings per note_kind verbatim, the three rungs of `## Formal statement`, the fixed discharge string for the bottom rung and where in the section that string goes, the hyperparameter test, the disqualification list, the heading rules (no H1 under `Knowledge/` and none on a chapter note, H1 on a container note and on any document read front to back, `##` for required sections, `###` for subsections), the rule against headings that name a source, whether a Bases query block is part of the required shape of an `index`, whether the list heading of an index is fixed or named for what it lists, and whether a forward-looking list of material not yet written is part of an index's shape or a defect in it, the discharge available to `## Implementation` in a domain with no pinned library, and what the file says about adding a new note_kind and when that question reopens.

Subagent B, the vault as it stands. Have it produce: the full tree of `Knowledge/` and `Sources/` with one line per file giving path, note_kind, `up`, and `confidence`; the complete text of every domain hub under `Knowledge/`, meaning the `index` note whose `up` is `[[Home]]`, including each one's scope statement and its Areas list in full; the complete text of the exemplar chapter note named in the parameters; the output of `git log --oneline -n 12`, and then `git show --stat` on the most recent commit that integrated a chapter, as the exemplar of what a chapter integration touches (new notes, the hub, Home, the container note, and backfilled `sources:` and `up:` on existing notes); and, when the domain status is FIRST RUN, additionally `git show --stat 5a1a0c2`, the commit that created the `Mathematics` and `MLOps` domains, as the exemplar of what starting a domain touches. Also have it report every line anywhere in `Knowledge/`, `Sources/`, `Home.md` and `README.md` that mentions the chapter being integrated, the book being integrated, or the subject domain by name, because each one is a promise this run must keep or correct; every `[[wikilink]]` target in the raw file that does not resolve to an existing note; and every existing note whose title or `aliases` collide with a term in the raw file, which is the input to the collision rule above. Finally, have it report which existing notes end with a Bases query block and which do not, so new notes can follow the pattern of their neighbours rather than invent one.

Do not proceed until both reports are in. Keep both reports in your context for the rest of the session.

PHASE 2: READ THE RAW NOTES (you, personally, in full).
Read the raw file from the first line to the last with no offset and no limit. If the file is long, read it in consecutive chunks until you have seen the final line, and say the final line back to yourself so you know you reached it. Do not delegate this. Do not summarize it yet.

Then read, yourself, every exemplar note named in the parameters, and the exemplar chapter note, so you know what a finished note looks like in each shape you will be producing.

PHASE 3: INVENTORY AND PLAN (you, personally).
Build the plan in this order and write it to a scratch file outside the vault, not into the vault.

Step 3a, the inventory. Split the raw file into atoms. An atom is the smallest piece that lands in one place: usually one heading with its bullets, sometimes a single bullet, a single table, or a single code block when its siblings go elsewhere. Number the atoms in file order. Every line of the raw file belongs to exactly one atom. Tables, figures and code blocks that only reproduce an exhibit from the source are still atoms; they are allowed to be dropped, but only by an explicit decision with a reason.

Three shapes of atom need naming before they are missed. A heading with nothing under it is an atom, and its disposition is a decision: either the chapter covered it and I did not write it down, in which case the plan says so and the material is researched or deferred, or it is a stub for a later reading session, in which case it is dropped with that reason. A term listed with an empty definition is an atom, and the first place to look for its definition is later in the same raw file, where a term glossed in one line at the top is often developed in full under its own heading further down; when nothing in the raw file defines it, the note is still owed a definition, which comes from research and is reported as an addition rather than quietly written as though I had said it. A bullet whose content is a bare cross reference, with no claim of its own, is an atom whose disposition is a link rather than a note.

Step 3b, the destination table. One row per atom: atom number, raw heading, action, target path, note_kind, `up`, what this atom contributes, and why here. Actions are exactly one of: CREATE a new note, AMEND an existing note (say which section), RENAME or MOVE an existing note (old path to new path), DELETE an existing note (with the reason and where its content goes), DROP the atom (with the reason). A raw atom may feed several rows, since one passage often carries both the definition of a thing and a claim about a different thing it is being contrasted with. Every unresolved `[[link]]` from the Phase 1 report must appear as a CREATE row or the plan is incomplete.

Step 3c, the structural decisions. Decide and write down, with the reason, every placement question the chapter raises, checked against the Areas list of the domain hub and against the folder rule (the folder says what kind of thing a note is, `up` says what it is part of). Do not add a new note_kind. Every decision here is a decision about where things go and what earns a note, never a decision about what is true.

Step 0 of this step applies only when the domain status is FIRST RUN, and when it applies nothing else in the plan is settled until it is. Read the domain hub named in the exemplars first, because it is the precedent: it was started from a single chapter of evidence, it opens with a paragraph saying what earns a place in the domain and what does not, and it has an Areas list whose entries are folders with the notes in them named in prose. Read it against what Subagent A reports about the index shape rather than copying it wholesale, since a hub written earlier may carry a section the contract has since dropped. Then decide and write down, with the reason:

- The domain's name and folder, and the hub note's path, which is the folder name repeated as the filename, matching `Machine Learning/Machine Learning.md`.
- The membership rule, one paragraph: what earns a place in this domain, and what looks like it belongs but goes to a neighbouring domain instead. Write this as that hub writes it, as a boundary against the neighbours rather than as a description of the subject. This paragraph is the thing that stops the next five chapters from being filed by accident, so it is worth more effort than anything else in this step.
- The folder taxonomy, with the evidence for each folder drawn from the atoms in the inventory and named atom by atom. One chapter is thin evidence, so the default is few folders. A folder is earned by holding notes this run actually produces, not by naming a topic the book will reach later; a topic with one note and a promise of more is a note at the top level of the domain, and the folder arrives with the second note. State plainly which folders are being created on one note of evidence and why that one note could not sit at the top level instead.
- Which atoms are not domain material at all. A chapter that opens a subject typically carries organizational material, meaning who does which job and what the teams are called, and it carries vendor and product names. Decide whether each of those is a note, a line inside another note, or a drop, and if the organizational material is a note, decide whether this domain or a neighbouring one owns it.
- The hub note itself: `note_kind: index`, `up: "[[Home]]"`, no H1, a scope paragraph, `## Areas` rather than `## Methods`, and a Bases query block, which every index carries. Whether it also carries a list of material not yet written is decided by what Subagent A reports, not by what an older hub happens to contain: an index that states what is on disk is honest about a domain one chapter old by being short, and a list of predicted arrivals is a claim made on the weakest evidence there is.
- The line in `Home.md` under `## Domains` that introduces the domain, in the shape of the lines already there, and whether `README.md`'s layout table needs the domain named too.

The rest of step 3c applies to every run. At minimum decide: which atoms become notes at all, given that a term the chapter merely names in passing is a line in another note rather than a note of its own, and say which terms you are refusing a note to and why; for each note, whether it is a `concept`, a `method` or an `index`, checked against the concept and method test in section 2 of the quality bar (a method is something you carry out as itself and that changes something downstream, or an ordered protocol whose reading depends on the order; everything else is a concept) and against the rule that `index` is earned by having members to list and that a topic with none is a `concept` carrying its own definition; whether any existing note should convert between kinds under that same rule; which notes are paired contrasts that want the optional `## VS` section rather than two notes each describing the other; which atoms touch a neighbouring domain, and for each one whether the existing note there is amended and linked or whether this domain owns a genuinely different thing under a similar name; and whether a dataset note is required, given that the quality bar wants one per dataset a chapter works on. Where a decision contradicts a promise an existing note made, say which promise is kept and which is corrected, and never leave both standing.

Also produce, as its own table, the collision ledger: one row per term in this chapter that matches an existing note's title or alias, whether the parameters named it or Subagent B found it, giving the term, the existing note, the new note or amendment, the verdict of SAME THING, DIFFERENT THINGS or SPECIAL CASE, and the exact sentence each side will carry to tell itself apart from the other. A term with no row is a term nobody checked.

Do not reach for a new note_kind. The quality bar says when that question reopens, and the answer is never "during an integration run". A note that fits no existing kind comfortably is written to the kind it fits least badly and the strain is recorded in the final report and in the chapter note's `## Open questions`, which is the evidence a kind is eventually argued from.

One decision is larger than placement and gets its own line in the plan when it arises: whether this run's material argues for restructuring an existing domain rather than only adding to one. A domain that turns out to be a special case of something this chapter introduces is a real finding and is allowed to move. It is proposed at the gate with its cost named in full: every file that moves, every `up` that changes, and every sentence in `Home.md`, `README.md` and the other hubs that names the old path in words, since wikilinks survive a folder move and prose does not. Never restructure silently and never as a side effect of a packet. Where the cost is small today and grows with every chapter, say so plainly, because that is an argument for doing it now rather than an argument against doing it at all.

Step 3d, the promises. For every line the Phase 1 report found that mentions this chapter, this book, or this subject, write the exact edit that retires the promise. At minimum this covers: the container note, whose chapter line becomes a link, or, where the link is already there, whose link must now resolve to a written note; `Home.md`, whose `## Sources` list records the book and whose `## Areas` list records the domain; `README.md`, wherever it describes what lives under each path or counts what the vault holds; the domain hub's Areas paragraphs, which name the notes in each folder in prose and so go stale the moment a folder gains one; and every note in a neighbouring domain that names this subject or promised this material. Add any others the report found. A promise that this run cannot keep is rewritten to say what is actually true, not left to rot.

Step 3e, the chapter note. Plan the chapter note at the path in the parameters, from `__Meta/templates/Note Kinds/Source (Chapter).md`: frontmatter with `note_kind: source`, `medium: book`, `chapter:` as the number from the parameters, `up:` as a link to the container note, `url:` from the parameters, and aliases for the chapter number and the chapter title. Then `## Thesis`, `## Extracted` grouped by theme with a group for notes amended rather than produced and a group for hubs and indexes revised, and `## Open questions`. It takes no H1. Also plan which open questions in earlier chapter notes, of this book and of any other, this chapter settles or partly settles, and what edit records that.

Step 3f, the research list. For every claim in the raw file that is uncertain, marked with a question mark, internally inconsistent, or likely wrong, write a research item: the claim, what to check, and which primary source settles it, using the ordering in the parameters. Do the same for every named system, product, service, protocol or standard in the raw file: what it is, who makes it, whether it still exists under that name, and whether the property the raw file attributes to it is true of it today rather than at the time some older source described it. A product list in the raw file is a research item in its own right, because product lists go stale faster than anything else in a note, and a list that was right when the book went to press may not be right now. Where the raw file carries formulas, every one gets rederived or checked against a reference; where it carries none, this clause is discharged and an arrow or an inequality used as shorthand in prose is not a formula.

Step 3g, the work packets. Group the destination rows into packets such that no file appears in two packets, and packets are independent of one another. Then fix, before any packet is dispatched, the final title of every note that will be created or renamed, because every packet links to notes owned by other packets and the link text must already be right. Each packet contains: the atom text verbatim, the target file or files and the action on each, the note_kind and its exact required sections from the Phase 1 report, `up`, a seed list of aliases (every name a person would type in a link, including plurals, abbreviations, the expansion of every acronym and the acronym of every expansion, and the name any widely used system gives the thing), the outbound links this note must make and the phrase that explains each relationship, the notes elsewhere that will link back to it, the research items that belong to it, the exemplar note to read first, and the style rules from the hard rules above. Every packet also carries: "Do not touch any file not named in this packet. Do not run git write commands. Report back before finishing."

Write the plan as: the destination table, the structural decisions with reasons, the promise edits, the chapter note outline, the research list, and the packet list. Then present it to me in full.

GATE: Stop here and wait for my explicit approval of the plan. Apply any changes I ask for to the plan before continuing. Do not write any file inside the vault before I say go.

PHASE 4: EXECUTE (subagents, in parallel where packets are independent).
Dispatch every packet to its own writing subagent. Independent packets go out together in one batch. A packet that amends a file another packet creates waits for that packet to finish. When the domain status is FIRST RUN, the packet carrying the domain hub goes last, because it describes what the other packets actually produced. Each writing subagent, in this order:
1. Reads `__Meta/Quality-Bar.md` and the exemplar note named in its packet.
2. Reads every existing note it is told to link to, so the link phrase is true and the `up` chain is consistent.
3. Does the research in its packet against primary sources, and records for each item: the finding, the source, and whether it confirms, corrects, or extends the raw notes. Research is for correctness and completeness. It is not licence to replace the raw file's wording with a source's wording.
4. Writes or amends the note to the contract for its kind. New notes get `confidence: draft`, never anything higher. Amended notes get this chapter appended to `sources:`, and the new material is merged into the section it belongs in by topic, sharpening or correcting what is there rather than being appended as a layer. A subsection is created only when the new material is a distinct sub-topic, and it is named for what it contains and never for where it came from. Where the new material contradicts what the note already says, the contradiction is settled by research and the note is rewritten to say one thing; two sections each asserting a different thing is never an acceptable outcome. A Bases query block is part of the required shape of an `index` and every new index gets one; on any other kind it is appended only where the note's neighbours in the same folder carry one.
5. Self-checks before reporting: every required section present and non-empty, `## Where it is used` has at least two outbound links each carrying its relationship phrase, every quantitative claim has a formula somewhere in the note, the discharge string is exact and last in its section if used, no H1 on a note under `Knowledge/` and none on a chapter note, no heading that names a source, no bare "the chapter" or "the book" as a subject in the prose, no dash characters of either kind, no "e.g.", no placement commentary in the body, frontmatter keys spelled exactly, `up` is a link and is never `[[Home]]` for a leaf.
6. Reports back with: files written or changed, with a one-line summary each; every fact in the raw notes it corrected, with the raw wording, the corrected wording, and the source; every claim it added that the raw notes did not contain, with the source; every raw bullet it suspects was pasted from elsewhere rather than written by me, with the raw wording; every research item it could not settle, stated as uncertain rather than guessed; and any deviation from its packet with the reason. A typing slip is not a correction. A misspelling, a transposed word, or a word the surrounding sentence plainly contradicts is fixed silently and listed in one short line at the end of the report, separately from the corrections, so that the corrections section stays a list of places I understood something wrongly rather than a list of places I typed badly.

Keep every report. You will need them in Phase 6.

PHASE 5: VERIFY (you, personally, with scripts where possible).
Run these checks over the whole vault and fix what fails, either yourself for one-line fixes or by sending the owning subagent back for anything larger:
- Every `[[wikilink]]` in `Knowledge/`, `Sources/`, `Home.md` and `README.md` resolves to an existing file, allowing for `|alias` display text and `#heading` suffixes.
- Every note under `Knowledge/` has `note_kind`, `up`, and (where its kind requires them) `aliases`, `sources`, and `confidence`, with the keys spelled exactly. Every `up` value resolves.
- Every new or amended note carries the required `##` sections for its kind.
- No note under `Knowledge/` carries an H1, and neither does any chapter note. Container notes, dataset notes, `Home.md`, `README.md`, `Notation.md` and the files under `__Meta/` do carry one, and this check must not strip it.
- No note under `Knowledge/` carries a heading that names a source, and none uses "the chapter" or "the book" as a bare subject in its prose.
- No file under `Knowledge/`, `Sources/`, `Home.md` or `README.md` contains an em-dash, an en-dash, or the string "e.g.".
- Every note the chapter produced or amended is listed under `## Extracted` in the chapter note, and every note listed there exists and names the chapter in its `sources:`.
- Every promise line from Phase 3d is gone or rewritten.
- No alias appears on two notes, and no note claims an alias that is another note's title. Collect every `aliases` entry and every filename across the vault and check for duplicates mechanically, since this failure is silent in Obsidian and invisible on inspection.
- Every row of the collision ledger landed: the verdict was applied, and where the verdict was DIFFERENT THINGS both notes actually carry the distinguishing sentence and the link.
- The container note links the chapter, `Home.md` counts it, and `README.md`'s layout table counts it. When the domain status is FIRST RUN, `Home.md` also introduces the domain and the hub's Areas list names every folder the run created.
- Every atom in the inventory is accounted for: walk the destination table row by row and confirm the content actually landed where the row says, in substance, not just by file existence. An atom marked DROP has its reason in the plan.
- `git status --short` shows only the files the plan named, plus the entries the working tree parameter says were already there before the run started. Anything else is a mistake to reverse by hand, never with a git command.

PHASE 6: EMPTY THE RAW FILE AND REPORT.
Only when every check in Phase 5 passes, truncate the raw file to zero bytes. Do not delete it and do not move it. Then give me the final report, which has exactly these parts:
1. A table of every file created, amended, renamed, moved or deleted, one row each, with the reason in a few words.
2. Structural decisions taken, one line each, with the reason. When the domain status was FIRST RUN, the domain's membership rule and the evidence behind every folder created come first, because they are the decisions I will be living with for the rest of the book. Any restructuring of an existing domain is stated here in full, with what moved and what prose was rewritten to match.
3. The collision ledger as it was finally resolved: term, the notes on each side, the verdict, and the sentence each note carries. Include the collisions you found that the parameters did not name, and the forward collisions you declined to act on, with what will trigger them.
4. Corrections: every place the notes now say something different from what my raw file said, with the raw wording, the new wording, and the source. This is the most important section. Writing is how I learn, so I need to know exactly where my understanding was wrong. Typing slips do not belong here; they go in one line at the end of this section, counted rather than enumerated.
5. Additions: every substantive claim in the vault that came from research rather than from my raw file, with the source, so I can read them as a reviewer.
6. Suspected pastes: every raw bullet a subagent judged to be someone else's prose rather than mine, with the raw wording, so I can check my own note taking.
7. Unsettled: every research item no subagent could settle from a primary source, with what to check in the book.
8. The chapter note's open questions, restated, and which earlier open questions this chapter settled.
9. The output of `git status --short`, unstaged, uncommitted.
No praise padding and no summary of what a good job was done. Facts, decisions, corrections, and gaps.
```
