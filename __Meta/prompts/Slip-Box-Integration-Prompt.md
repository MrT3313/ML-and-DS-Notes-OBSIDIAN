# Slip box integration prompt

Paste into a fresh conversation with an agent that has file access to the vault and can launch subagents. Fill in the parameter block at the top; nothing below it names a subject, a book, a library, or a folder, so the parameter block is the whole of adapting this prompt to a new source. The prompt has one approval gate, after the plan and before any file is written. To run it end to end without stopping, delete the single line that begins "GATE:".

This is a different role from `Chapter-Study-Prompt.md`. The study prompt's agent is an interrogator and never writes note bodies. This prompt's agent moves notes I already wrote in my own words from the slip box into their permanent place, restating and completing them. Everything it adds beyond what the raw file says is reported back to me at the end so I can read it as a reviewer, not a reader.

One prompt serves every subject. A subject with no domain under `Knowledge/` yet is handled by the FIRST RUN branch in Phase 3c, which is the only place in the prompt that behaves differently for a new subject than for an established one.

---

```
You are the coordinator agent for a knowledge-integration task in an Obsidian vault. The vault root is the current working directory. You direct subagents, you own the plan, and you own the final verification. The subagents do the reading of the repository and the writing of the notes. You personally read only the files named in Phase 2 and Phase 3, plus whatever you need to verify the result.

PARAMETERS.

- Raw notes to integrate: [`_Slip Box (Zettelkasten)/RAW DMLS Ch3 Data Engineering Fundamentals.md`, the only file in the slip box. 171 lines, the last of which carries no trailing newline, 22 headings running three levels deep (nine `##`, nine `###`, four `####`), one table, no code block and no mermaid block. The file contains no dash characters of either kind and no "e.g.", both checked, so those two normalizations are discharged before the run starts and finding either in the final vault means a subagent introduced it. Four of the atom shapes step 3a names are present by inspection, and they are named here so they are not missed rather than decided here. `#### Analytical` at line 117 is the empty atom of this run in its purest form: it holds one line, the expansion of the acronym OLAP and nothing else, while its sibling `#### Transactional` runs nine lines, so its disposition is a decision and not an oversight. `### Javascript Object Notation (JSON)` at line 37 is the same shape one degree less empty, carrying a three-word fragment and then stopping, while the five other formats in the table above it get no heading at all, which makes the heading itself the anomaly rather than its contents. `### NoSQL` at line 79 carries nothing of its own before its first subsection. And line 27 is a bare Wikipedia URL standing alone as a bullet, which is an atom whose disposition is a link or a drop and which is simultaneously the strongest single piece of evidence about where the table beneath it came from. One further shape is particular to this file and has no counterpart in the previous two runs: the enumeration under `- Data Normalization` stops after Second Normal Form, so that atom is neither complete nor empty. Whether the vault states the remaining normal forms from research, states only the two that were read, or says plainly that the reading stopped there, is a decision to take in the plan rather than a gap to leave in a note.]
- Chapter note to produce: [`Sources/Books/DMLS/DMLS Ch03 Data Engineering Fundamentals.md`. The file does not exist at all, so the action is CREATE outright, as in the previous run. Note that the raw file spells the chapter `Ch3` while every chapter note in the vault and every line of [[DMLS]]'s own `## Chapters` list spells it `Ch03`; the zero-padded form is the one that survives, and the raw file's spelling is not inherited by anything.]
- Chapter number, for the `chapter:` key: [3]
- Chapter note `url:`: [https://github.com/chiphuyen/dmls-book/blob/main/resources.md#chapter-3-data-engineering-fundamentals. This follows the scheme the ch01 run settled and both later runs kept, which points at the same `resources.md` with a per-chapter anchor rather than at `summary.md`. Check that the anchor resolves and that the section behind it is chapter 3's; if the file has no such heading, fall back to `summary.md` and then to the repository root, and say in the final report which one was used and why. The container note keeps the repository root either way.]
- Container note: [`Sources/Books/DMLS/DMLS.md`, linked as [[DMLS]]. One edit and one only: chapter 3's line in `## Chapters` is plain text today and becomes a wikilink to the note this run produces. The ch01 run already corrected the numbering and checked all eleven titles against the book's own `ToC.pdf`, so that work is finished and is not reopened; a packet that finds itself editing a second line of that file has drifted and says so instead of editing.]
- Subject domain: [`Knowledge/Data Systems/`, hub at [[Data Systems]]. The first thing the plan says out loud is that the primary domain has changed. The two previous runs of this book were Machine Learning runs and this one is not: it is a chapter of a machine learning book whose material is almost entirely another domain's, and almost every atom in it would still be true if no model were ever fitted on anything it describes, which is [[Data Systems]]'s membership test stated in its own words. The domain holds thirteen notes across four folders, `_Foundations/`, `Workloads/`, `Analytics/` and `Operations/`, every one of them written in a single run from [[DDIA]] chapter 1. It is the smallest established domain in the vault and this chapter is larger than it is. `Knowledge/Machine Learning/` is a neighbour on this run rather than the subject, and it receives at most two or three atoms: the static against dynamic feature split at the end of the file, the line that rows are instances and columns are features, and possibly the malformed-user-input material. A packet that finds itself writing a fourth note into `Knowledge/Machine Learning/` has misread the chapter and says so instead of writing.]
- Domain status: [ESTABLISHED, and barely, which changes what the status buys. Phase 3c step 0 does not apply and is skipped in full: no domain folder is created, no hub is created, and no new line is added to `Home.md` under `## Areas`. `Home.md` owes this run nothing structural, since `## Areas` already carries the [[Data Systems]] line and `## Sources` already carries the [[DMLS]] line, and `README.md`'s layout table describes paths rather than counting notes, so it is expected to need nothing. What does owe something is the hub, and it owes a specific sentence rather than an amendment in general. [[Data Systems]]'s scope paragraph closes: "The domain is one chapter old. All thirteen notes come from [[DDIA Ch01 Trade-Offs in Data Systems Architecture|DDIA chapter 1]], which surveys the architectural choices rather than the mechanisms, so what is on disk is the vocabulary and the shape of the trade-offs rather than how any single system works inside." Every clause of that is false the moment this run lands, and it is false in an interesting direction: the domain stops being one book's and the new material is mechanisms rather than architectural choices. That sentence is rewritten, not appended to. It is the same edit the previous run made to [[Systems Architecture]]'s own "one chapter old" sentence, made for the same reason. The Areas paragraphs are the second and larger pressure point: they are prose naming the notes in each folder one by one, and this chapter is very likely to add folders rather than only notes, which makes the hub the single largest amendment in the run and a packet of its own that goes last.]
- Library pin: [None, and for the same reasons as both previous runs: the book ships no notebook, says outright that it is not a tutorial book, and the raw file contains no code. Every rule below conditioned on a library pin is discharged, and no `## Implementation` section carries a version line because none carries runnable code. The rider, though, is the opposite of the previous run's. That chapter named almost no products; this one names more of them than any chapter extracted so far, and they are the substance rather than the decoration: JSON, CSV, Parquet, Avro, Protobuf, Pickle, BSON, Hadoop, Amazon Redshift, TensorFlow and TFRecord, PyTorch, CockroachDB, Apache Iceberg, DuckDB, Apache Kafka, Amazon Kinesis, NumPy and pandas. Where the raw file or the research names a product, a service, a format or a platform, the note names it as what it is, says what it is an instance of, and does not pretend to be a reference for it. Two riders follow from that. First, this chapter is where a question an existing note left open becomes live: [[DDIA Ch01 Trade-Offs in Data Systems Architecture]] records that [[Cloud-Native Architecture]] carries a twelve-product roster, that the note is therefore doing three jobs at once, and that "whether a product roster belongs inside a concept note at all is decidable the next time a chapter arrives carrying one of its own, which on [[DDIA]]'s chapter list is chapter 4". This chapter arrived first and carries several rosters. The question is answered in the plan or explicitly declined with a reason, and leaving it unaddressed is not an option. Second, there is exactly one place a version line is genuinely owed despite the absence of a pin: the claim that a pandas DataFrame is column-major is a claim about an implementation that has changed, so whatever the vault ends up saying about it names the pandas version it is true of.]
- Quantitative expectation: [Moderate, which is the highest of the three runs so far and moves the risk again rather than removing it. Four atoms carry real rung-two or rung-one content, none of it owned anywhere in the vault, and all four are easy to discharge by accident because the surrounding chapter is vocabulary. The row-major against column-major atom is the strongest: for a table of $n$ rows and $m$ columns, reading $k$ columns touches $nk$ elements in a column store against $nm$ in a row store, the ratio is $k/m$ and does not depend on $n$, and the raw file's own worked example of four features out of thousands is that ratio with numbers in it. The ACID atom is rung two by the quality bar's own wording, since a table of what is guaranteed and what is not is rung two and needs no mathematics. First and Second Normal Form are exact conditions on a relation and are stated precisely with functional dependency, which is rung one and is available from the raw file's own bullets. And the stateful-computation argument at the end of the file is an incremental-computation claim with an arithmetic behind it: recomputing a thirty day window every day touches thirty days of data per run, while maintaining it incrementally touches one day per run plus a join against the stored state, so the saving is a factor of the window length and is stated as one. Two failures carry over from the previous runs and still apply: a discharge written where rung two was available, and padding written above a discharge where a genuinely discharged note should be short. The third failure is specific to this run and is the likeliest. Several notes this chapter amends already carry the fixed discharge string, among them [[Transaction]], [[Data Engineering]] and [[Node]], and an amendment that adds checkable content to such a note while leaving "**Not further quantitative at this depth.**" standing underneath it produces a note that asserts its own emptiness on top of its own content. That failure is invisible on a section-present check and is caught only by reading the section, so the verification in Phase 5 reads every amended `## Formal statement` in full.]
- Primary sources, in order of trust: [the original papers and specifications behind a claim, reached through the book's own `resources.md` at https://github.com/chiphuyen/dmls-book/blob/main/resources.md and through the references in the chapter itself; then the current official documentation of any named system, service, format or platform, read for what it does today rather than for its marketing; then the book's own text if the agent has it. Because this chapter is mostly formats and mechanisms, the specifications are unusually reachable and there is little excuse for a research item settled from a blog post: RFC 8259 for JSON, RFC 4180 for CSV, the Apache Parquet and Apache Avro specifications, the Protocol Buffers documentation, and the CPython `pickle` documentation, which is also where the one security property the raw file omits is stated by the maintainers themselves. For the data models, Codd's 1970 paper is the primary source for the relational model and his own later papers for the normal forms, and the document and graph material has a fuller treatment in [[DDIA]] chapter 3 which this run does not have and must not pretend to. For the transactional guarantees, Haerder and Reuter's 1983 paper is where the acronym ACID was coined and is the citation that settles what each letter was meant to mean, with Pritchett's 2008 ACM Queue article as the source for BASE. For the dataflow material, Fielding's 2000 dissertation is the primary source for REST, and the Apache Kafka and Amazon Kinesis documentation for the transports. The 2022 staleness rider from both previous runs stands, and it bites harder here than anywhere so far, for two separate reasons. The product claims in the transactional and analytical convergence bullet are four years old and at least one of them looks like a category error rather than a stale fact. And the third-party data market the chapter describes has been reshaped since the book went to press by platform policy rather than by technology, so a claim about what is bought and sold is checked against what is true now and dated if it is not. A vendor blog post is not a primary source for a claim about a competitor, and a vendor's own benchmark is not a primary source for its own performance. Wikipedia is not a primary source for anything and may be used only to find one, which matters more in this chapter than in either previous one, because the raw file contains a Wikipedia URL as a bullet of its own immediately above the table it evidently sourced.]
- Exemplar notes, read in Phase 2, one per shape this run will produce: [`Knowledge/Data Systems/Analytics/Extract-Transform-Load.md`, the single most important file to read before anything is planned: it is the only `method` note in the domain, it is the exemplar of the shape this chapter's mechanism material will reach for, and it is the note the ETL atom collides with entirely rather than partly; `Knowledge/Data Systems/Workloads/Online Transaction Processing.md`, which carries the eight-row comparison table the whole `Workloads/` folder turns on and is therefore the exemplar of the paired contrast, which is the dominant shape in this chapter, since row against column, text against binary, structured against unstructured, transactional against analytical and batch against stream are five of them in one file; `Knowledge/Data Systems/Workloads/Transaction.md`, the exemplar of a short honest note that defines its subject, defers what it cannot support, and discharges its formal statement, and simultaneously the note the ACID atom collides with head on; `Knowledge/Data Systems/Analytics/Data Lake.md`, the exemplar of a `## Formal statement` that is exact about what a convention fixes and what it does not without a formula in sight, and the note whose `### File formats that turn up most` subsection already owns Avro and Parquet including their row and column orientation, which means part of this chapter's formats atom is already on disk; `Knowledge/Data Systems/Operations/Data Engineering.md`, for the correct discharge and because the chapter is named after it; `Knowledge/Data Systems/Data Systems.md`, the domain hub, for the Areas paragraphs written as prose naming the notes rather than as a bare list, and because it is the largest amendment in the run; `Knowledge/Systems Architecture/Distribution/Microservices.md`, the neighbouring-domain note that already owns service-to-service requests over a network and already names gRPC and OpenAPI, which is where the request-driven half of the dataflow material collides; `Knowledge/Machine Learning/Paradigms/Training Regime/Online Learning.md`, read for one paragraph only, the sentence separating the two senses of "online", because it is the exact shape three of this run's collisions need and it was written in this vault for this reason; `Knowledge/Machine Learning/Data/Preprocessing/Min-Max Scaling.md`, because it claims the bare alias `normalization` and is therefore an amendment target under the alias rule rather than a note this chapter has anything to say about. Read every one of these. Six of them are files this run will amend, so the rule is sharper here rather than looser: what is copied is the shape, the density and the voice, never the content, and an exemplar that is also a target is read in full before a word of the amendment is written.]
- Exemplar chapter note: [two, and for different reasons. `Sources/Books/DMLS/DMLS Ch02 Introduction to Machine Learning Systems Design.md` is the most recent finished chapter note in the vault and the previous chapter of this same book, so it is read for shape: both of the `## Extracted` groups this run will need, one for notes amended rather than produced and one for hubs and indexes revised, and the "Settled here, having been asked earlier" block that step 3e asks for. `Sources/Books/DDIA/DDIA Ch01 Trade-Offs in Data Systems Architecture.md` is read for content and is the more important of the two on this run: it is the chapter note that created the domain this run is writing into, its `## Extracted` groups are the precedent for a run that lands many notes in one domain at once, and its `## Open questions` name four things this chapter is directly placed to settle or disturb, being the deferral of batch and stream processing, the product-roster question, the overloading of [[Node]], and the scheduling of data models against [[Model]] for [[DDIA]] chapter 3, which this chapter now reaches first.]
- Neighbouring domains: [three, and the traffic runs the other way from the previous two runs. `Knowledge/Machine Learning/`, which is the domain this book belongs to and the domain this chapter mostly is not. It owns [[Feature]] and [[Feature Engineering]], which are where the static against dynamic feature split lands; [[Training Instance]] and [[Feature]] again, which the rows-are-instances line links to rather than restates; [[Batch Learning]] and [[Online Learning]], which hold the other sense of two of this chapter's most important words; [[Poor-Quality Data]], which already owns malformed input as a data problem; and two notes that hold aliases this chapter needs, [[Min-Max Scaling]] with `normalization` and [[Open Data Repositories]] with `data sources`. `Knowledge/Systems Architecture/`, whose membership test is that a note earns a place there when its definition holds for any software system regardless of what that system stores, and which is decisive on exactly one block of this chapter: request-driven against event-driven communication passes that test on its face, and [[Microservices]] already holds half of it. It also holds [[Node]], whose bare title is the collision the graph model atom creates, and [[Object Storage]] and [[Distributed System]], which the storage and stream-processing atoms touch in passing. `Knowledge/Mathematics/`, which this chapter will not touch: the access-cost ratio and the functional dependency conditions are stated where they are used, and a packet that finds itself adding to `Mathematics/` has drifted and says so instead. The cross-domain rule in the hard rules below governs all three: link, never duplicate.]
- Known collisions, to be resolved in the plan under the collision rule before any packet is written, and not an exhaustive list: ["Data model", against [[Model]], which is the fitted function from a feature vector to an output. This collision was scheduled in writing by [[DDIA Ch01 Trade-Offs in Data Systems Architecture]], which recorded that "[[Model]] is the fitted hypothesis, and [[DDIA]] chapter 3 brings data models"; that chapter has not arrived and this one has, so the run that was expected to resolve it is this one. The verdict is DIFFERENT THINGS and the shape is fixed: the new note is titled for the qualified phrase, [[Model]] gains the sentence naming the other sense with a link, and neither side claims the bare word. [[Model]] does not carry `model` in its `aliases` today, which makes this cheap to do correctly and easy to get wrong by adding it. "Node" and "edge", against [[Node]], which claims the bare aliases `node` and `nodes` and defines the word as one participant in a [[Distributed System]], with a four-part test that is about private state, an independent clock, message-only reachability and independent failure. A vertex in a graph data model has none of those properties and is not a machine at all. That note already carries a recorded warning that its title is bare and heavily overloaded, with a decision-tree node scheduled to arrive from [[HOML]] chapter 6, so this is the second collision on one word and the first to actually land. Whatever the graph model atom produces may not claim `node`, `nodes`, `edge` or `edges` unqualified. "Normalization", against [[Min-Max Scaling]], which claims the bare alias `normalization` today, with [[Feature Scaling]] holding `feature normalization` and [[Standardization]] holding `z-score normalization` beside it. Database normalization is a different operation on a different object for a different reason: one rescales a numeric column into a range, the other decomposes a relation to remove redundancy, and nothing about either illuminates the other. This is the sharpest silent collision in the run, because Obsidian resolves by name and a link typed as `normalization` from a new data systems note would land in a scikit-learn preprocessing note with no error anywhere. The verdict is DIFFERENT THINGS, and the alias rule then forces the second half: an ambiguous word is nobody's alias, so [[Min-Max Scaling]] gives up the bare `normalization` or qualifies it, and the new note claims the qualified forms and the normal forms rather than the bare word. "Data sources", against [[Open Data Repositories]], which claims it as an alias and means by it a place to download a public dataset. The chapter's own opening heading means the origins of the data arriving in a running system, being user input, system generated, internal databases and third party. Those are not the same subject and the second is not a special case of the first. DIFFERENT THINGS, neither claims the bare phrase, and both carry the sentence that gets a reader who arrived at the wrong one out of it. "ETL", against [[Extract-Transform-Load]], which already claims `ETL`, `ELT`, both letter orderings spelled out, `data pipeline` and `ETL pipeline`, and which is the only `method` note in the domain, carrying the three steps in both orders, a hyperparameter table, six failure modes and a tool taxonomy. The verdict is SAME THING and the action is AMEND, and creating a note for this atom would duplicate the most complete note the domain has. What the raw file adds beyond it is thin and is to be found rather than invented: an explicit list of what the transform step actually does, being joining, standardizing value ranges, transposing, deduplicating, sorting, aggregating, deriving new features and validating, and the observation that the load destination may be a file, a database or a warehouse. Both are additions to sections that already exist, not subsections of their own, and neither is named for where it came from. "Transactional" and "analytical processing", against [[Online Transaction Processing]], [[Online Analytical Processing]] and [[Hybrid Transactional-Analytical Processing]], all three of which are thorough, and the first two of which already hold the eight-row comparison and the columnar consequence. SAME THING throughout, so this block is AMEND and the plan says so rather than discovering it in Phase 4, and the one-line `#### Analytical` atom is where that is most obvious. Exactly two things here are new. The association of the two workloads with the two storage layouts, which ties this block to the formats block and is the reason the row against column atom probably earns a note. And the convergence bullet's three products, which are a research item before they are a claim: [[Hybrid Transactional-Analytical Processing]] already makes the convergence argument with TiDB as its worked case, and of the three names the raw file offers, one is a table format rather than a database at all, which is a correction in kind and not a stale fact. "ACID", against [[Transaction]], and this is the most consequential collision in the run because it is a promise rather than a word. That note defines the bracket an application draws around a group of operations and then says in its own prose that "which guarantees the database attaches to that boundary, and what happens when two transactions overlap, is the subject of DDIA chapter 8 and is not settled here", and it discharges its `## Formal statement` on exactly that ground. This chapter delivers precisely the deferred material, many chapters of book-time early. The decision is explicit and is taken in the plan: either an `ACID` note under `Workloads/` with [[Transaction]] as its `up`, or an AMEND that rewrites both the deferral sentence and the discharge. What is not allowed is the deferral sentence standing next to the material it defers. Nothing in the vault claims `ACID`, `atomicity`, `isolation`, `durability` or `BASE` today, and the expansion of BASE is in the raw file. `consistency` is the trap inside this atom: it is unclaimed now and [[DDIA]] chapter 10 is named for it in a different and precise sense, so the bare word goes to nobody, and the fact that the C in ACID is a property of the application rather than of the database, and was included largely to make the acronym pronounceable, is the sentence that keeps that door open and is itself a research item against the 1983 paper that coined it. "Batch processing" and "stream processing", against [[Batch Learning]], [[Online Learning]] which claims `streaming learning`, [[Batch Gradient Descent]], [[Mini-Batch Gradient Descent]] and [[Epoch]]. DIFFERENT THINGS, and the vault has already written this exact sentence once in the other direction: [[Online Learning]] carries the paragraph separating its "online" from the "online" of the workload names, added when [[DDIA]] chapter 1 landed. The same treatment is owed here in both directions, since a batch job is a bounded computation over data at rest and a batch in gradient descent is the set of instances one parameter update is computed from, and the two have nothing in common but the word. Two further things about this block. [[DDIA Ch01 Trade-Offs in Data Systems Architecture]] recorded batch and stream processing as the largest deliberate deferral of its own chapter, naming them as building blocks it used without defining, so this run retires part of a debt that note is carrying, and the edit that records that belongs in this run. And [[DDIA]] chapters 11 and 12 are named for these two subjects, so whatever lands here lands knowing it will be amended rather than replaced. "Data warehouse" and "data lake", against [[Data Warehouse]] and [[Data Lake]], which exist, are thorough, and are axed against each other on when the schema is imposed, at write time in one and read time in the other. The chapter re-axes the same pair on structured against unstructured data. Those are close but not identical claims, since when a schema is imposed and whether the data has one at all are different questions, and the failure mode is two sections in one note each asserting a different axis. One note says one thing: the axes are reconciled in the prose or one of them is named as the more precise statement of the other. Note also that the raw file's line giving the lake as the repository for structured data contradicts the line four above it and is a slip carried down from that line, not a claim. "Structured" and "unstructured data", which nothing claims today, and whose disposition is a decision: one note, two notes, or a `## VS` section inside something that already exists. Whichever way it goes, the existing warehouse and lake pair is linked and its axis is not restated. "Services", "REST" and "RPC", against [[Microservices]] in the neighbouring architecture domain, which already defines an architecture in which each service exposes an API that clients and other servers call over the network, already names gRPC as a remote procedure call framework with Protocol Buffers as its wire format, and already names OpenAPI. Most of the request-driven atom is that note's material and the cross-domain rule governs it: link, never duplicate. Decide explicitly whether REST and RPC earn notes of their own, a subsection in [[Microservices]], or a line each, and note that the raw file's "REST & RPC / TRPC" puts a TypeScript library beside two architectural styles, which is a category error and a correction rather than a typing slip. "Real-time transport", "event", "pubsub" and "message queue", none of which is claimed anywhere and which together are the largest genuinely new block in the chapter, with Apache Kafka and Amazon Kinesis as its named systems and neither word appearing anywhere in the vault today. `event` alone is far too ambiguous to be anybody's alias. Where this block lives is the first boundary question below. "Serialization" and the six formats, where the collision is that the vault is quietly ahead of the chapter in one place: [[Data Lake]]'s `### File formats that turn up most` already defines Avro as a row-oriented binary format carrying the writer's schema and Parquet as a column-oriented one, which is a piece of this chapter's formats atom already written, sourced to another book, and sitting in a note about something else. Restating it in a new note is the duplication the cross-domain rule forbids, applied inside one domain and inside one folder. Decide whether each format earns a note, whether one note holds the comparison, or whether the table is an exhibit that stays in the chapter note, and remember that the table's evident source is the Wikipedia page cited one line above it. "Row-major" and "column-major", which nothing claims and which have the strongest case in the chapter for a note of their own, because the vault already leans on the distinction three times without ever defining it: [[Online Analytical Processing]] calls columnar storage the property every downstream design choice follows from, [[Hybrid Transactional-Analytical Processing]] describes row storage in TiKV against column storage in TiFlash, and [[Data Lake]] separates Avro from Parquet on exactly this. This is also the atom with the most rung-two content available. "Latency", which is named here only to be declined again, and which has moved since the previous run. [[DMLS Ch01 Overview of Machine Learning Systems]] left latency and throughput unowned and said they are owed by the first chapter that states one of them precisely enough to check a claim against; [[DMLS Ch02 Introduction to Machine Learning Systems Design]] reported that its chapter contained neither word. This chapter does contain `latency`, three times, and every use is qualitative: data must be processed quickly, an approach is unsuitable under strict latency requirements, and stream processing is often low latency. So the debt is not discharged, the report says so explicitly rather than by silence, and no packet may quietly create either note. [[DDIA]] chapter 2 is still where both words are expected. "Privacy", which nothing under `Knowledge/` claims and which appears in only two notes, [[Cloud Computing]] and [[Self-Hosting]], where it is about which country bytes physically sit in. The raw file's claim, that behavioural tracking is system generated and is nonetheless user data subject to privacy regulation, is a real claim about a boundary and is either a line in whatever the data-sources atom produces or a deferral with a named chapter, not a new subject opened at the end of a packet. Forward collisions to record and decline, each with the chapter that triggers it: storage engine internals, indexes and the structures underneath them, to [[DDIA]] chapter 4, which is the chapter this one's storage-engines heading points at without reaching; schema evolution and encoding compatibility, to [[DDIA]] chapter 5, which is where the formats material is treated at length; replication and sharding, to chapters 6 and 7; isolation levels and what actually happens when two transactions overlap, to chapter 8, which is the half of the ACID atom this chapter does not reach; `consistency` in the distributed sense, to chapter 10; the full treatment of the relational, document and graph models, to chapter 3, which is a chapter-length treatment of what this chapter gives twenty lines to, and which is the reason every note in that block is written to be amended rather than to settle anything; `feature engineering` and the static against dynamic feature split in full, to [[DMLS]] chapter 5; training data, sampling and labelling, to [[DMLS]] chapter 4, which is the very next chapter and takes the same story forward; `data distribution shift`, to [[DMLS]] chapter 8.]
- Domain boundary questions this run must answer: [Three, and the second is the one that governs the shape of the whole run. First, where the modes of data flow live. The three modes are passing data through a database, through a request to a service, and through a real-time transport, and the second and third are request-driven and event-driven communication under other names. That distinction holds for any software system regardless of what it stores, which is [[Systems Architecture]]'s membership test verbatim, and [[Microservices]] already sits in that domain holding the request-driven half. But [[Data Systems]]'s own test is about where the data lives, how it gets there, and who answers a query about it, and "how it gets there" is this material in the chapter's own words. Both tests claim it, so it is cut rather than assumed, and the cut is argued in the plan with the cost of each side named. The precedent to reason from is the previous run, which sent three of four requirements out to the architecture domain and kept the fourth, on the grounds that the general case belongs where the general case lives. Second, and largest, how thin a treatment is allowed to land when the book that owns the subject is already in the vault and has not reached it yet. This chapter is being extracted ahead of [[DDIA]] chapters 2, 3, 4, 5, 8, 11 and 12, every one of which is named for material in this one file. That is the previous run's requirements problem multiplied by six, and it does not have the same answer, because the previous run was moving four notes into a domain that owned them, while this run is writing the first and thinnest account of seven subjects that a chapter-length treatment is already scheduled to replace. The decision is taken per block, not once, and the test each block has to pass is stated so it cannot be dodged: a note written from twenty lines of reading notes is honest if it says what it knows, carries `confidence: draft`, and names the chapter that will deepen it, and is a defect if it reads as though it settled the subject. Where a block cannot meet that bar the plan says which block, defers it with the chapter named, and that deferral is reported rather than quietly executed as a DROP. Third, and consequent on the first two, whether `Knowledge/Data Systems/` gains folders and what they are called. It holds four today. This chapter's material suggests at least two more, one for how data is represented and one for how it is encoded and laid out, and possibly a third for how it moves, depending on the first question's answer. The folder rule applies unchanged, that a folder is earned by holding notes this run actually produces rather than by naming a topic a later book will reach, and the additional test is the one the previous run learned the hard way and recorded: a folder name has to still be the right name on the day the book that owns the subject arrives. That run named `Requirements/` and recorded in its own open questions that a chapter titled "Defining Nonfunctional Requirements" may force a rename plus the prose in the hub that names the folder in words. Do not repeat that by accident. The standing question from the previous runs is reported on and not reopened: `Machine Learning/Operations/` holds four notes, the position is that nothing moves, and the triggers are the folder passing 25 notes or [[DMLS]] chapter 10 arriving. This chapter adds nothing to it, so say so in one line and leave it alone.]
- Working tree at the start of the run: [clean, apart from one untracked directory. `git status --short` reports exactly one line before this run writes anything, `?? "_Slip Box (Zettelkasten)/"`, which is the slip box itself and is expected. The previous run's work is committed in full at abf802b. Anything else appearing in that output at Phase 5 is this run's own doing, and it is either a file the plan named or a mistake to reverse by hand and never with a git command.]

The raw file is my own notes, taken while reading the chapter, in my own words. It is the source of truth for what I understood. The vault has a strict shape and a strict quality bar, and the job is to move every atom of the raw file into that shape, so that at the end the raw file is empty and the vault has grown by one chapter.

Hard rules for the whole session, for you and every subagent:
- Never run `git add`, `git commit`, `git stash`, `git checkout`, `git restore`, or anything else that stages, commits, or discards. `git status`, `git diff`, `git log` and `git show` are allowed, read only.
- Never delete the raw file. It is emptied in Phase 6 by truncating its contents to zero bytes, and only then.
- Never delete an existing note without listing the deletion in the plan and having it approved at the gate.
- Every file is owned by exactly one subagent per phase. Two subagents never write the same file.
- No em-dashes and no en-dashes anywhere, in prose, in tables, or in code comments. Use commas, colons, parentheses, or a new sentence. Normalize any the raw file contains.
- Never write "e.g." Write "ex" or restructure the sentence.
- Every note body is a restatement in the vault owner's voice. The raw file's wording is preferred wherever it already says the thing. Book phrasing is never copied. Anything quoted verbatim from any source is marked as a quote and attributed.
- The preference for the raw file's wording holds only where the raw file is actually in my voice. A raw bullet written in a register the rest of the file does not use, typically a long fluent sentence with marketing adjectives where the surrounding bullets are terse fragments, is very likely pasted from a glossary or a vendor page and must be restated from scratch rather than preserved. Flag every such bullet in the Phase 4 report as a suspected paste, with the raw wording, so I can see which of my own notes were not mine.
- A heading that names a source is a defect, without exception: no `### From Chapter 1`, no `### What the book says`, no `### Kleppmann's version`. New material is merged into the section it belongs in by topic, and any subsection is named for what it contains. "The chapter" and "the book" are never bare subjects in the prose of a note under `Knowledge/`; name the source with a link or state the fact without attribution. Attribution lives in `sources:` and in links.
- A note belongs to exactly one domain. Where this chapter's material overlaps a neighbouring domain, the rule is link, never duplicate: if a note already exists there, amend it and link to it, and do not restate its content in this domain. Where the overlap is real but the existing note says something different rather than something less, that is a research item and a correction, not a second note.
- A word that already exists in the vault is a collision until proved otherwise. Before creating any note whose title, or any alias you would put on it, matches an existing note's title or alias, read that existing note in full and return one of exactly three verdicts, recorded in the plan with the reason:
  - SAME THING. Two names for one concept. One note survives, in the domain that owns the concept, the other name becomes an alias on it, and the second domain links to it and restates nothing.
  - DIFFERENT THINGS. Two notes. Each one's `## Definition` names the other and says in one sentence what separates them, with a link, so a reader who arrived at the wrong note leaves immediately rather than reading the wrong thing carefully. Neither note may claim the bare ambiguous word as an alias.
  - SPECIAL CASE. One is the other applied to a narrower setting. The general note lives in the domain that owns the general case; the specific note points `up` at it, or links to it with a phrase naming exactly what it specializes, and does not repeat the general note's definition.
  The failure this prevents is not a merge conflict, it is silence: two notes describing different things under the same word, neither aware of the other, both looking correct in isolation.
- Aliases are globally unique across the vault. Obsidian resolves a link by name, so two notes claiming one alias makes one of them unreachable under that name with no warning and no error. Before any packet is dispatched, check every seed alias in every packet against every title and alias already in the vault, and against the other packets. A word that is ambiguous on its own is not an alias for anybody: it is qualified in both places or dropped from both alias lists.

Work in six phases. Do not skip a phase and do not start writing files before the gate.

PHASE 1: STRUCTURE (subagents, in parallel, read only).
Launch two read-only exploration subagents at once and wait for both.

Subagent A, the vault contract. Have it read in full and report back, quoting rather than paraphrasing wherever a rule is exact: `__Meta/Quality-Bar.md` (the whole file, it is the spec), `README.md`, `Home.md`, `Notation.md`, `__Meta/Callouts.md` (only the list of callout types), every file in `__Meta/templates/`, and `.obsidian/types.json`. Its report must contain: the frontmatter contract per note_kind, the required section headings per note_kind verbatim, the three rungs of `## Formal statement`, the fixed discharge string for the bottom rung and where in the section that string goes, the hyperparameter test, the disqualification list, the heading rules (no H1 under `Knowledge/` and none on a chapter note, H1 on a container note and on any document read front to back, `##` for required sections, `###` for subsections), the rule against headings that name a source, whether a Bases query block is part of the required shape of an `index`, whether the list heading of an index is fixed or named for what it lists, and whether a forward-looking list of material not yet written is part of an index's shape or a defect in it, the discharge available to `## Implementation` in a domain with no pinned library, and what the file says about adding a new note_kind and when that question reopens.

Subagent B, the vault as it stands. Have it produce: the full tree of `Knowledge/` and `Sources/` with one line per file giving path, note_kind, `up`, and `confidence`; the complete text of every domain hub under `Knowledge/`, meaning the `index` note whose `up` is `[[Home]]`, including each one's scope statement and its Areas list in full; the complete text of the exemplar chapter note named in the parameters; the output of `git log --oneline -n 12`, and then `git show --stat` on the most recent commit that integrated a chapter, as the exemplar of what a chapter integration touches (new notes, the hub, Home, the container note, and backfilled `sources:` and `up:` on existing notes); and, when the domain status is FIRST RUN, additionally `git show --stat 5a1a0c2`, the commit that created the `Mathematics` and `MLOps` domains, as the exemplar of what starting a domain touches. Also have it report every line anywhere in `Knowledge/`, `Sources/`, `Home.md` and `README.md` that mentions the chapter being integrated, the book being integrated, or the subject domain by name, because each one is a promise this run must keep or correct; every `[[wikilink]]` target in the raw file that does not resolve to an existing note; and every existing note whose title or `aliases` collide with a term in the raw file, which is the input to the collision rule above. Finally, have it report which existing notes end with a Bases query block and which do not, so new notes can follow the pattern of their neighbours rather than invent one.

Do not proceed until both reports are in. Keep both reports in your context for the rest of the session.

PHASE 2: READ THE RAW NOTES (you, personally, in full).
Read the raw file from the first line to the last with no offset and no limit. If the file is long, read it in consecutive chunks until you have seen the final line, and say the final line back to yourself so you know you reached it. Do not delegate this. Do not summarize it yet.

Then read, yourself, every exemplar note named in the parameters, and the exemplar chapter note, so you know what a finished note looks like in each shape you will be producing.

PHASE 3: INVENTORY AND PLAN (you, personally).
Build the plan in this order and write it to a scratch file outside the vault, not into the vault.

Step 3a, the inventory. Split the raw file into atoms. An atom is the smallest piece that lands in one place: usually one heading with its bullets, sometimes a single bullet, a single table, or a single code block when its siblings go elsewhere. Number the atoms in file order. Every line of the raw file belongs to exactly one atom. Tables, figures and code blocks that only reproduce an exhibit from the source are still atoms; they are allowed to be dropped, but only by an explicit decision with a reason.

Three shapes of atom need naming before they are missed. A heading with nothing under it is an atom, and its disposition is a decision: either the chapter covered it and I did not write it down, in which case the plan says so and the material is researched or deferred, or it is a stub for a later reading session, in which case it is dropped with that reason. A term listed with an empty definition is an atom, and the first place to look for its definition is later in the same raw file, where a term glossed in one line at the top is often developed in full under its own heading further down; when nothing in the raw file defines it, the note is still owed a definition, which comes from research and is reported as an addition rather than quietly written as though I had said it. A bullet whose content is a bare cross reference, with no claim of its own, is an atom whose disposition is a link rather than a note.

Step 3b, the destination table. One row per atom: atom number, raw heading, action, target path, note_kind, `up`, what this atom contributes, and why here. Actions are exactly one of: CREATE a new note, AMEND an existing note (say which section), RENAME or MOVE an existing note (old path to new path), DELETE an existing note (with the reason and where its content goes), DROP the atom (with the reason). A raw atom may feed several rows, since one passage often carries both the definition of a thing and a claim about a different thing it is being contrasted with. Every unresolved `[[link]]` from the Phase 1 report must appear as a CREATE row or the plan is incomplete.

Step 3c, the structural decisions. Decide and write down, with the reason, every placement question the chapter raises, checked against the Areas list of the domain hub and against the folder rule (the folder says what kind of thing a note is, `up` says what it is part of). Do not add a new note_kind. Every decision here is a decision about where things go and what earns a note, never a decision about what is true.

Step 0 of this step applies only when the domain status is FIRST RUN, and when it applies nothing else in the plan is settled until it is. Read the domain hub named in the exemplars first, because it is the precedent: it was started from a single chapter of evidence, it opens with a paragraph saying what earns a place in the domain and what does not, and it has an Areas list whose entries are folders with the notes in them named in prose. Read it against what Subagent A reports about the index shape rather than copying it wholesale, since a hub written earlier may carry a section the contract has since dropped. Then decide and write down, with the reason:

- The domain's name and folder, and the hub note's path, which is the folder name repeated as the filename, matching `Machine Learning/Machine Learning.md`.
- The membership rule, one paragraph: what earns a place in this domain, and what looks like it belongs but goes to a neighbouring domain instead. Write this as that hub writes it, as a boundary against the neighbours rather than as a description of the subject. This paragraph is the thing that stops the next five chapters from being filed by accident, so it is worth more effort than anything else in this step.
- The folder taxonomy, with the evidence for each folder drawn from the atoms in the inventory and named atom by atom. One chapter is thin evidence, so the default is few folders. A folder is earned by holding notes this run actually produces, not by naming a topic the book will reach later; a topic with one note and a promise of more is a note at the top level of the domain, and the folder arrives with the second note. State plainly which folders are being created on one note of evidence and why that one note could not sit at the top level instead.
- Which atoms are not domain material at all. A chapter that opens a subject typically carries organizational material, meaning who does which job and what the teams are called, and it carries vendor and product names. Decide whether each of those is a note, a line inside another note, or a drop, and if the organizational material is a note, decide whether this domain or a neighbouring one owns it.
- The hub note itself: `note_kind: index`, `up: "[[Home]]"`, no H1, a scope paragraph, `## Areas` rather than `## Methods`, and a Bases query block, which every index carries. Whether it also carries a list of material not yet written is decided by what Subagent A reports, not by what an older hub happens to contain: an index that states what is on disk is honest about a domain one chapter old by being short, and a list of predicted arrivals is a claim made on the weakest evidence there is.
- The line in `Home.md` under `## Domains` that introduces the domain, in the shape of the lines already there, and whether `README.md`'s layout table needs the domain named too.

The rest of step 3c applies to every run. At minimum decide: which atoms become notes at all, given that a term the chapter merely names in passing is a line in another note rather than a note of its own, and say which terms you are refusing a note to and why; for each note, whether it is a `concept`, a `method` or an `index`, checked against the concept and method test in section 2 of the quality bar (a method is something you carry out as itself and that changes something downstream, or an ordered protocol whose reading depends on the order; everything else is a concept) and against the rule that `index` is earned by having members to list and that a topic with none is a `concept` carrying its own definition; whether any existing note should convert between kinds under that same rule; which notes are paired contrasts that want the optional `## VS` section rather than two notes each describing the other; which atoms touch a neighbouring domain, and for each one whether the existing note there is amended and linked or whether this domain owns a genuinely different thing under a similar name; and whether a dataset note is required, given that the quality bar wants one per dataset a chapter works on. Where a decision contradicts a promise an existing note made, say which promise is kept and which is corrected, and never leave both standing.

Also produce, as its own table, the collision ledger: one row per term in this chapter that matches an existing note's title or alias, whether the parameters named it or Subagent B found it, giving the term, the existing note, the new note or amendment, the verdict of SAME THING, DIFFERENT THINGS or SPECIAL CASE, and the exact sentence each side will carry to tell itself apart from the other. A term with no row is a term nobody checked.

Do not reach for a new note_kind. The quality bar says when that question reopens, and the answer is never "during an integration run". A note that fits no existing kind comfortably is written to the kind it fits least badly and the strain is recorded in the final report and in the chapter note's `## Open questions`, which is the evidence a kind is eventually argued from.

One decision is larger than placement and gets its own line in the plan when it arises: whether this run's material argues for restructuring an existing domain rather than only adding to one. A domain that turns out to be a special case of something this chapter introduces is a real finding and is allowed to move. It is proposed at the gate with its cost named in full: every file that moves, every `up` that changes, and every sentence in `Home.md`, `README.md` and the other hubs that names the old path in words, since wikilinks survive a folder move and prose does not. Never restructure silently and never as a side effect of a packet. Where the cost is small today and grows with every chapter, say so plainly, because that is an argument for doing it now rather than an argument against doing it at all.

Step 3d, the promises. For every line the Phase 1 report found that mentions this chapter, this book, or this subject, write the exact edit that retires the promise. At minimum this covers: the container note, whose chapter line becomes a link, or, where the link is already there, whose link must now resolve to a written note; `Home.md`, whose `## Sources` list records the book and whose `## Areas` list records the domain; `README.md`, wherever it describes what lives under each path or counts what the vault holds; the domain hub's Areas paragraphs, which name the notes in each folder in prose and so go stale the moment a folder gains one; and every note in a neighbouring domain that names this subject or promised this material. Add any others the report found. A promise that this run cannot keep is rewritten to say what is actually true, not left to rot.

Step 3e, the chapter note. Plan the chapter note at the path in the parameters, from `__Meta/templates/Note Kinds/Source (Chapter).md`: frontmatter with `note_kind: source`, `medium: book`, `chapter:` as the number from the parameters, `up:` as a link to the container note, `url:` from the parameters, and aliases for the chapter number and the chapter title. Then `## Thesis`, `## Extracted` grouped by theme with a group for notes amended rather than produced and a group for hubs and indexes revised, and `## Open questions`. It takes no H1. Also plan which open questions in earlier chapter notes, of this book and of any other, this chapter settles or partly settles, and what edit records that.

Step 3f, the research list. For every claim in the raw file that is uncertain, marked with a question mark, internally inconsistent, or likely wrong, write a research item: the claim, what to check, and which primary source settles it, using the ordering in the parameters. Do the same for every named system, product, service, protocol or standard in the raw file: what it is, who makes it, whether it still exists under that name, and whether the property the raw file attributes to it is true of it today rather than at the time some older source described it. A product list in the raw file is a research item in its own right, because product lists go stale faster than anything else in a note, and a list that was right when the book went to press may not be right now. Where the raw file carries formulas, every one gets rederived or checked against a reference; where it carries none, this clause is discharged and an arrow or an inequality used as shorthand in prose is not a formula.

Step 3g, the work packets. Group the destination rows into packets such that no file appears in two packets, and packets are independent of one another. Then fix, before any packet is dispatched, the final title of every note that will be created or renamed, because every packet links to notes owned by other packets and the link text must already be right. Each packet contains: the atom text verbatim, the target file or files and the action on each, the note_kind and its exact required sections from the Phase 1 report, `up`, a seed list of aliases (every name a person would type in a link, including plurals, abbreviations, the expansion of every acronym and the acronym of every expansion, and the name any widely used system gives the thing), the outbound links this note must make and the phrase that explains each relationship, the notes elsewhere that will link back to it, the research items that belong to it, the exemplar note to read first, and the style rules from the hard rules above. Every packet also carries: "Do not touch any file not named in this packet. Do not run git write commands. Report back before finishing."

Write the plan as: the destination table, the structural decisions with reasons, the promise edits, the chapter note outline, the research list, and the packet list. Then present it to me in full.

GATE: Stop here and wait for my explicit approval of the plan. Apply any changes I ask for to the plan before continuing. Do not write any file inside the vault before I say go.

PHASE 4: EXECUTE (subagents, in parallel where packets are independent).
Dispatch every packet to its own writing subagent. Independent packets go out together in one batch. A packet that amends a file another packet creates waits for that packet to finish. When the domain status is FIRST RUN, the packet carrying the domain hub goes last, because it describes what the other packets actually produced. Each writing subagent, in this order:
1. Reads `__Meta/Quality-Bar.md` and the exemplar note named in its packet.
2. Reads every existing note it is told to link to, so the link phrase is true and the `up` chain is consistent.
3. Does the research in its packet against primary sources, and records for each item: the finding, the source, and whether it confirms, corrects, or extends the raw notes. Research is for correctness and completeness. It is not licence to replace the raw file's wording with a source's wording.
4. Writes or amends the note to the contract for its kind. New notes get `confidence: draft`, never anything higher. Amended notes get this chapter appended to `sources:`, and the new material is merged into the section it belongs in by topic, sharpening or correcting what is there rather than being appended as a layer. A subsection is created only when the new material is a distinct sub-topic, and it is named for what it contains and never for where it came from. Where the new material contradicts what the note already says, the contradiction is settled by research and the note is rewritten to say one thing; two sections each asserting a different thing is never an acceptable outcome. A Bases query block is part of the required shape of an `index` and every new index gets one; on any other kind it is appended only where the note's neighbours in the same folder carry one.
5. Self-checks before reporting: every required section present and non-empty, `## Where it is used` has at least two outbound links each carrying its relationship phrase, every quantitative claim has a formula somewhere in the note, the discharge string is exact and last in its section if used, no H1 on a note under `Knowledge/` and none on a chapter note, no heading that names a source, no bare "the chapter" or "the book" as a subject in the prose, no dash characters of either kind, no "e.g.", no placement commentary in the body, frontmatter keys spelled exactly, `up` is a link and is never `[[Home]]` for a leaf.
6. Reports back with: files written or changed, with a one-line summary each; every fact in the raw notes it corrected, with the raw wording, the corrected wording, and the source; every claim it added that the raw notes did not contain, with the source; every raw bullet it suspects was pasted from elsewhere rather than written by me, with the raw wording; every research item it could not settle, stated as uncertain rather than guessed; and any deviation from its packet with the reason. A typing slip is not a correction. A misspelling, a transposed word, or a word the surrounding sentence plainly contradicts is fixed silently and listed in one short line at the end of the report, separately from the corrections, so that the corrections section stays a list of places I understood something wrongly rather than a list of places I typed badly.

Keep every report. You will need them in Phase 6.

PHASE 5: VERIFY (you, personally, with scripts where possible).
Run these checks over the whole vault and fix what fails, either yourself for one-line fixes or by sending the owning subagent back for anything larger:
- Every `[[wikilink]]` in `Knowledge/`, `Sources/`, `Home.md` and `README.md` resolves to an existing file, allowing for `|alias` display text and `#heading` suffixes.
- Every note under `Knowledge/` has `note_kind`, `up`, and (where its kind requires them) `aliases`, `sources`, and `confidence`, with the keys spelled exactly. Every `up` value resolves.
- Every new or amended note carries the required `##` sections for its kind.
- No note under `Knowledge/` carries an H1, and neither does any chapter note. Container notes, dataset notes, `Home.md`, `README.md`, `Notation.md` and the files under `__Meta/` do carry one, and this check must not strip it.
- No note under `Knowledge/` carries a heading that names a source, and none uses "the chapter" or "the book" as a bare subject in its prose.
- No file under `Knowledge/`, `Sources/`, `Home.md` or `README.md` contains an em-dash, an en-dash, or the string "e.g.".
- Every note the chapter produced or amended is listed under `## Extracted` in the chapter note, and every note listed there exists and names the chapter in its `sources:`.
- Every promise line from Phase 3d is gone or rewritten.
- No alias appears on two notes, and no note claims an alias that is another note's title. Collect every `aliases` entry and every filename across the vault and check for duplicates mechanically, since this failure is silent in Obsidian and invisible on inspection.
- Every row of the collision ledger landed: the verdict was applied, and where the verdict was DIFFERENT THINGS both notes actually carry the distinguishing sentence and the link.
- The container note links the chapter, `Home.md` counts it, and `README.md`'s layout table counts it. When the domain status is FIRST RUN, `Home.md` also introduces the domain and the hub's Areas list names every folder the run created.
- Every atom in the inventory is accounted for: walk the destination table row by row and confirm the content actually landed where the row says, in substance, not just by file existence. An atom marked DROP has its reason in the plan.
- `git status --short` shows only the files the plan named, plus the entries the working tree parameter says were already there before the run started. Anything else is a mistake to reverse by hand, never with a git command.

PHASE 6: EMPTY THE RAW FILE AND REPORT.
Only when every check in Phase 5 passes, truncate the raw file to zero bytes. Do not delete it and do not move it. Then give me the final report, which has exactly these parts:
1. A table of every file created, amended, renamed, moved or deleted, one row each, with the reason in a few words.
2. Structural decisions taken, one line each, with the reason. When the domain status was FIRST RUN, the domain's membership rule and the evidence behind every folder created come first, because they are the decisions I will be living with for the rest of the book. Any restructuring of an existing domain is stated here in full, with what moved and what prose was rewritten to match.
3. The collision ledger as it was finally resolved: term, the notes on each side, the verdict, and the sentence each note carries. Include the collisions you found that the parameters did not name, and the forward collisions you declined to act on, with what will trigger them.
4. Corrections: every place the notes now say something different from what my raw file said, with the raw wording, the new wording, and the source. This is the most important section. Writing is how I learn, so I need to know exactly where my understanding was wrong. Typing slips do not belong here; they go in one line at the end of this section, counted rather than enumerated.
5. Additions: every substantive claim in the vault that came from research rather than from my raw file, with the source, so I can read them as a reviewer.
6. Suspected pastes: every raw bullet a subagent judged to be someone else's prose rather than mine, with the raw wording, so I can check my own note taking.
7. Unsettled: every research item no subagent could settle from a primary source, with what to check in the book.
8. The chapter note's open questions, restated, and which earlier open questions this chapter settled.
9. The output of `git status --short`, unstaged, uncommitted.
No praise padding and no summary of what a good job was done. Facts, decisions, corrections, and gaps.
```
